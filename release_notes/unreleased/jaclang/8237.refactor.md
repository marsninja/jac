- **Refactor: the type system's keystone module reaches native codegen**: `types.jac` produced no native IR at all, because `ClassType` declared `ClassDetailsShared` and `ClassDetailsPrivate` as nested archetypes and the native lane cannot lower a nested-archetype field type, so the module refused codegen at its own `private:` declaration and nothing downstream in `jaclang.compiler.type_system` could link it; both objs are now module-scope siblings of `ClassType` with names, fields and defaults unchanged across their ~30 reference sites, `UnionType.postinit` normalizes its optional `types` list into an annotated local before iterating it the way its sibling postinits already do, and `TypeEvaluator.postinit` stops holding CPython `ast` handles for two nested closures that now live in `_read_dunder_all` and `_read_ambient_imports_by_module`, private methods that own their own demotion instead of refusing the whole 196-ability module. `types.jac` now lowers with 13 seams instead of 27 and none, the evaluator's census records 171 measured seams instead of nothing at all, and `test_type_system_seal_census.jac` pins all of it one waived seam at a time the way the unitree seal census does.
