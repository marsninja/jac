- **Native: `isinstance` decides an imported archetype**: a check against a class defined in another module now lowers natively even when that class carries no vtable slot, which is what a flat archetype gets whenever the module defining it neither subclasses nor `isinstance`-tests it. Identity comes from the class name each instantiation already records, matched against the program-wide ancestor table, so an imported base matches through an imported MRO. Two hierarchy facts were corrected alongside: the module that defines a class now owns its recorded parents and MRO, and a check whose targets sit past the point where a consumer's own linearization stops now refuses out loud instead of answering `False` for a base the operand genuinely derives from.
