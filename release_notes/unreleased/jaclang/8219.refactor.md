- **Refactor: the sealable closure drops its permanently-Python imports**: Ten modules the native seal must close over reached for Python-only machinery for work Jac (or plain data) already expressed, and each one made its whole module a placement blocker. `pascal_to_snake` and `native_safe_sym` spell their ASCII-only `re.sub` character classes as code-point loops; `helpers.jac`'s module scope reduces to `import os` alone after the `Jdb` debugger, the `jac.toml` reader with `MalformedJacTomlError`, and `dump_traceback` move to `jac0core/debugger.jac`, `jac0core/tomlio.jac`, and `jac0core/traceback_render.jac` (the exception keeps a single class identity, so existing `except MalformedJacTomlError` sites are unaffected); the type evaluator's `ast` symbols move into the one `postinit` that uses them, converting a whole-module refusal into a per-callable demotion; `SymTabBuildPass`'s `dir(TestCase())` sweep and `StaticAnalysisPass`'s `dir(builtins)` membership test become frozen name tables generated from CPython 3.14; the version-comparison `operator` dispatch dict becomes a plain comparison chain; `native_target_os` reads `sys.platform` instead of `platform.system()`; and `BaseTransform.cancel_token` is annotated `any`, matching what all ten concrete passes already declared.
