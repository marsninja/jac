- **Refactor: client-kind config resolution split from its path predicates**: `mobui_module_exempt` and a new `mobui_enforced_in` move to `jaclang.project.client_kind_paths`, which has no project-config dependency, while `jaclang.project.client_kind` keeps `resolve_client_kind_context` and reaches the config through a new `get_config_for_dir` that takes the directory as a string. `JsxIntrinsicGuardPass` now takes the predicate from the pure module and the resolution from the config lane, so its native closure no longer refuses on an imported module that produced no IR; `client_kind` reduces to three per-callable seams that are genuinely Python-side config reads. Behavior is unchanged in both lanes.
