- **Fix: escape and comptime diagnostics stop sharing a code**: A merge artifact left two different diagnostics registered as E0109 (comptime expansion and the unsupported `\N{...}` escape refusal), silently shadowing one in reports and refusing the native link of the diagnostics module. Comptime keeps E0109; the escape refusal is E0110. Also clears the control-flow pass's checker escalations and its Python-package-init import as groundwork for a future sealing wave.
