- **Compiler: retire the last bare dotted imports**: `compiler/symbol_utils.jac` and `jac0core/const_fold.jac` now bind symbols instead of `import jaclang.jac0core.unitree as uni`, which the placement scanner read as a non-native import and which kept both modules out of every native unit. `fold_const` also spells its return type, so the module keeps its IR instead of refusing on an unparameterized `tuple` the moment it becomes placeable.
