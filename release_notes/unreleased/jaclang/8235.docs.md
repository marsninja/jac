- **Docs: corrected two native seal-waiver rationales**: The waived seams `Import.is_service_import.__get__` and `Import.__jac_detected.__get__` now name the blocker the census actually reports. The first calls `is_service_module_stem` from `jac0core.service_cut`, not `jac0core.ext_registry`, and demotes with an E5092 lowering failure rather than an E5090 import; the second is blocked first by its own E5090 import of `jac0core.ext_registry` and only then by `resolve_relative_path`. Waiver membership and behavior are unchanged.
