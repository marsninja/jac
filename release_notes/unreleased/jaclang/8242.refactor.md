- **Refactor: import orchestration and annex discovery move into the compile driver**: `SymTabBuildPass` resolved import paths and compiled dependencies from inside `_exit_import_absorb`, and `JacAnnexPass` walked the filesystem and re-entered the parser from `_merge_annex`; the compile driver is now the only place that resolves paths, reads files and triggers dependency compiles. `JacCompiler.resolve_absorb_imports` stamps each absorb import's dependency module on the new `Import.absorbed_mod` slot before any `SymTabBuildPass` run, and `JacCompiler.load_annex_modules` parses the `.impl.jac`/`.test.jac` annexes onto the new `Module.annex_mods` slot before `JacAnnexPass` runs, leaving that pass a pure tree merge. Resolution order, hub-hit semantics, the exception surface of nested dependency compiles, annex merge order and error attribution are unchanged.
