- **Refactor: import detection and boundary classification become driver-stamped data**: The compile driver now answers every filesystem, config, and program-state question an import raises and stamps the results on the Import node (`jac_detected` for the is-jac verdict, `ImportBoundaryFacts` for native/service/pinned/bridged classification) before `SymTabBuildPass` and `BoundaryAnalysisPass` run, so the passes consume data instead of resolving paths, reading files, or constructing compilers mid-traversal. The symbol-binding helpers (`bind_assignment_targets` and friends) and `ability_of_symbol` move into `unitree`, whose vocabulary they read. Behavior is unchanged; this clears the wave-2 native-seal refusals for `sym_tab_build_pass` and `decl_impl_match_pass` and reduces `boundary_analysis_pass` to its manifest-vocabulary family.
