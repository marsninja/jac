- **A syntax error is reported once, not once per import path or reload** (#7960): a module that failed to parse was never cached, so every path that reached it in a build (the import walk, the type checker, the placement solver, the client-bundle interop worklist) re-parsed the file and appended another full copy of its alerts to the shared `errors_had`. That list is append-only, so the copies also accumulated across compiles: a broken module reported 2 errors on the first HMR reload, 4 on the second, 6 on the third. One missing brace surfaced as N identical diagnostics in the dev-server error overlay, `/__build_status`, and editor diagnostics. A partial module is now published to the hub under the same key a clean one gets, the recorded failure stops the client-bundle worklist from re-asking it for interop facts it can never produce, and the HMR reloader drops a module's stale alerts through `invalidate_module` before re-importing. Boot-time duplication, from a module compiled once for the server and again for the client bundle, is not addressed here.
