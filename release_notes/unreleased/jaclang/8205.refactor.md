- **Refactor: jac0core compiler passes take unitree by symbol import**: The ten jac0core pass families that reached the unified tree through `import jaclang.jac0core.unitree as uni;` now use `import from jaclang.jac0core.unitree { ... }` and bare node names at all 1,201 reference sites, because a module-object import of a jac module cannot join a native compilation unit while a symbol import compiles and links as a separate one; `jac gen-uni-dispatch` emits the new form for the generated visitor surface, and `EndpointEffectPass`'s writer/touch/visit state plus `SymTabBuildPass.cur_sym_tab` are declared as `has` fields instead of being stashed dynamically in `before_pass`.
