- **Native Python standard-library modules**: Run bisect, heap, and random operations in native Jac while preserving Python object protocols, and exclude their original CPython C sources from release builds.
