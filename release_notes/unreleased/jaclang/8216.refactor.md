- **Refactor: the type checker and type system take their jac modules by symbol import**: `type_checker_pass` and the seven `compiler/type_system` modules reached unitree, each other and CPython's `ast` through module-object imports (`import ... as uni;`, `import from jaclang.compiler.type_system { types }`, `import ast as py_ast;`), which cannot join a native compilation unit; all 2,277 reference sites now read link-time symbols, with `SymbolType` folded onto the constant binding unitree re-exports, `ParamKind` and `Enum` aliased where two modules define the same name, and the `ast` names aliased on the way in. `TypeCheckPass.exit_sub_tag` loses its `SubTag[uni.T]` annotation, whose `T` was never a unitree attribute and only survived because jac keeps annotations as strings; the erased meaning `SubTag` is now written out, which retires two spurious errors and six warnings from the checker's own diagnostics.
