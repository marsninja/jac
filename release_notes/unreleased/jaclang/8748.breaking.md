- **Compiler: the graph-shaped compiler (#8744)**: syntax nodes are immutable object-spatial nodes, tree structure and semantic relations are typed edges, the type system is nodes and edges in the same graph, and every pass is a walker. Node fields that passes used to assign (`kid`, `body`, `type`, `sym`, ...) are read through accessors over edges. The jac0 seed tier now lowers the object-spatial subset (`node`/`edge`/`walker`, connect, edge references, `spawn`, `visit`, abilities) so the seed compiler is graph-shaped too. Also fixes the bytecode kernel's quadratic connect (#8743, hash only).
- **Runtime: subtree-fenced `visit:0:`**: front-splicing visits (`visit:0: targets;`) now give walkers true depth-first traversal with enter/exit abilities bracketing each subtree (a node's exit fires when its subtree completes, not when the queue drains). Plain `visit` keeps the legacy queue semantics. The kernel also gained `osp_skip_rest` (suppress the current node's remaining abilities), per-walker `__jac_post_entry__`/`__jac_post_exit__` callbacks, and a typed-dispatch match cache.
- **Compiler: pass hooks are typed OSP abilities**: the `enter_<node>`/`exit_<node>` visitor surface (`accept_enter`/`accept_exit` double dispatch, ~460 generated stubs) is deleted. Passes declare `can enter_x with IfStmt entry { ... }` and the walker kernel dispatches them by trigger type; ability triggers use `isinstance` semantics, so hooks on types with node subclasses carry explicit exact-type guards where the old exact-match behaviour matters. Third-party passes using `def enter_<node>` methods no longer dispatch and must convert to abilities. `jac gen-uni-dispatch` is removed.
- **Compiler: the type system is part of the graph**: every type (`ClassType`, `FunctionType`, `UnionType`, `TypeVarType`, ...) is an object-spatial node, and type composition is typed edges (`TypeOf` from expressions to their types, `BaseOf`/`MroOf`/`TypeArgOf`, `ParamOf`/`ReturnsOf`, `UnionMemberOf`, `BoundOf`, overloads, enum members). Field APIs are preserved through accessors (edges are the source of truth; hot reads memoize per node). Identical class types intern by declaration identity, and the type evaluator is a walker (`expr spawn evaluator` types a subtree on demand).
- **Compiler: graph lifetime and scheduling (WS5)**: `JacProgram.release_module` releases one module's graph -- it severs every edge crossing the module boundary (placement edges to codespace singletons, `TypeOf` edges to interned types, the program root's ties) so the tree is garbage-collectable while the program lives (regression-tested with weakrefs). Pass scheduling speaks edge kinds: writers declare what they produce (`SymTabBuildPass` -> `SymOf`/`Defines`/`InScope`, `CFGBuildPass` -> `CfgSucc`/`CfgTrue`/`CfgFalse`, `TypeCheckPass` -> `TypeOf`, `DeclImplMatchPass` -> `ImplOf`, placement -> `PlacedIn`/`DecidedCodespace`) and hard readers require them; every schedule is validated against these declarations when it is built. After a module's pipeline completes, `FreezeStructurePass` locks its tree: structural mutation (`set_kids`/`replace_kid`/`_role_set`) on a frozen node raises, with `thaw_structure()` as the deliberate escape. `set_parent` no longer fabricates a non-kid edge -- synthesized nodes carry a `_ctx_parent` context reference instead. LSP definition/references already run on `Defines`/`Uses` edges via the symbol accessors.
- **Runtime: the kernel's adjacency is indexed and ordered (K1)**: `osp_graph` keeps, per node, one insertion-ordered chain per (direction, edge type), populated with the edge class's MRO tags so a hop on a base edge type sees every subclass row; rows carry flag bits (`FLAG_IN_KID`, `FLAG_ONE_SIDED`) and an optional key for (node, type, key) lookups (`osp_key_put` / `osp_key_rows`); `osp_conn2` takes an ordered-insert position; one-sided rows are reachable from their source only and never register or pin their target; row removal is O(1) through prev links; dedup is a stamped table instead of `o not in out`. The existing `osp_conn` / `osp_refs` / `osp_refs_many` / `osp_disconn_between` / `osp_region_died` signatures are unchanged. The sv driver's `sv_conn` connects through `osp_conn2` (flags from the edge's `in_kid` / `in_kids` attributes, `before_a` for ordered insert) and `sv_live_edges` reads the typed chain instead of filtering every edge with `isinstance`.
- **Compiler: the seed tier lives on the kernel (K3)**: `jac0core/osp0` no longer keeps its own adjacency in `anchor.edges`; `connect0` / `refs0` / `destroy0` mint handles in a seed store and read the kernel's typed chains (`in_kid == True` folds into the row-flag filter). Module lifetime is a kernel region: `parse_str` opens one per module (and `run_pass` for a `PythonModuleAst` root), passes re-enter it, a node joins the region of the first already-registered peer it connects to, and `release_module` / `hub.reset` ask for it to close. A close is deferred while the module's compile is in flight (from `parse_str` until `compile()` returns, or while its region is on the region stack) or while rows from another live region still point into it (`Uses` back-references excepted); it then closes when its referrers do, a cycle of pending regions closing together (`region_close` retires every row touching the module's nodes and frees their handles; stub modules are never closed by `reset`). `JAC_OSP_NO_REGION_CLOSE=1` disables closing for diagnosis. `_conn_light` is `connect0(..., one_sided=True)`; `sever_external_edges` and `detached_copy` (`clone_subtree`) work on rows; `UniNode._after_clone` resets per-node state on a clone.
- **Runtime: `NodeAnchor.edges` is gone (K2)**: the kernel's rows are the only edge store. `JacGraph.connect` writes once (row flags see `conn_assign` attributes), `graph_walk.walk` reads the typed chain for each hop, `save` / `destroy` / `disconnect` enumerate live edges through the sv driver, and `remove_edge` / `edge_delta` are deleted. A persisted node's edge ids are hydrated into rows by `sv_register_hydrated`; until then they sit in the private `_edge_stubs` payload (serializer, session `_materialize`, `__setstate__`), which is not a live reader. `NodeAnchor.is_populated` is "has an archetype". `NodeAnchor(archetype=...)` takes no `edges` argument.
- **Compiler: edges without objects (K4)**: an edge class with no fields beyond the row flags (`Role` and its subclasses, `SymOf`, `Defines`, `InScope`, `TypeOf`, ...) is a row and nothing else: no `EdgeAnchor`, no archetype instance. `[edge x ->:T:->]` materialises a proxy instance on demand (same class, `in_kid` from the row flags, `__jac__` giving `source` / `target`), so `del e`, `e.in_kid` and `getattr(e, "__jac__").target.archetype` keep working. Edge classes with fields (`ScopePrimary.alias`, `ScopeChild.in_kids`, `TypeMemberOf.name`, `EnumMemberOf.name`) keep an object.
- **Compiler: role-less tokens leave the row store (K5)**: punctuation and keyword tokens that carry no role are no longer edges; each node keeps a per-node layout (tokens and `(role, ordinal)` markers in source order) and `kid` is derived from it, so `kid` order, positional `kid[i]` reads, `loc` and the formatter's token interleaving are unchanged while those rows and their handles disappear. Tokens reach their parent through the context reference. Role-bearing tokens (`Name`, operators, strings) stay nodes with rows.
- **Runtime: walker dispatch skips empty ability lists (K6)** before computing a node's tag.
- **Retired (K7)**: `FreezeStructurePass`, `freeze_structure` / `thaw_structure` / `_struct_frozen` (nodes are rows the kernel owns; the freeze was a guard on setters that no longer exist). Scope inserts also index the `ScopePrimary` row under the symbol name in the kernel (`osp_key_put`); the per-scope name dicts remain the bytecode fast path.
- **Retired**: `jac gen-uni-dispatch` and the generated visitor surface, `AstTool.pass_template` (emitted templates for the deleted visitor dispatch), `UniPass.term_signal`, and the role-name string bridge.
- **Compiler: `JAC_COMPILER_DIGEST_PIN`**: a developer knob that freezes the compiler-tree digest folded into every JIR's environment key, so an edit to the compiler recompiles only the modules whose own source changed. Unset, behaviour is unchanged.
