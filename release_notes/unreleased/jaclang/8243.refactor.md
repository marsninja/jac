- **Refactor: passes accumulate diagnostics as data and the driver delivers them**: `Transform.emit` reached through the `JacProgram` handle on every diagnostic and re-read project config per emission, a Python seam inside every pass body. Emission is now pure accumulation on the pass-local `errors_had`/`warnings_had` lists, suppression is a lookup against the new `DiagnosticPolicy` data object resolved once per schedule, and the driver's `run_pass` delivers the survivors to the program ledger after the pass body finishes, including when it raises, so `Transform.ice` still records its `E9001`. `BaseTransform` also drops its per-instance logger field in favor of resolving the logger at `log_info` time. Suppression semantics, diagnostic ordering and the program ledger contents are unchanged.
