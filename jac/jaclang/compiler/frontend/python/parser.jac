"""jacpython PEG parser (unified eval + file profile).

Originally generated from CPython 3.14.6 Grammar/python.gram (starts: eval, file).
Maintained directly in Jac.
"""

import from jaclang.compiler.frontend.python.syntax_error {
    syntax_failure,
    split_source_lines,
    source_character_column
}

import from jaclang.compiler.frontend.python.token_model {
    ENDMARKER,
    NAME,
    NUMBER,
    STRING,
    OP,
    NT_OFFSET,
    NEWLINE,
    TYPE_COMMENT,
    INDENT,
    DEDENT,
    FSTRING_START,
    FSTRING_MIDDLE,
    FSTRING_END,
    TSTRING_START,
    TSTRING_MIDDLE,
    TSTRING_END,
}
import from jaclang.compiler.frontend.python.peg_runtime {
    peg_parser,
    peg_parser_from_source,
    peg_set_keywords,
    peg_set_soft_keywords,
    peg_keyword_entry,
    peg_mark,
    peg_reset,
    peg_check_memo,
    peg_insert_memo,
    peg_update_memo,
    peg_expect_token,
    peg_expect_soft_keyword,
    peg_expect_forced_token,
    peg_expect_forced_result,
    peg_has_error,
    peg_fill_token,
    peg_get_last_nonnwhitespace_token,
    peg_positive_lookahead_token,
    peg_negative_lookahead_token,
    peg_left_rec_finish,
    peg_token,
    peg_reset_for_error_pass,
    STAR_TARGETS,
    DEL_TARGETS,
    FOR_TARGETS,
}
import from jaclang.compiler.frontend.python.parser_actions {
    kv_pair,
    kp_pair,
    keyword_or_starred_node,
    name_default_pair,
    pa_cmpop_expr_pair,
    pa_key_value_pair,
    pa_key_pattern_pair,
    pa_keyword_or_starred,
    pa_name_default_pair,
    pa_make_cmpop_pair,
    pa_name_from_token,
    pa_name_id,
    pa_number_from_token,
    pa_ast_expression,
    pa_ast_binop,
    pa_ast_unaryop,
    pa_ast_boolop,
    pa_ast_compare,
    pa_ast_call,
    pa_ast_ifexp,
    pa_constant_bool,
    pa_constant_none,
    pa_constant_ellipsis,
    pa_constant_from_expr,
    pa_match_singleton,
    pa_pattern_list,
    pa_singleton_seq,
    pa_singleton_seq_expr,
    pa_singleton_seq_stmt,
    pa_singleton_seq_alias,
    pa_stmt_list_or_empty,
    pa_seq_insert_front_expr,
    pa_seq_insert_front_pattern,
    pa_seq_insert_front_stmt,
    pa_seq_insert_front_alias,
    pa_seq_insert_front_type_param,
    pa_seq_insert_front_withitem,
    pa_seq_insert_front_arg,
    pa_seq_insert_front_kw_or_starred,
    pa_seq_insert_front_kvpair,
    pa_seq_insert_front_kvpattern,
    pa_seq_insert_front,
    pa_seq_append_to_end,
    pa_seq_append_to_end_expr,
    pa_seq_append_to_end_stmt,
    pa_get_cmpops,
    pa_get_exprs,
    pa_get_keys,
    pa_get_values,
    pa_get_patterns,
    pa_get_pattern_keys,
    pa_collect_call_seqs,
    pa_call_from_optional_args,
    pa_ast_starred,
    pa_check,
    pa_make_module,
    pa_seq_flatten,
    pa_set_context,
    pa_map_names_to_ids,
    pa_ast_expr_stmt,
    pa_ast_assign,
    pa_ast_annassign,
    pa_ast_return,
    pa_ast_pass,
    pa_ast_break,
    pa_ast_continue,
    pa_ast_tuple,
    pa_ast_attribute,
    pa_ast_subscript,
    pa_ast_slice,
    pa_ast_augassign,
    pa_ast_delete,
    pa_raise_syntax,
    pa_raise_syntax_known_expr,
    pa_raise_syntax_known_range,
    pa_raise_invalid_target,
    pa_aug_op,
    pa_call_args,
    pa_call_keywords,
    pa_comp_field,
    pa_empty_arguments,
    pa_dummy_name,
    pa_interactive_exit,
    pa_seq_count_dots,
    pa_seq_first,
    pa_seq_last,
    pa_seq_len,
    pa_seq_get,
    pa_or_pattern_singleton,
    pa_check_legacy_stmt_or_raise,
    pa_raise_type_param_error,
    pa_raise_kvpair_error,
    pa_checked_future_import,
    pa_nonparen_genexp_in_call,
    pa_arguments_parsing_error,
    pa_check_legacy_stmt,
    pa_concatenate_strings,
    pa_concatenate_tstrings,
    pa_constant_from_string,
    pa_constant_from_token,
    pa_constant_from_fstring_middle,
    pa_decoded_constant_from_token,
    pa_ensure_imaginary,
    pa_ensure_real,
    pa_get_expr_name,
    pa_get_last_comprehension_item,
    pa_join_names_with_dot,
    pa_join_sequences,
    pa_join_sequences_kw_or_starred,
    pa_alias_for_star,
    pa_add_type_comment_to_arg,
    pa_seq_delete_starred_exprs,
    pa_seq_extract_starred_exprs,
    pa_setup_full_format_spec,
    pa_check_fstring_conversion,
    pa_function_def_decorators,
    pa_class_def_decorators,
    pa_make_arguments,
    pa_star_etc,
    pa_slash_with_default,
    pa_joined_str,
    pa_template_str,
    pa_formatted_value,
    pa_interpolation,
    pa_err_occurred,
    pa_expr_lineno,
    pa_expr_end_col_offset,
    pa_expr_end_lineno,
}
import from jaclang.compiler.frontend.python.ast_nodes {
    ast_node,
    expr,
    mod,
    stmt,
    pattern,
    operator,
    cmpop,
    expr_context,
    arguments,
    alias,
    arg,
    keyword,
    comprehension,
    excepthandler,
    match_case,
    type_param,
    withitem,
    Expression,
    Interactive,
    FunctionType,
    Module,
    Name,
    Load,
    Store,
    Del,
    Assign,
    AnnAssign,
    AugAssign,
    Expr,
    Return,
    Pass,
    Break,
    Continue,
    Delete,
    Raise,
    Import,
    ImportFrom,
    Global,
    Nonlocal,
    Assert,
    If,
    For,
    AsyncFor,
    While,
    With,
    AsyncWith,
    FunctionDef,
    AsyncFunctionDef,
    ClassDef,
    Try,
    TryStar,
    ExceptHandler,
    Match,
    TypeAlias,
    Attribute,
    Subscript,
    Slice,
    Tuple,
    List,
    Dict,
    Set,
    Starred,
    NamedExpr,
    Lambda,
    BinOp,
    UnaryOp,
    BoolOp,
    Compare,
    Call,
    IfExp,
    Await,
    Yield,
    YieldFrom,
    ListComp,
    SetComp,
    DictComp,
    GeneratorExp,
    Constant,
    JoinedStr,
    TemplateStr,
    FormattedValue,
    Interpolation,
    MatchAs,
    MatchOr,
    MatchSequence,
    MatchMapping,
    MatchClass,
    MatchStar,
    MatchSingleton,
    MatchValue,
    Add,
    Sub,
    Mult,
    Div,
    FloorDiv,
    Mod,
    MatMult,
    Pow,
    LShift,
    RShift,
    BitOr,
    BitXor,
    BitAnd,
    USub,
    UAdd,
    Not,
    Invert,
    Or,
    And,
    Lt,
    LtE,
    Gt,
    GtE,
    Eq,
    NotEq,
    In,
    IsNot,
    Is,
    NotIn,
    TypeVar,
    TypeVarTuple,
    ParamSpec,
}

glob RULE__gather_101: int = NT_OFFSET + 0,
     RULE__gather_104: int = NT_OFFSET + 1,
     RULE__gather_106: int = NT_OFFSET + 2,
     RULE__gather_108: int = NT_OFFSET + 3,
     RULE__gather_113: int = NT_OFFSET + 4,
     RULE__gather_115: int = NT_OFFSET + 5,
     RULE__gather_118: int = NT_OFFSET + 6,
     RULE__gather_121: int = NT_OFFSET + 7,
     RULE__gather_124: int = NT_OFFSET + 8,
     RULE__gather_127: int = NT_OFFSET + 9,
     RULE__gather_131: int = NT_OFFSET + 10,
     RULE__gather_138: int = NT_OFFSET + 11,
     RULE__gather_140: int = NT_OFFSET + 12,
     RULE__gather_142: int = NT_OFFSET + 13,
     RULE__gather_17: int = NT_OFFSET + 14,
     RULE__gather_21: int = NT_OFFSET + 15,
     RULE__gather_23: int = NT_OFFSET + 16,
     RULE__gather_3: int = NT_OFFSET + 17,
     RULE__gather_33: int = NT_OFFSET + 18,
     RULE__gather_38: int = NT_OFFSET + 19,
     RULE__gather_42: int = NT_OFFSET + 20,
     RULE__gather_62: int = NT_OFFSET + 21,
     RULE__gather_70: int = NT_OFFSET + 22,
     RULE__gather_97: int = NT_OFFSET + 23,
     RULE__gather_99: int = NT_OFFSET + 24,
     RULE__loop0_1: int = NT_OFFSET + 25,
     RULE__loop0_100: int = NT_OFFSET + 26,
     RULE__loop0_103: int = NT_OFFSET + 27,
     RULE__loop0_105: int = NT_OFFSET + 28,
     RULE__loop0_107: int = NT_OFFSET + 29,
     RULE__loop0_109: int = NT_OFFSET + 30,
     RULE__loop0_112: int = NT_OFFSET + 31,
     RULE__loop0_114: int = NT_OFFSET + 32,
     RULE__loop0_116: int = NT_OFFSET + 33,
     RULE__loop0_117: int = NT_OFFSET + 34,
     RULE__loop0_120: int = NT_OFFSET + 35,
     RULE__loop0_123: int = NT_OFFSET + 36,
     RULE__loop0_126: int = NT_OFFSET + 37,
     RULE__loop0_129: int = NT_OFFSET + 38,
     RULE__loop0_130: int = NT_OFFSET + 39,
     RULE__loop0_134: int = NT_OFFSET + 40,
     RULE__loop0_135: int = NT_OFFSET + 41,
     RULE__loop0_137: int = NT_OFFSET + 42,
     RULE__loop0_139: int = NT_OFFSET + 43,
     RULE__loop0_141: int = NT_OFFSET + 44,
     RULE__loop0_16: int = NT_OFFSET + 45,
     RULE__loop0_20: int = NT_OFFSET + 46,
     RULE__loop0_22: int = NT_OFFSET + 47,
     RULE__loop0_24: int = NT_OFFSET + 48,
     RULE__loop0_26: int = NT_OFFSET + 49,
     RULE__loop0_28: int = NT_OFFSET + 50,
     RULE__loop0_29: int = NT_OFFSET + 51,
     RULE__loop0_32: int = NT_OFFSET + 52,
     RULE__loop0_35: int = NT_OFFSET + 53,
     RULE__loop0_37: int = NT_OFFSET + 54,
     RULE__loop0_41: int = NT_OFFSET + 55,
     RULE__loop0_46: int = NT_OFFSET + 56,
     RULE__loop0_47: int = NT_OFFSET + 57,
     RULE__loop0_59: int = NT_OFFSET + 58,
     RULE__loop0_61: int = NT_OFFSET + 59,
     RULE__loop0_67: int = NT_OFFSET + 60,
     RULE__loop0_68: int = NT_OFFSET + 61,
     RULE__loop0_69: int = NT_OFFSET + 62,
     RULE__loop0_79: int = NT_OFFSET + 63,
     RULE__loop0_80: int = NT_OFFSET + 64,
     RULE__loop0_91: int = NT_OFFSET + 65,
     RULE__loop0_95: int = NT_OFFSET + 66,
     RULE__loop0_98: int = NT_OFFSET + 67,
     RULE__loop1_110: int = NT_OFFSET + 68,
     RULE__loop1_111: int = NT_OFFSET + 69,
     RULE__loop1_125: int = NT_OFFSET + 70,
     RULE__loop1_13: int = NT_OFFSET + 71,
     RULE__loop1_132: int = NT_OFFSET + 72,
     RULE__loop1_133: int = NT_OFFSET + 73,
     RULE__loop1_14: int = NT_OFFSET + 74,
     RULE__loop1_15: int = NT_OFFSET + 75,
     RULE__loop1_18: int = NT_OFFSET + 76,
     RULE__loop1_25: int = NT_OFFSET + 77,
     RULE__loop1_27: int = NT_OFFSET + 78,
     RULE__loop1_36: int = NT_OFFSET + 79,
     RULE__loop1_54: int = NT_OFFSET + 80,
     RULE__loop1_72: int = NT_OFFSET + 81,
     RULE__loop1_73: int = NT_OFFSET + 82,
     RULE__loop1_8: int = NT_OFFSET + 83,
     RULE__loop1_81: int = NT_OFFSET + 84,
     RULE__loop1_83: int = NT_OFFSET + 85,
     RULE__loop1_84: int = NT_OFFSET + 86,
     RULE__loop1_88: int = NT_OFFSET + 87,
     RULE__loop1_89: int = NT_OFFSET + 88,
     RULE__loop1_92: int = NT_OFFSET + 89,
     RULE__loop1_93: int = NT_OFFSET + 90,
     RULE__tmp_10: int = NT_OFFSET + 91,
     RULE__tmp_102: int = NT_OFFSET + 92,
     RULE__tmp_11: int = NT_OFFSET + 93,
     RULE__tmp_119: int = NT_OFFSET + 94,
     RULE__tmp_12: int = NT_OFFSET + 95,
     RULE__tmp_122: int = NT_OFFSET + 96,
     RULE__tmp_128: int = NT_OFFSET + 97,
     RULE__tmp_136: int = NT_OFFSET + 98,
     RULE__tmp_143: int = NT_OFFSET + 99,
     RULE__tmp_144: int = NT_OFFSET + 100,
     RULE__tmp_145: int = NT_OFFSET + 101,
     RULE__tmp_146: int = NT_OFFSET + 102,
     RULE__tmp_147: int = NT_OFFSET + 103,
     RULE__tmp_148: int = NT_OFFSET + 104,
     RULE__tmp_149: int = NT_OFFSET + 105,
     RULE__tmp_150: int = NT_OFFSET + 106,
     RULE__tmp_151: int = NT_OFFSET + 107,
     RULE__tmp_152: int = NT_OFFSET + 108,
     RULE__tmp_153: int = NT_OFFSET + 109,
     RULE__tmp_154: int = NT_OFFSET + 110,
     RULE__tmp_155: int = NT_OFFSET + 111,
     RULE__tmp_156: int = NT_OFFSET + 112,
     RULE__tmp_157: int = NT_OFFSET + 113,
     RULE__tmp_19: int = NT_OFFSET + 114,
     RULE__tmp_2: int = NT_OFFSET + 115,
     RULE__tmp_30: int = NT_OFFSET + 116,
     RULE__tmp_31: int = NT_OFFSET + 117,
     RULE__tmp_34: int = NT_OFFSET + 118,
     RULE__tmp_39: int = NT_OFFSET + 119,
     RULE__tmp_4: int = NT_OFFSET + 120,
     RULE__tmp_40: int = NT_OFFSET + 121,
     RULE__tmp_43: int = NT_OFFSET + 122,
     RULE__tmp_44: int = NT_OFFSET + 123,
     RULE__tmp_45: int = NT_OFFSET + 124,
     RULE__tmp_48: int = NT_OFFSET + 125,
     RULE__tmp_49: int = NT_OFFSET + 126,
     RULE__tmp_5: int = NT_OFFSET + 127,
     RULE__tmp_50: int = NT_OFFSET + 128,
     RULE__tmp_51: int = NT_OFFSET + 129,
     RULE__tmp_52: int = NT_OFFSET + 130,
     RULE__tmp_53: int = NT_OFFSET + 131,
     RULE__tmp_55: int = NT_OFFSET + 132,
     RULE__tmp_56: int = NT_OFFSET + 133,
     RULE__tmp_57: int = NT_OFFSET + 134,
     RULE__tmp_58: int = NT_OFFSET + 135,
     RULE__tmp_6: int = NT_OFFSET + 136,
     RULE__tmp_60: int = NT_OFFSET + 137,
     RULE__tmp_63: int = NT_OFFSET + 138,
     RULE__tmp_64: int = NT_OFFSET + 139,
     RULE__tmp_65: int = NT_OFFSET + 140,
     RULE__tmp_66: int = NT_OFFSET + 141,
     RULE__tmp_7: int = NT_OFFSET + 142,
     RULE__tmp_71: int = NT_OFFSET + 143,
     RULE__tmp_74: int = NT_OFFSET + 144,
     RULE__tmp_75: int = NT_OFFSET + 145,
     RULE__tmp_76: int = NT_OFFSET + 146,
     RULE__tmp_77: int = NT_OFFSET + 147,
     RULE__tmp_78: int = NT_OFFSET + 148,
     RULE__tmp_82: int = NT_OFFSET + 149,
     RULE__tmp_85: int = NT_OFFSET + 150,
     RULE__tmp_86: int = NT_OFFSET + 151,
     RULE__tmp_87: int = NT_OFFSET + 152,
     RULE__tmp_9: int = NT_OFFSET + 153,
     RULE__tmp_90: int = NT_OFFSET + 154,
     RULE__tmp_94: int = NT_OFFSET + 155,
     RULE__tmp_96: int = NT_OFFSET + 156,
     RULE_annotated_rhs: int = NT_OFFSET + 157,
     RULE_annotation: int = NT_OFFSET + 158,
     RULE_args: int = NT_OFFSET + 159,
     RULE_arguments: int = NT_OFFSET + 160,
     RULE_as_pattern: int = NT_OFFSET + 161,
     RULE_assert_stmt: int = NT_OFFSET + 162,
     RULE_assignment: int = NT_OFFSET + 163,
     RULE_assignment_expression: int = NT_OFFSET + 164,
     RULE_atom: int = NT_OFFSET + 165,
     RULE_attr: int = NT_OFFSET + 166,
     RULE_augassign: int = NT_OFFSET + 167,
     RULE_await_primary: int = NT_OFFSET + 168,
     RULE_bitwise_and: int = NT_OFFSET + 169,
     RULE_bitwise_or: int = NT_OFFSET + 170,
     RULE_bitwise_xor: int = NT_OFFSET + 171,
     RULE_block: int = NT_OFFSET + 172,
     RULE_break_stmt: int = NT_OFFSET + 173,
     RULE_capture_pattern: int = NT_OFFSET + 174,
     RULE_case_block: int = NT_OFFSET + 175,
     RULE_class_def: int = NT_OFFSET + 176,
     RULE_class_def_raw: int = NT_OFFSET + 177,
     RULE_class_pattern: int = NT_OFFSET + 178,
     RULE_closed_pattern: int = NT_OFFSET + 179,
     RULE_compare_op_bitwise_or_pair: int = NT_OFFSET + 180,
     RULE_comparison: int = NT_OFFSET + 181,
     RULE_complex_number: int = NT_OFFSET + 182,
     RULE_compound_stmt: int = NT_OFFSET + 183,
     RULE_conjunction: int = NT_OFFSET + 184,
     RULE_continue_stmt: int = NT_OFFSET + 185,
     RULE_decorators: int = NT_OFFSET + 186,
     RULE_default: int = NT_OFFSET + 187,
     RULE_del_stmt: int = NT_OFFSET + 188,
     RULE_del_t_atom: int = NT_OFFSET + 189,
     RULE_del_target: int = NT_OFFSET + 190,
     RULE_del_targets: int = NT_OFFSET + 191,
     RULE_dict: int = NT_OFFSET + 192,
     RULE_dictcomp: int = NT_OFFSET + 193,
     RULE_disjunction: int = NT_OFFSET + 194,
     RULE_dotted_as_name: int = NT_OFFSET + 195,
     RULE_dotted_as_names: int = NT_OFFSET + 196,
     RULE_dotted_name: int = NT_OFFSET + 197,
     RULE_double_star_pattern: int = NT_OFFSET + 198,
     RULE_double_starred_kvpair: int = NT_OFFSET + 199,
     RULE_double_starred_kvpairs: int = NT_OFFSET + 200,
     RULE_elif_stmt: int = NT_OFFSET + 201,
     RULE_else_block: int = NT_OFFSET + 202,
     RULE_eq_bitwise_or: int = NT_OFFSET + 203,
     RULE_eval: int = NT_OFFSET + 204,
     RULE_except_block: int = NT_OFFSET + 205,
     RULE_except_star_block: int = NT_OFFSET + 206,
     RULE_expression: int = NT_OFFSET + 207,
     RULE_expression_without_invalid: int = NT_OFFSET + 208,
     RULE_expressions: int = NT_OFFSET + 209,
     RULE_factor: int = NT_OFFSET + 210,
     RULE_file: int = NT_OFFSET + 211,
     RULE_finally_block: int = NT_OFFSET + 212,
     RULE_for_if_clause: int = NT_OFFSET + 213,
     RULE_for_if_clauses: int = NT_OFFSET + 214,
     RULE_for_stmt: int = NT_OFFSET + 215,
     RULE_fstring: int = NT_OFFSET + 216,
     RULE_fstring_conversion: int = NT_OFFSET + 217,
     RULE_fstring_format_spec: int = NT_OFFSET + 218,
     RULE_fstring_full_format_spec: int = NT_OFFSET + 219,
     RULE_fstring_middle: int = NT_OFFSET + 220,
     RULE_fstring_replacement_field: int = NT_OFFSET + 221,
     RULE_func_type: int = NT_OFFSET + 222,
     RULE_func_type_comment: int = NT_OFFSET + 223,
     RULE_function_def: int = NT_OFFSET + 224,
     RULE_function_def_raw: int = NT_OFFSET + 225,
     RULE_genexp: int = NT_OFFSET + 226,
     RULE_global_stmt: int = NT_OFFSET + 227,
     RULE_group: int = NT_OFFSET + 228,
     RULE_group_pattern: int = NT_OFFSET + 229,
     RULE_gt_bitwise_or: int = NT_OFFSET + 230,
     RULE_gte_bitwise_or: int = NT_OFFSET + 231,
     RULE_guard: int = NT_OFFSET + 232,
     RULE_if_stmt: int = NT_OFFSET + 233,
     RULE_imaginary_number: int = NT_OFFSET + 234,
     RULE_import_from: int = NT_OFFSET + 235,
     RULE_import_from_as_name: int = NT_OFFSET + 236,
     RULE_import_from_as_names: int = NT_OFFSET + 237,
     RULE_import_from_targets: int = NT_OFFSET + 238,
     RULE_import_name: int = NT_OFFSET + 239,
     RULE_import_stmt: int = NT_OFFSET + 240,
     RULE_in_bitwise_or: int = NT_OFFSET + 241,
     RULE_interactive: int = NT_OFFSET + 242,
     RULE_invalid_ann_assign_target: int = NT_OFFSET + 243,
     RULE_invalid_arguments: int = NT_OFFSET + 244,
     RULE_invalid_arithmetic: int = NT_OFFSET + 245,
     RULE_invalid_as_pattern: int = NT_OFFSET + 246,
     RULE_invalid_assignment: int = NT_OFFSET + 247,
     RULE_invalid_block: int = NT_OFFSET + 248,
     RULE_invalid_case_block: int = NT_OFFSET + 249,
     RULE_invalid_class_argument_pattern: int = NT_OFFSET + 250,
     RULE_invalid_class_def_raw: int = NT_OFFSET + 251,
     RULE_invalid_class_pattern: int = NT_OFFSET + 252,
     RULE_invalid_comprehension: int = NT_OFFSET + 253,
     RULE_invalid_def_raw: int = NT_OFFSET + 254,
     RULE_invalid_default: int = NT_OFFSET + 255,
     RULE_invalid_del_stmt: int = NT_OFFSET + 256,
     RULE_invalid_dict_comprehension: int = NT_OFFSET + 257,
     RULE_invalid_dotted_as_name: int = NT_OFFSET + 258,
     RULE_invalid_double_starred_kvpairs: int = NT_OFFSET + 259,
     RULE_invalid_double_type_comments: int = NT_OFFSET + 260,
     RULE_invalid_elif_stmt: int = NT_OFFSET + 261,
     RULE_invalid_else_stmt: int = NT_OFFSET + 262,
     RULE_invalid_except_star_stmt: int = NT_OFFSET + 263,
     RULE_invalid_except_star_stmt_indent: int = NT_OFFSET + 264,
     RULE_invalid_except_stmt: int = NT_OFFSET + 265,
     RULE_invalid_except_stmt_indent: int = NT_OFFSET + 266,
     RULE_invalid_expression: int = NT_OFFSET + 267,
     RULE_invalid_factor: int = NT_OFFSET + 268,
     RULE_invalid_finally_stmt: int = NT_OFFSET + 269,
     RULE_invalid_for_if_clause: int = NT_OFFSET + 270,
     RULE_invalid_for_stmt: int = NT_OFFSET + 271,
     RULE_invalid_for_target: int = NT_OFFSET + 272,
     RULE_invalid_fstring_conversion_character: int = NT_OFFSET + 273,
     RULE_invalid_fstring_replacement_field: int = NT_OFFSET + 274,
     RULE_invalid_group: int = NT_OFFSET + 275,
     RULE_invalid_if_stmt: int = NT_OFFSET + 276,
     RULE_invalid_import: int = NT_OFFSET + 277,
     RULE_invalid_import_from_as_name: int = NT_OFFSET + 278,
     RULE_invalid_import_from_targets: int = NT_OFFSET + 279,
     RULE_invalid_kvpair: int = NT_OFFSET + 280,
     RULE_invalid_kwarg: int = NT_OFFSET + 281,
     RULE_invalid_kwds: int = NT_OFFSET + 282,
     RULE_invalid_lambda_kwds: int = NT_OFFSET + 283,
     RULE_invalid_lambda_parameters: int = NT_OFFSET + 284,
     RULE_invalid_lambda_parameters_helper: int = NT_OFFSET + 285,
     RULE_invalid_lambda_star_etc: int = NT_OFFSET + 286,
     RULE_invalid_legacy_expression: int = NT_OFFSET + 287,
     RULE_invalid_match_stmt: int = NT_OFFSET + 288,
     RULE_invalid_named_expression: int = NT_OFFSET + 289,
     RULE_invalid_parameters: int = NT_OFFSET + 290,
     RULE_invalid_parameters_helper: int = NT_OFFSET + 291,
     RULE_invalid_star_etc: int = NT_OFFSET + 292,
     RULE_invalid_starred_expression: int = NT_OFFSET + 293,
     RULE_invalid_starred_expression_unpacking: int = NT_OFFSET + 294,
     RULE_invalid_string_tstring_concat: int = NT_OFFSET + 295,
     RULE_invalid_try_stmt: int = NT_OFFSET + 296,
     RULE_invalid_tstring_conversion_character: int = NT_OFFSET + 297,
     RULE_invalid_tstring_replacement_field: int = NT_OFFSET + 298,
     RULE_invalid_type_param: int = NT_OFFSET + 299,
     RULE_invalid_type_params: int = NT_OFFSET + 300,
     RULE_invalid_while_stmt: int = NT_OFFSET + 301,
     RULE_invalid_with_item: int = NT_OFFSET + 302,
     RULE_invalid_with_stmt: int = NT_OFFSET + 303,
     RULE_invalid_with_stmt_indent: int = NT_OFFSET + 304,
     RULE_inversion: int = NT_OFFSET + 305,
     RULE_is_bitwise_or: int = NT_OFFSET + 306,
     RULE_isnot_bitwise_or: int = NT_OFFSET + 307,
     RULE_items_pattern: int = NT_OFFSET + 308,
     RULE_key_value_pattern: int = NT_OFFSET + 309,
     RULE_keyword_pattern: int = NT_OFFSET + 310,
     RULE_keyword_patterns: int = NT_OFFSET + 311,
     RULE_kvpair: int = NT_OFFSET + 312,
     RULE_kwarg_or_double_starred: int = NT_OFFSET + 313,
     RULE_kwarg_or_starred: int = NT_OFFSET + 314,
     RULE_kwargs: int = NT_OFFSET + 315,
     RULE_kwds: int = NT_OFFSET + 316,
     RULE_lambda_kwds: int = NT_OFFSET + 317,
     RULE_lambda_param: int = NT_OFFSET + 318,
     RULE_lambda_param_maybe_default: int = NT_OFFSET + 319,
     RULE_lambda_param_no_default: int = NT_OFFSET + 320,
     RULE_lambda_param_with_default: int = NT_OFFSET + 321,
     RULE_lambda_parameters: int = NT_OFFSET + 322,
     RULE_lambda_params: int = NT_OFFSET + 323,
     RULE_lambda_slash_no_default: int = NT_OFFSET + 324,
     RULE_lambda_slash_with_default: int = NT_OFFSET + 325,
     RULE_lambda_star_etc: int = NT_OFFSET + 326,
     RULE_lambdef: int = NT_OFFSET + 327,
     RULE_list: int = NT_OFFSET + 328,
     RULE_listcomp: int = NT_OFFSET + 329,
     RULE_literal_expr: int = NT_OFFSET + 330,
     RULE_literal_pattern: int = NT_OFFSET + 331,
     RULE_lt_bitwise_or: int = NT_OFFSET + 332,
     RULE_lte_bitwise_or: int = NT_OFFSET + 333,
     RULE_mapping_pattern: int = NT_OFFSET + 334,
     RULE_match_stmt: int = NT_OFFSET + 335,
     RULE_maybe_sequence_pattern: int = NT_OFFSET + 336,
     RULE_maybe_star_pattern: int = NT_OFFSET + 337,
     RULE_name_or_attr: int = NT_OFFSET + 338,
     RULE_named_expression: int = NT_OFFSET + 339,
     RULE_nonlocal_stmt: int = NT_OFFSET + 340,
     RULE_noteq_bitwise_or: int = NT_OFFSET + 341,
     RULE_notin_bitwise_or: int = NT_OFFSET + 342,
     RULE_open_sequence_pattern: int = NT_OFFSET + 343,
     RULE_or_pattern: int = NT_OFFSET + 344,
     RULE_param: int = NT_OFFSET + 345,
     RULE_param_maybe_default: int = NT_OFFSET + 346,
     RULE_param_no_default: int = NT_OFFSET + 347,
     RULE_param_no_default_star_annotation: int = NT_OFFSET + 348,
     RULE_param_star_annotation: int = NT_OFFSET + 349,
     RULE_param_with_default: int = NT_OFFSET + 350,
     RULE_parameters: int = NT_OFFSET + 351,
     RULE_params: int = NT_OFFSET + 352,
     RULE_pass_stmt: int = NT_OFFSET + 353,
     RULE_pattern: int = NT_OFFSET + 354,
     RULE_pattern_capture_target: int = NT_OFFSET + 355,
     RULE_patterns: int = NT_OFFSET + 356,
     RULE_positional_patterns: int = NT_OFFSET + 357,
     RULE_power: int = NT_OFFSET + 358,
     RULE_primary: int = NT_OFFSET + 359,
     RULE_raise_stmt: int = NT_OFFSET + 360,
     RULE_real_number: int = NT_OFFSET + 361,
     RULE_return_stmt: int = NT_OFFSET + 362,
     RULE_sequence_pattern: int = NT_OFFSET + 363,
     RULE_set: int = NT_OFFSET + 364,
     RULE_setcomp: int = NT_OFFSET + 365,
     RULE_shift_expr: int = NT_OFFSET + 366,
     RULE_signed_number: int = NT_OFFSET + 367,
     RULE_signed_real_number: int = NT_OFFSET + 368,
     RULE_simple_stmt: int = NT_OFFSET + 369,
     RULE_simple_stmts: int = NT_OFFSET + 370,
     RULE_single_compound_stmt: int = NT_OFFSET + 371,
     RULE_single_subscript_attribute_target: int = NT_OFFSET + 372,
     RULE_single_target: int = NT_OFFSET + 373,
     RULE_slash_no_default: int = NT_OFFSET + 374,
     RULE_slash_with_default: int = NT_OFFSET + 375,
     RULE_slice: int = NT_OFFSET + 376,
     RULE_slices: int = NT_OFFSET + 377,
     RULE_star_annotation: int = NT_OFFSET + 378,
     RULE_star_atom: int = NT_OFFSET + 379,
     RULE_star_etc: int = NT_OFFSET + 380,
     RULE_star_expression: int = NT_OFFSET + 381,
     RULE_star_expressions: int = NT_OFFSET + 382,
     RULE_star_named_expression: int = NT_OFFSET + 383,
     RULE_star_named_expressions: int = NT_OFFSET + 384,
     RULE_star_pattern: int = NT_OFFSET + 385,
     RULE_star_target: int = NT_OFFSET + 386,
     RULE_star_targets: int = NT_OFFSET + 387,
     RULE_star_targets_list_seq: int = NT_OFFSET + 388,
     RULE_star_targets_tuple_seq: int = NT_OFFSET + 389,
     RULE_starred_expression: int = NT_OFFSET + 390,
     RULE_statement: int = NT_OFFSET + 391,
     RULE_statement_newline: int = NT_OFFSET + 392,
     RULE_statements: int = NT_OFFSET + 393,
     RULE_string: int = NT_OFFSET + 394,
     RULE_strings: int = NT_OFFSET + 395,
     RULE_subject_expr: int = NT_OFFSET + 396,
     RULE_sum: int = NT_OFFSET + 397,
     RULE_t_lookahead: int = NT_OFFSET + 398,
     RULE_t_primary: int = NT_OFFSET + 399,
     RULE_target_with_star_atom: int = NT_OFFSET + 400,
     RULE_term: int = NT_OFFSET + 401,
     RULE_try_stmt: int = NT_OFFSET + 402,
     RULE_tstring: int = NT_OFFSET + 403,
     RULE_tstring_format_spec: int = NT_OFFSET + 404,
     RULE_tstring_format_spec_replacement_field: int = NT_OFFSET + 405,
     RULE_tstring_full_format_spec: int = NT_OFFSET + 406,
     RULE_tstring_middle: int = NT_OFFSET + 407,
     RULE_tstring_replacement_field: int = NT_OFFSET + 408,
     RULE_tuple: int = NT_OFFSET + 409,
     RULE_type_alias: int = NT_OFFSET + 410,
     RULE_type_expressions: int = NT_OFFSET + 411,
     RULE_type_param: int = NT_OFFSET + 412,
     RULE_type_param_bound: int = NT_OFFSET + 413,
     RULE_type_param_default: int = NT_OFFSET + 414,
     RULE_type_param_seq: int = NT_OFFSET + 415,
     RULE_type_param_starred_default: int = NT_OFFSET + 416,
     RULE_type_params: int = NT_OFFSET + 417,
     RULE_value_pattern: int = NT_OFFSET + 418,
     RULE_while_stmt: int = NT_OFFSET + 419,
     RULE_wildcard_pattern: int = NT_OFFSET + 420,
     RULE_with_item: int = NT_OFFSET + 421,
     RULE_with_stmt: int = NT_OFFSET + 422,
     RULE_yield_expr: int = NT_OFFSET + 423,
     RULE_yield_stmt: int = NT_OFFSET + 424;

def rule__gather_101(p: peg_parser) -> list[kp_pair] | None {
    mark = peg_mark(p);
    res: list[kp_pair] | None = None;
    elem = rule_key_value_pattern(p);
    if elem is not None {
        seq = rule__loop0_100(p);
        if seq is not None {
            res = pa_seq_insert_front_kvpattern(elem as kp_pair, seq) as list[kp_pair];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_104(p: peg_parser) -> list[kp_pair] | None {
    mark = peg_mark(p);
    res: list[kp_pair] | None = None;
    elem = rule_keyword_pattern(p);
    if elem is not None {
        seq = rule__loop0_103(p);
        if seq is not None {
            res = pa_seq_insert_front_kvpattern(elem as kp_pair, seq) as list[kp_pair];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_106(p: peg_parser) -> list[keyword_or_starred_node] | None {
    mark = peg_mark(p);
    res: list[keyword_or_starred_node] | None = None;
    elem = rule_kwarg_or_starred(p);
    if elem is not None {
        seq = rule__loop0_105(p);
        if seq is not None {
            res = pa_seq_insert_front_kw_or_starred(
                elem as keyword_or_starred_node, seq
            ) as list[keyword_or_starred_node];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_108(p: peg_parser) -> list[keyword_or_starred_node] | None {
    mark = peg_mark(p);
    res: list[keyword_or_starred_node] | None = None;
    elem = rule_kwarg_or_double_starred(p);
    if elem is not None {
        seq = rule__loop0_107(p);
        if seq is not None {
            res = pa_seq_insert_front_kw_or_starred(
                elem as keyword_or_starred_node, seq
            ) as list[keyword_or_starred_node];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_113(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    elem = rule_maybe_star_pattern(p);
    if elem is not None {
        seq = rule__loop0_112(p);
        if seq is not None {
            res = pa_seq_insert_front_pattern(elem as pattern, seq) as list[pattern];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_115(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    elem = rule_closed_pattern(p);
    if elem is not None {
        seq = rule__loop0_114(p);
        if seq is not None {
            res = pa_seq_insert_front_pattern(elem as pattern, seq) as list[pattern];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_118(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    elem = rule_pattern(p);
    if elem is not None {
        seq = rule__loop0_117(p);
        if seq is not None {
            res = pa_seq_insert_front_pattern(elem as pattern, seq) as list[pattern];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_121(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    elem = rule_simple_stmt(p);
    if elem is not None {
        seq = rule__loop0_120(p);
        if seq is not None {
            res = pa_seq_insert_front_stmt(elem as stmt, seq) as list[stmt];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_124(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule__tmp_143(p);
    if elem is not None {
        seq = rule__loop0_123(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_127(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule_star_named_expression(p);
    if elem is not None {
        seq = rule__loop0_126(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_131(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule_star_target(p);
    if elem is not None {
        seq = rule__loop0_130(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_138(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule_expression(p);
    if elem is not None {
        seq = rule__loop0_137(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_140(p: peg_parser) -> list[type_param] | None {
    mark = peg_mark(p);
    res: list[type_param] | None = None;
    elem = rule_type_param(p);
    if elem is not None {
        seq = rule__loop0_139(p);
        if seq is not None {
            res = pa_seq_insert_front_type_param(elem as type_param, seq) as list[
                type_param
            ];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_142(p: peg_parser) -> list[withitem] | None {
    mark = peg_mark(p);
    res: list[withitem] | None = None;
    elem = rule_with_item(p);
    if elem is not None {
        seq = rule__loop0_141(p);
        if seq is not None {
            res = pa_seq_insert_front_withitem(elem as withitem, seq) as list[withitem];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_17(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule_del_target(p);
    if elem is not None {
        seq = rule__loop0_16(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_21(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    res: list[alias] | None = None;
    elem = rule_dotted_as_name(p);
    if elem is not None {
        seq = rule__loop0_20(p);
        if seq is not None {
            res = pa_seq_insert_front_alias(elem as alias, seq) as list[alias];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_23(p: peg_parser) -> list[kv_pair] | None {
    mark = peg_mark(p);
    res: list[kv_pair] | None = None;
    elem = rule_double_starred_kvpair(p);
    if elem is not None {
        seq = rule__loop0_22(p);
        if seq is not None {
            res = pa_seq_insert_front_kvpair(elem as kv_pair, seq) as list[kv_pair];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_3(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule__tmp_144(p);
    if elem is not None {
        seq = rule__loop0_1(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_33(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = pa_name_from_token(peg_expect_token(p, NAME));
    if elem is not None {
        seq = rule__loop0_32(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_38(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    res: list[alias] | None = None;
    elem = rule_import_from_as_name(p);
    if elem is not None {
        seq = rule__loop0_37(p);
        if seq is not None {
            res = pa_seq_insert_front_alias(elem as alias, seq) as list[alias];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_42(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule__tmp_145(p);
    if elem is not None {
        seq = rule__loop0_41(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_62(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule_dotted_name(p);
    if elem is not None {
        seq = rule__loop0_61(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_70(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    res: list[arg] | None = None;
    elem = rule_lambda_param(p);
    if elem is not None {
        seq = rule__loop0_69(p);
        if seq is not None {
            res = pa_seq_insert_front_arg(elem as arg, seq) as list[arg];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_97(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule__tmp_146(p);
    if elem is not None {
        seq = rule__loop0_95(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__gather_99(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    elem = rule__tmp_147(p);
    if elem is not None {
        seq = rule__loop0_98(p);
        if seq is not None {
            res = pa_seq_insert_front_expr(elem as expr, seq) as list[expr];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__loop0_1(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule__tmp_144(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_100(p: peg_parser) -> list[kp_pair] | None {
    mark = peg_mark(p);
    children: list[kp_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_key_value_pattern(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as kp_pair);
        mark = peg_mark(p);
    }
    return children as list[kp_pair];
}

def rule__loop0_103(p: peg_parser) -> list[kp_pair] | None {
    mark = peg_mark(p);
    children: list[kp_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_keyword_pattern(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as kp_pair);
        mark = peg_mark(p);
    }
    return children as list[kp_pair];
}

def rule__loop0_105(p: peg_parser) -> list[keyword_or_starred_node] | None {
    mark = peg_mark(p);
    children: list[keyword_or_starred_node] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_kwarg_or_starred(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as keyword_or_starred_node);
        mark = peg_mark(p);
    }
    return children as list[keyword_or_starred_node];
}

def rule__loop0_107(p: peg_parser) -> list[keyword_or_starred_node] | None {
    mark = peg_mark(p);
    children: list[keyword_or_starred_node] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_kwarg_or_double_starred(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as keyword_or_starred_node);
        mark = peg_mark(p);
    }
    return children as list[keyword_or_starred_node];
}

def rule__loop0_109(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lambda_param_with_default = rule_lambda_param_with_default(p);
        if lambda_param_with_default is None {
            break;
        }
        children.append(lambda_param_with_default as name_default_pair);
        mark = peg_mark(p);
    }
    return children as list[name_default_pair];
}

def rule__loop0_112(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    children: list[pattern] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_maybe_star_pattern(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as pattern);
        mark = peg_mark(p);
    }
    return children as list[pattern];
}

def rule__loop0_114(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    children: list[pattern] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 18);
        if lit is None {
            break;
        }
        elem = rule_closed_pattern(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as pattern);
        mark = peg_mark(p);
    }
    return children as list[pattern];
}

def rule__loop0_116(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        param_with_default = rule_param_with_default(p);
        if param_with_default is None {
            break;
        }
        children.append(param_with_default as name_default_pair);
        mark = peg_mark(p);
    }
    return children as list[name_default_pair];
}

def rule__loop0_117(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    children: list[pattern] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_pattern(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as pattern);
        mark = peg_mark(p);
    }
    return children as list[pattern];
}

def rule__loop0_120(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    children: list[stmt] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 13);
        if lit is None {
            break;
        }
        elem = rule_simple_stmt(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as stmt);
        mark = peg_mark(p);
    }
    return children as list[stmt];
}

def rule__loop0_123(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule__tmp_143(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_126(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_star_named_expression(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_129(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_148(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_130(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_star_target(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_134(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        tstring_middle = rule_tstring_middle(p);
        if tstring_middle is None {
            break;
        }
        children.append(tstring_middle as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_135(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        tstring_format_spec = rule_tstring_format_spec(p);
        if tstring_format_spec is None {
            break;
        }
        children.append(tstring_format_spec as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_137(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_expression(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_139(p: peg_parser) -> list[type_param] | None {
    mark = peg_mark(p);
    children: list[type_param] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_type_param(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as type_param);
        mark = peg_mark(p);
    }
    return children as list[type_param];
}

def rule__loop0_141(p: peg_parser) -> list[withitem] | None {
    mark = peg_mark(p);
    children: list[withitem] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_with_item(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as withitem);
        mark = peg_mark(p);
    }
    return children as list[withitem];
}

def rule__loop0_16(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_del_target(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_20(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    children: list[alias] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_dotted_as_name(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as alias);
        mark = peg_mark(p);
    }
    return children as list[alias];
}

def rule__loop0_22(p: peg_parser) -> list[kv_pair] | None {
    mark = peg_mark(p);
    children: list[kv_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_double_starred_kvpair(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as kv_pair);
        mark = peg_mark(p);
    }
    return children as list[kv_pair];
}

def rule__loop0_24(p: peg_parser) -> list[peg_token] | None {
    mark = peg_mark(p);
    children: list[peg_token] = [];
    while True {
        comma_mark = peg_mark(p);
        _newline = peg_expect_token(p, NEWLINE);
        if _newline is None {
            break;
        }
        children.append(_newline as peg_token);
        mark = peg_mark(p);
    }
    return children as list[peg_token];
}

def rule__loop0_26(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_149(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_28(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        fstring_middle = rule_fstring_middle(p);
        if fstring_middle is None {
            break;
        }
        children.append(fstring_middle as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_29(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        fstring_format_spec = rule_fstring_format_spec(p);
        if fstring_format_spec is None {
            break;
        }
        children.append(fstring_format_spec as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_32(p: peg_parser) -> list[Name] | None {
    mark = peg_mark(p);
    children: list[Name] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = pa_name_from_token(peg_expect_token(p, NAME));
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as Name);
        mark = peg_mark(p);
    }
    return children as list[Name];
}

def rule__loop0_35(p: peg_parser) -> list[peg_token] | None {
    mark = peg_mark(p);
    children: list[peg_token] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_150(p);
        if rhs is None {
            break;
        }
        children.append(rhs as peg_token);
        mark = peg_mark(p);
    }
    return children as list[peg_token];
}

def rule__loop0_37(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    children: list[alias] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_import_from_as_name(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as alias);
        mark = peg_mark(p);
    }
    return children as list[alias];
}

def rule__loop0_41(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule__tmp_145(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_46(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[object] = [];
    while True {
        comma_mark = peg_mark(p);
        star_named_expressions = rule_star_named_expressions(p);
        if star_named_expressions is None {
            break;
        }
        children.append(star_named_expressions as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_47(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_151(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_59(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_152(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_61(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_dotted_name(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_67(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lambda_param_maybe_default = rule_lambda_param_maybe_default(p);
        if lambda_param_maybe_default is None {
            break;
        }
        children.append(lambda_param_maybe_default as name_default_pair);
        mark = peg_mark(p);
    }
    return children as list[name_default_pair];
}

def rule__loop0_68(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    children: list[arg] = [];
    while True {
        comma_mark = peg_mark(p);
        lambda_param_no_default = rule_lambda_param_no_default(p);
        if lambda_param_no_default is None {
            break;
        }
        children.append(lambda_param_no_default as arg);
        mark = peg_mark(p);
    }
    return children as list[arg];
}

def rule__loop0_69(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    children: list[arg] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule_lambda_param(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as arg);
        mark = peg_mark(p);
    }
    return children as list[arg];
}

def rule__loop0_79(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        param_maybe_default = rule_param_maybe_default(p);
        if param_maybe_default is None {
            break;
        }
        children.append(param_maybe_default as name_default_pair);
        mark = peg_mark(p);
    }
    return children as list[name_default_pair];
}

def rule__loop0_80(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    children: list[arg] = [];
    while True {
        comma_mark = peg_mark(p);
        param_no_default = rule_param_no_default(p);
        if param_no_default is None {
            break;
        }
        children.append(param_no_default as arg);
        mark = peg_mark(p);
    }
    return children as list[arg];
}

def rule__loop0_91(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    children: list[object] = [];
    while True {
        comma_mark = peg_mark(p);
        block = rule_block(p);
        if block is None {
            break;
        }
        children.append(block as stmt);
        mark = peg_mark(p);
    }
    return children as list[stmt];
}

def rule__loop0_95(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule__tmp_146(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop0_98(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        lit = peg_expect_token(p, 12);
        if lit is None {
            break;
        }
        elem = rule__tmp_147(p);
        if elem is None {
            peg_reset(p, comma_mark);
            break;
        }
        children.append(elem as expr);
        mark = peg_mark(p);
    }
    return children as list[expr];
}

def rule__loop1_110(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    children: list[arg] = [];
    while True {
        comma_mark = peg_mark(p);
        lambda_param_no_default = rule_lambda_param_no_default(p);
        if lambda_param_no_default is None {
            break;
        }
        children.append(lambda_param_no_default as arg);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[arg];
}

def rule__loop1_111(p: peg_parser) -> list[match_case] | None {
    mark = peg_mark(p);
    children: list[match_case] = [];
    while True {
        comma_mark = peg_mark(p);
        case_block = rule_case_block(p);
        if case_block is None {
            break;
        }
        children.append(case_block as match_case);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[match_case];
}

def rule__loop1_125(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_153(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_13(p: peg_parser) -> list[pa_cmpop_expr_pair] | None {
    mark = peg_mark(p);
    children: list[pa_cmpop_expr_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        compare_op_bitwise_or_pair = rule_compare_op_bitwise_or_pair(p);
        if compare_op_bitwise_or_pair is None {
            break;
        }
        children.append(compare_op_bitwise_or_pair as pa_cmpop_expr_pair);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[pa_cmpop_expr_pair];
}

def rule__loop1_132(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_148(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_133(p: peg_parser) -> list[list[stmt]] | None {
    mark = peg_mark(p);
    children: list[list[stmt]] = [];
    while True {
        comma_mark = peg_mark(p);
        statement = rule_statement(p);
        if statement is None {
            break;
        }
        children.append(statement);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children;
}

def rule__loop1_14(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_154(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_15(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_155(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_18(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_156(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_25(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_5(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_27(p: peg_parser) -> list[comprehension] | None {
    mark = peg_mark(p);
    children: list[comprehension] = [];
    while True {
        comma_mark = peg_mark(p);
        for_if_clause = rule_for_if_clause(p);
        if for_if_clause is None {
            break;
        }
        children.append(for_if_clause as comprehension);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[comprehension];
}

def rule__loop1_36(p: peg_parser) -> list[peg_token] | None {
    mark = peg_mark(p);
    children: list[peg_token] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_150(p);
        if rhs is None {
            break;
        }
        children.append(rhs as peg_token);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[peg_token];
}

def rule__loop1_54(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_157(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_72(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lambda_param_maybe_default = rule_lambda_param_maybe_default(p);
        if lambda_param_maybe_default is None {
            break;
        }
        children.append(lambda_param_maybe_default as name_default_pair);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[name_default_pair];
}

def rule__loop1_73(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        lambda_param_with_default = rule_lambda_param_with_default(p);
        if lambda_param_with_default is None {
            break;
        }
        children.append(lambda_param_with_default as name_default_pair);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[name_default_pair];
}

def rule__loop1_8(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_151(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_81(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    children: list[arg] = [];
    while True {
        comma_mark = peg_mark(p);
        param_no_default = rule_param_no_default(p);
        if param_no_default is None {
            break;
        }
        children.append(param_no_default as arg);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[arg];
}

def rule__loop1_83(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        param_maybe_default = rule_param_maybe_default(p);
        if param_maybe_default is None {
            break;
        }
        children.append(param_maybe_default as name_default_pair);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[name_default_pair];
}

def rule__loop1_84(p: peg_parser) -> list[name_default_pair] | None {
    mark = peg_mark(p);
    children: list[name_default_pair] = [];
    while True {
        comma_mark = peg_mark(p);
        param_with_default = rule_param_with_default(p);
        if param_with_default is None {
            break;
        }
        children.append(param_with_default as name_default_pair);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[name_default_pair];
}

def rule__loop1_88(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        rhs = rule__tmp_90(p);
        if rhs is None {
            break;
        }
        children.append(rhs as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_89(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    children: list[expr] = [];
    while True {
        comma_mark = peg_mark(p);
        tstring = rule_tstring(p);
        if tstring is None {
            break;
        }
        children.append(tstring as expr);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[expr];
}

def rule__loop1_92(p: peg_parser) -> list[excepthandler] | None {
    mark = peg_mark(p);
    children: list[excepthandler] = [];
    while True {
        comma_mark = peg_mark(p);
        except_block = rule_except_block(p);
        if except_block is None {
            break;
        }
        children.append(except_block as excepthandler);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[excepthandler];
}

def rule__loop1_93(p: peg_parser) -> list[excepthandler] | None {
    mark = peg_mark(p);
    children: list[excepthandler] = [];
    while True {
        comma_mark = peg_mark(p);
        except_star_block = rule_except_star_block(p);
        if except_star_block is None {
            break;
        }
        children.append(except_star_block as excepthandler);
        mark = peg_mark(p);
    }
    if len(children) == 0 {
        return None;
    }
    return children as list[excepthandler];
}

def rule__tmp_10(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    list = rule_list(p);
    if list is not None {
        res = list;
        return res;
    }
    peg_reset(p, mark);
    listcomp = rule_listcomp(p);
    if listcomp is not None {
        res = listcomp;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_102(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    literal_expr = rule_literal_expr(p);
    if literal_expr is not None {
        res = literal_expr;
        return res;
    }
    peg_reset(p, mark);
    attr = rule_attr(p);
    if attr is not None {
        res = attr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_11(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    dict = rule_dict(p);
    if dict is not None {
        res = dict;
        return res;
    }
    peg_reset(p, mark);
    set = rule_set(p);
    if set is not None {
        res = set;
        return res;
    }
    peg_reset(p, mark);
    dictcomp = rule_dictcomp(p);
    if dictcomp is not None {
        res = dictcomp;
        return res;
    }
    peg_reset(p, mark);
    setcomp = rule_setcomp(p);
    if setcomp is not None {
        res = setcomp;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_119(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    kw = peg_expect_token(p, 702);
    if kw is not None {
        z = rule_expression(p);
        if z is not None {
            res = z;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_12(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        z = rule_arguments(p);
        lit_1 = peg_expect_token(p, 8);
        if lit_1 is not None {
            res = z;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_122(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 11);
    if lit is not None {
        d = rule_expression(p);
        res = d;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_128(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    if (peg_negative_lookahead_token(p, 16)) {
        star_target = rule_star_target(p);
        if star_target is not None {
            res = star_target;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_136(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    y = rule_star_named_expression(p);
    if y is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            z = rule_star_named_expressions(p);
            res = pa_seq_insert_front_expr(y, z);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_143(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    slice = rule_slice(p);
    if slice is not None {
        res = slice;
        return res;
    }
    peg_reset(p, mark);
    starred_expression = rule_starred_expression(p);
    if starred_expression is not None {
        res = starred_expression;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_144(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    starred_expression = rule_starred_expression(p);
    if starred_expression is not None {
        res = starred_expression;
        return res;
    }
    peg_reset(p, mark);
    rhs = rule__tmp_2(p);
    if rhs is not None {
        if (peg_negative_lookahead_token(p, 22)) {
            res = rhs;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_145(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    starred_expression = rule_starred_expression(p);
    if starred_expression is not None {
        if (peg_negative_lookahead_token(p, 22)) {
            res = starred_expression;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_146(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    expression = rule_expression(p);
    if expression is not None {
        rhs = rule__tmp_96(p);
        res = [expression, rhs];
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_147(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    expressions = rule_expressions(p);
    if expressions is not None {
        rhs = rule__tmp_96(p);
        res = [expressions, rhs];
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_148(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        c = rule_star_target(p);
        if c is not None {
            res = c;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_149(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    kw = peg_expect_token(p, 623);
    if kw is not None {
        z = rule_disjunction(p);
        if z is not None {
            res = z;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_150(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 23);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 52);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_151(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    star_targets = rule_star_targets(p);
    if star_targets is not None {
        lit = peg_expect_token(p, 22);
        if lit is not None {
            res = star_targets;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_152(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        bitwise_or = rule_bitwise_or(p);
        if bitwise_or is not None {
            res = bitwise_or;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_153(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        c = rule_star_expression(p);
        if c is not None {
            res = c;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_154(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    kw = peg_expect_token(p, 518);
    if kw is not None {
        c = rule_inversion(p);
        if c is not None {
            res = c;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_155(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 49);
    if lit is not None {
        f = rule_named_expression(p);
        if f is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = f;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_156(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    kw = peg_expect_token(p, 521);
    if kw is not None {
        c = rule_conjunction(p);
        if c is not None {
            res = c;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_157(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    if (peg_negative_lookahead_token(p, STRING)) {
        expression_without_invalid = rule_expression_without_invalid(p);
        if expression_without_invalid is not None {
            res = expression_without_invalid;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_19(p: peg_parser) -> Name | None {
    mark = peg_mark(p);
    res: Name | None = None;
    kw = peg_expect_token(p, 694);
    if kw is not None {
        z = pa_name_from_token(peg_expect_token(p, NAME));
        if z is not None {
            res = z;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_2(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    assignment_expression = rule_assignment_expression(p);
    if assignment_expression is not None {
        res = assignment_expression;
        return res;
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        if (peg_negative_lookahead_token(p, 53)) {
            res = expression;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_30(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        _indent = peg_expect_token(p, INDENT);
        if _indent is not None {
            res = [_newline, _indent];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_31(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 51);
    if lit is not None {
        z = rule_expression(p);
        if z is not None {
            res = z;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_34(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    yield_expr = rule_yield_expr(p);
    if yield_expr is not None {
        res = yield_expr;
        return res;
    }
    peg_reset(p, mark);
    named_expression = rule_named_expression(p);
    if named_expression is not None {
        res = named_expression;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_39(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    rhs = rule__tmp_40(p);
    if rhs is not None {
        res = rhs as object;
        return res;
    }
    peg_reset(p, mark);
    kwargs = rule_kwargs(p);
    if kwargs is not None {
        res = kwargs as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_4(p: peg_parser) -> list[keyword_or_starred_node] | None {
    mark = peg_mark(p);
    res: list[keyword_or_starred_node] | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        k = rule_kwargs(p);
        if k is not None {
            res = k;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_40(p: peg_parser) -> list[object] | None {
    mark = peg_mark(p);
    res: list[object] | None = None;
    gather = rule__gather_3(p);
    if gather is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            kwargs = rule_kwargs(p);
            if kwargs is not None {
                res = [gather, lit, kwargs];
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_43(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    args = rule_args(p);
    if args is not None {
        res = args as object;
        return res;
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        for_if_clauses = rule_for_if_clauses(p);
        if for_if_clauses is not None {
            res = [expression, for_if_clauses];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_44(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    args = rule_args(p);
    if args is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = args;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_45(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 14);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 15);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 17);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 24);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 47);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 49);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_48(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    positional_patterns = rule_positional_patterns(p);
    if positional_patterns is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = positional_patterns;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_49(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 9);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_5(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        z = rule_expression(p);
        if z is not None {
            res = z;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_50(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 9);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_51(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        rhs = rule__tmp_52(p);
        if rhs is not None {
            res = [name_var, rhs];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_52(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 8);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 13);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        res = _newline;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_53(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        res = _newline;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 11);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_55(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        string_var = peg_expect_token(p, STRING);
        if string_var is not None {
            res = [name_var, string_var];
            return res;
        }
    }
    peg_reset(p, mark);
    soft_kw = peg_expect_soft_keyword(p, "");
    if soft_kw is not None {
        res = soft_kw as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_56(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    res: stmt | None = None;
    pass_stmt = rule_pass_stmt(p);
    if pass_stmt is not None {
        res = pass_stmt;
        return res;
    }
    peg_reset(p, mark);
    break_stmt = rule_break_stmt(p);
    if break_stmt is not None {
        res = break_stmt;
        return res;
    }
    peg_reset(p, mark);
    continue_stmt = rule_continue_stmt(p);
    if continue_stmt is not None {
        res = continue_stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_57(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 14);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 15);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 31);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_58(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    bitwise_or = rule_bitwise_or(p);
    if bitwise_or is not None {
        seq = rule__loop0_59(p);
        if seq is not None {
            lit = peg_expect_token(p, 12);
            res = [bitwise_or, seq, lit];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_6(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 22);
    if lit is not None {
        d = rule_annotated_rhs(p);
        if d is not None {
            res = d;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_60(p: peg_parser) -> Name | None {
    mark = peg_mark(p);
    res: Name | None = None;
    lit = peg_expect_token(p, 54);
    if lit is not None {
        name_var = pa_name_from_token(peg_expect_token(p, NAME));
        if name_var is not None {
            res = name_var;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_63(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    kw = peg_expect_token(p, 668);
    if kw is not None {
        res = kw;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 669);
    if kw is not None {
        res = kw;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 667);
    if kw is not None {
        res = kw;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_64(p: peg_parser) -> Name | None {
    mark = peg_mark(p);
    res: Name | None = None;
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        lit = peg_expect_token(p, 22);
        if lit is not None {
            res = name_var;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_65(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 17);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_66(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    lambda_slash_no_default = rule_lambda_slash_no_default(p);
    if lambda_slash_no_default is not None {
        res = lambda_slash_no_default as object;
        return res;
    }
    peg_reset(p, mark);
    lambda_slash_with_default = rule_lambda_slash_with_default(p);
    if lambda_slash_with_default is not None {
        res = lambda_slash_with_default;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_7(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        b = rule_single_target(p);
        if b is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = b;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    single_subscript_attribute_target = rule_single_subscript_attribute_target(p);
    if single_subscript_attribute_target is not None {
        res = single_subscript_attribute_target;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_71(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        res = lit as object;
        return res;
    }
    peg_reset(p, mark);
    lambda_param_no_default = rule_lambda_param_no_default(p);
    if lambda_param_no_default is not None {
        res = lambda_param_no_default as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_74(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 11);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 12);
    if lit is not None {
        rhs = rule__tmp_75(p);
        if rhs is not None {
            res = rhs;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_75(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 11);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_76(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    lambda_param_no_default = rule_lambda_param_no_default(p);
    if lambda_param_no_default is not None {
        res = lambda_param_no_default as object;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 12);
    if lit is not None {
        res = lit as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_77(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    list = rule_list(p);
    if list is not None {
        res = list as object;
        return res;
    }
    peg_reset(p, mark);
    tuple = rule_tuple(p);
    if tuple is not None {
        res = tuple as object;
        return res;
    }
    peg_reset(p, mark);
    genexp = rule_genexp(p);
    if genexp is not None {
        res = genexp as object;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 668);
    if kw is not None {
        res = kw as object;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 667);
    if kw is not None {
        res = kw as object;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 669);
    if kw is not None {
        res = kw as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_78(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    slash_no_default = rule_slash_no_default(p);
    if slash_no_default is not None {
        res = slash_no_default as object;
        return res;
    }
    peg_reset(p, mark);
    slash_with_default = rule_slash_with_default(p);
    if slash_with_default is not None {
        res = slash_with_default;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_82(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 12);
    if lit is not None {
        res = lit as object;
        return res;
    }
    peg_reset(p, mark);
    param_no_default = rule_param_no_default(p);
    if param_no_default is not None {
        res = param_no_default as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_85(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 8);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 12);
    if lit is not None {
        rhs = rule__tmp_86(p);
        if rhs is not None {
            res = rhs;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_86(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    lit = peg_expect_token(p, 8);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        res = lit;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_87(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    param_no_default = rule_param_no_default(p);
    if param_no_default is not None {
        res = param_no_default as object;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 12);
    if lit is not None {
        res = lit as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_9(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    tuple = rule_tuple(p);
    if tuple is not None {
        res = tuple;
        return res;
    }
    peg_reset(p, mark);
    group = rule_group(p);
    if group is not None {
        res = group;
        return res;
    }
    peg_reset(p, mark);
    genexp = rule_genexp(p);
    if genexp is not None {
        res = genexp;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_90(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    fstring = rule_fstring(p);
    if fstring is not None {
        res = fstring;
        return res;
    }
    peg_reset(p, mark);
    string = rule_string(p);
    if string is not None {
        res = string;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_94(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    expression = rule_expression(p);
    if expression is not None {
        rhs = rule__tmp_19(p);
        items: list[object] = [expression, rhs];
        res = items;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule__tmp_96(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 694);
    if kw is not None {
        star_target = rule_star_target(p);
        if star_target is not None {
            res = [kw, star_target];
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_annotated_rhs(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    yield_expr = rule_yield_expr(p);
    if yield_expr is not None {
        res = yield_expr;
        return res;
    }
    peg_reset(p, mark);
    star_expressions = rule_star_expressions(p);
    if star_expressions is not None {
        res = star_expressions;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_annotation(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 11);
    if lit is not None {
        a = rule_expression(p);
        if a is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_args(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule__gather_3(p);
    if a is not None {
        b = rule__tmp_4(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_collect_call_seqs(
            a, b, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    a_2 = rule_kwargs(p);
    if a_2 is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_call(
            pa_dummy_name(start_lineno, start_col_offset, end_lineno, end_col_offset),
            pa_check(pa_seq_extract_starred_exprs(a_2)) as list[expr],
            pa_check(pa_seq_delete_starred_exprs(a_2)) as list[keyword],
            start_lineno,
            start_col_offset,
            end_lineno,
            end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_arguments(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_arguments);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    a = rule_args(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if (peg_positive_lookahead_token(p, 8)) {
            res = a;
            peg_insert_memo(p, mark, RULE_arguments, res);
            return res;
        }
    }
    peg_reset(p, mark);
    invalid_arguments = rule_invalid_arguments(p);
    if invalid_arguments is not None {
        res = invalid_arguments as expr;
        peg_insert_memo(p, mark, RULE_arguments, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_arguments, None);
    return None;
}

def rule_as_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    pattern = rule_or_pattern(p);
    if pattern is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            target = rule_pattern_capture_target(p);
            if target is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = MatchAs(
                    pattern=pattern,
                    name=pa_name_id(target),
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_as_pattern = rule_invalid_as_pattern(p);
    if invalid_as_pattern is not None {
        res = invalid_as_pattern as pattern;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_assert_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 685);
    if kw is not None {
        a = rule_expression(p);
        if a is not None {
            b = rule__tmp_5(p);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Assert(
                `test=a,
                msg=b,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_assignment(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            b = rule_expression(p);
            if b is not None {
                c = rule__tmp_6(p);
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_annassign(
                    pa_check(pa_set_context(a, Store())) as expr,
                    b,
                    c,
                    1,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = rule__tmp_7(p);
    if a_2 is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            b = rule_expression(p);
            if b is not None {
                c = rule__tmp_6(p);
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_annassign(
                    a_2,
                    b,
                    c,
                    0,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_3 = rule__loop1_8(p);
    if a_3 is not None {
        b = rule_annotated_rhs(p);
        if b is not None {
            if (peg_negative_lookahead_token(p, 22)) {
                tc = peg_expect_token(p, TYPE_COMMENT);
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_assign(
                    a_3, b, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                res.type_comment = tc.spelling if tc is not None else None;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_4 = rule_single_target(p);
    if a_4 is not None {
        b_4 = rule_augassign(p);
        if b_4 is not None {
            cut = True;
            c = rule_annotated_rhs(p);
            if c is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_augassign(
                    a_4,
                    pa_aug_op(b_4),
                    c,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_assignment = rule_invalid_assignment(p);
    if invalid_assignment is not None {
        res = invalid_assignment as stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_assignment_expression(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        lit = peg_expect_token(p, 53);
        if lit is not None {
            cut = True;
            b = rule_expression(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = NamedExpr(
                    target=pa_check(pa_set_context(a, Store())) as expr,
                    value=b,
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_atom(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        res = name_var as expr;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 668);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_bool(
            True, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 669);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_bool(
            False, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 667);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_none(
            start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    if (
        (
            peg_positive_lookahead_token(p, STRING)
            or peg_positive_lookahead_token(p, FSTRING_START)
            or peg_positive_lookahead_token(p, TSTRING_START)
        )
    ) {
        strings = rule_strings(p);
        if strings is not None {
            res = strings;
            return res;
        }
    }
    peg_reset(p, mark);
    number_var = pa_number_from_token(peg_expect_token(p, NUMBER));
    if number_var is not None {
        res = number_var;
        return res;
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 7)) {
        rhs = rule__tmp_9(p);
        if rhs is not None {
            res = rhs;
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 9)) {
        rhs = rule__tmp_10(p);
        if rhs is not None {
            res = rhs;
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 25)) {
        rhs = rule__tmp_11(p);
        if rhs is not None {
            res = rhs;
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 52);
    if lit is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_ellipsis(
            start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_attr(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_attr);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_attr, res);
        peg_reset(p, mark);
        raw = rule_attr_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_attr_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    value = rule_name_or_attr(p);
    if value is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            attr = pa_name_from_token(peg_expect_token(p, NAME));
            if attr is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_attribute(
                    value,
                    pa_name_id(attr),
                    Load(),
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_augassign(p: peg_parser) -> operator | None {
    mark = peg_mark(p);
    res: operator | None = None;
    lit = peg_expect_token(p, 36);
    if lit is not None {
        res = Add();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 37);
    if lit is not None {
        res = Sub();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 38);
    if lit is not None {
        res = Mult();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 50);
    if lit is not None {
        res = MatMult();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 39);
    if lit is not None {
        res = Div();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 40);
    if lit is not None {
        res = Mod();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 41);
    if lit is not None {
        res = BitAnd();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 42);
    if lit is not None {
        res = BitOr();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 43);
    if lit is not None {
        res = BitXor();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 44);
    if lit is not None {
        res = LShift();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 45);
    if lit is not None {
        res = RShift();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 46);
    if lit is not None {
        res = Pow();
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 48);
    if lit is not None {
        res = FloorDiv();
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_await_primary(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_await_primary);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    kw = peg_expect_token(p, 505);
    if kw is not None {
        a = rule_primary(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Await(
                value=a,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            peg_insert_memo(p, mark, RULE_await_primary, res);
            return res;
        }
    }
    peg_reset(p, mark);
    primary = rule_primary(p);
    if primary is not None {
        res = primary;
        peg_insert_memo(p, mark, RULE_await_primary, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_await_primary, None);
    return None;
}

def rule_bitwise_and(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_bitwise_and);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_bitwise_and, res);
        peg_reset(p, mark);
        raw = rule_bitwise_and_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_bitwise_and_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_bitwise_and(p);
    if a is not None {
        lit = peg_expect_token(p, 19);
        if lit is not None {
            b = rule_shift_expr(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    BitAnd(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    shift_expr = rule_shift_expr(p);
    if shift_expr is not None {
        res = shift_expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_bitwise_or(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_bitwise_or);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_bitwise_or, res);
        peg_reset(p, mark);
        raw = rule_bitwise_or_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_bitwise_or_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_bitwise_or(p);
    if a is not None {
        lit = peg_expect_token(p, 18);
        if lit is not None {
            b = rule_bitwise_xor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    BitOr(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    bitwise_xor = rule_bitwise_xor(p);
    if bitwise_xor is not None {
        res = bitwise_xor;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_bitwise_xor(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_bitwise_xor);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_bitwise_xor, res);
        peg_reset(p, mark);
        raw = rule_bitwise_xor_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_bitwise_xor_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_bitwise_xor(p);
    if a is not None {
        lit = peg_expect_token(p, 32);
        if lit is not None {
            b = rule_bitwise_and(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    BitXor(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    bitwise_and = rule_bitwise_and(p);
    if bitwise_and is not None {
        res = bitwise_and;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_block(p: peg_parser) -> list[stmt] | None {
    hit = peg_check_memo(p, RULE_block);
    if hit.hit {
        return hit.result as list[stmt] | None;
    }
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        _indent = peg_expect_token(p, INDENT);
        if _indent is not None {
            a = rule_statements(p);
            if a is not None {
                _dedent = peg_expect_token(p, DEDENT);
                if _dedent is not None {
                    res = a;
                    peg_insert_memo(p, mark, RULE_block, res);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    simple_stmts = rule_simple_stmts(p);
    if simple_stmts is not None {
        res = simple_stmts;
        peg_insert_memo(p, mark, RULE_block, res);
        return res;
    }
    peg_reset(p, mark);
    invalid_block = rule_invalid_block(p);
    if invalid_block is not None {
        res = invalid_block as list[stmt];
        peg_insert_memo(p, mark, RULE_block, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_block, None);
    return None;
}

def rule_break_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 686);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_break(start_lineno, start_col_offset, end_lineno, end_col_offset);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_capture_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    target = rule_pattern_capture_target(p);
    if target is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = MatchAs(
            pattern=None,
            name=pa_name_id(target),
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_case_block(p: peg_parser) -> match_case | None {
    mark = peg_mark(p);
    res: match_case | None = None;
    invalid_case_block = rule_invalid_case_block(p);
    if invalid_case_block is not None {
        res = invalid_case_block as match_case;
        return res;
    }
    peg_reset(p, mark);
    soft = peg_expect_soft_keyword(p, "case");
    if soft is not None {
        pattern = rule_patterns(p);
        if pattern is not None {
            guard = rule_guard(p);
            lit = peg_expect_token(p, 11);
            if lit is not None {
                body = rule_block(p);
                if body is not None {
                    res = match_case(
                        pattern=pattern,
                        guard=guard,
                        body=(body if body is not None else [])
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_class_def(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    res: stmt | None = None;
    a = rule_decorators(p);
    if a is not None {
        b = rule_class_def_raw(p);
        if b is not None {
            res = pa_class_def_decorators(a, b);
            return res;
        }
    }
    peg_reset(p, mark);
    class_def_raw = rule_class_def_raw(p);
    if class_def_raw is not None {
        res = class_def_raw;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_class_def_raw(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_class_def_raw = rule_invalid_class_def_raw(p);
    if invalid_class_def_raw is not None {
        res = invalid_class_def_raw as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 570);
    if kw is not None {
        a = pa_name_from_token(peg_expect_token(p, NAME));
        if a is not None {
            t = rule_type_params(p);
            b = rule__tmp_12(p);
            lit = peg_expect_token(p, 11);
            if lit is not None {
                c = rule_block(p);
                if c is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = ClassDef(
                        name=pa_name_id(a),
                        bases=(
                            (pa_call_args(b) if b is not None else [])
                                if (pa_call_args(b) if b is not None else [])
                                    is not None
                                else []
                        ),
                        keywords=(
                            (pa_call_keywords(b) if b is not None else [])
                                if (pa_call_keywords(b) if b is not None else [])
                                    is not None
                                else []
                        ),
                        body=(c if c is not None else []),
                        decorator_list=[],
                        type_params=(t if t is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_class_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    cls = rule_name_or_attr(p);
    if cls is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = MatchClass(
                    cls=cls,
                    patterns=[],
                    kwd_attrs=[],
                    kwd_patterns=[],
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    cls = rule_name_or_attr(p);
    if cls is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            patterns = rule_positional_patterns(p);
            if patterns is not None {
                lit_1 = peg_expect_token(p, 12);
                lit_2 = peg_expect_token(p, 8);
                if lit_2 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = MatchClass(
                        cls=cls,
                        patterns=(patterns if patterns is not None else []),
                        kwd_attrs=[],
                        kwd_patterns=[],
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    cls = rule_name_or_attr(p);
    if cls is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            keywords = pa_pattern_list(rule_keyword_patterns(p));
            if keywords is not None {
                lit_1 = peg_expect_token(p, 12);
                lit_2 = peg_expect_token(p, 8);
                if lit_2 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = MatchClass(
                        cls=cls,
                        patterns=[],
                        kwd_attrs=(
                            pa_check(
                                pa_map_names_to_ids(
                                    pa_check(pa_get_pattern_keys(keywords)) as list[
                                        expr
                                    ]
                                )
                            ) as list[str]
                                if pa_check(
                                    pa_map_names_to_ids(
                                        pa_check(pa_get_pattern_keys(keywords)) as list[
                                            expr
                                        ]
                                    )
                                ) as list[str]
                                    is not None
                                else []
                        ),
                        kwd_patterns=(
                            pa_check(pa_get_patterns(keywords)) as list[pattern]
                                if pa_check(pa_get_patterns(keywords)) as list[pattern]
                                    is not None
                                else []
                        ),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    cls = rule_name_or_attr(p);
    if cls is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            patterns = rule_positional_patterns(p);
            if patterns is not None {
                lit_1 = peg_expect_token(p, 12);
                if lit_1 is not None {
                    keywords = pa_pattern_list(rule_keyword_patterns(p));
                    if keywords is not None {
                        lit_2 = peg_expect_token(p, 12);
                        lit_3 = peg_expect_token(p, 8);
                        if lit_3 is not None {
                            end_tok = peg_get_last_nonnwhitespace_token(p);
                            if end_tok is None {
                                p.error_indicator = True;
                                peg_reset(p, mark);
                                return None;
                            }
                            end_lineno = end_tok.end_lineno;
                            end_col_offset = end_tok.end_col_offset;
                            res = MatchClass(
                                cls=cls,
                                patterns=(patterns if patterns is not None else []),
                                kwd_attrs=(
                                    pa_check(
                                        pa_map_names_to_ids(
                                            pa_check(pa_get_pattern_keys(keywords)) as list[
                                                expr
                                            ]
                                        )
                                    ) as list[str]
                                        if pa_check(
                                            pa_map_names_to_ids(
                                                pa_check(
                                                    pa_get_pattern_keys(keywords)
                                                ) as list[expr]
                                            )
                                        ) as list[str]
                                            is not None
                                        else []
                                ),
                                kwd_patterns=(
                                    pa_check(pa_get_patterns(keywords)) as list[pattern]
                                        if pa_check(pa_get_patterns(keywords)) as list[
                                            pattern
                                        ]
                                            is not None
                                        else []
                                ),
                                lineno=start_lineno,
                                col_offset=start_col_offset,
                                end_lineno=end_lineno,
                                end_col_offset=end_col_offset
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_class_pattern = rule_invalid_class_pattern(p);
    if invalid_class_pattern is not None {
        res = invalid_class_pattern as pattern;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_closed_pattern(p: peg_parser) -> pattern | None {
    hit = peg_check_memo(p, RULE_closed_pattern);
    if hit.hit {
        return hit.result as pattern | None;
    }
    mark = peg_mark(p);
    res: pattern | None = None;
    literal_pattern = rule_literal_pattern(p);
    if literal_pattern is not None {
        res = literal_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    capture_pattern = rule_capture_pattern(p);
    if capture_pattern is not None {
        res = capture_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    wildcard_pattern = rule_wildcard_pattern(p);
    if wildcard_pattern is not None {
        res = wildcard_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    value_pattern = rule_value_pattern(p);
    if value_pattern is not None {
        res = value_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    group_pattern = rule_group_pattern(p);
    if group_pattern is not None {
        res = group_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    sequence_pattern = rule_sequence_pattern(p);
    if sequence_pattern is not None {
        res = sequence_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    mapping_pattern = rule_mapping_pattern(p);
    if mapping_pattern is not None {
        res = mapping_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    class_pattern = rule_class_pattern(p);
    if class_pattern is not None {
        res = class_pattern;
        peg_insert_memo(p, mark, RULE_closed_pattern, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_closed_pattern, None);
    return None;
}

def rule_compare_op_bitwise_or_pair(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    eq_bitwise_or = rule_eq_bitwise_or(p);
    if eq_bitwise_or is not None {
        res = eq_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    noteq_bitwise_or = rule_noteq_bitwise_or(p);
    if noteq_bitwise_or is not None {
        res = noteq_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    lte_bitwise_or = rule_lte_bitwise_or(p);
    if lte_bitwise_or is not None {
        res = lte_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    lt_bitwise_or = rule_lt_bitwise_or(p);
    if lt_bitwise_or is not None {
        res = lt_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    gte_bitwise_or = rule_gte_bitwise_or(p);
    if gte_bitwise_or is not None {
        res = gte_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    gt_bitwise_or = rule_gt_bitwise_or(p);
    if gt_bitwise_or is not None {
        res = gt_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    notin_bitwise_or = rule_notin_bitwise_or(p);
    if notin_bitwise_or is not None {
        res = notin_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    in_bitwise_or = rule_in_bitwise_or(p);
    if in_bitwise_or is not None {
        res = in_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    isnot_bitwise_or = rule_isnot_bitwise_or(p);
    if isnot_bitwise_or is not None {
        res = isnot_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    is_bitwise_or = rule_is_bitwise_or(p);
    if is_bitwise_or is not None {
        res = is_bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_comparison(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_bitwise_or(p);
    if a is not None {
        b = rule__loop1_13(p);
        if b is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_compare(
                a,
                pa_check(pa_get_cmpops(b)) as list[cmpop],
                pa_check(pa_get_exprs(b)) as list[expr],
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    bitwise_or = rule_bitwise_or(p);
    if bitwise_or is not None {
        res = bitwise_or;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_complex_number(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    real = rule_signed_real_number(p);
    if real is not None {
        lit = peg_expect_token(p, 14);
        if lit is not None {
            imag = rule_imaginary_number(p);
            if imag is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    real,
                    Add(),
                    imag,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    real = rule_signed_real_number(p);
    if real is not None {
        lit = peg_expect_token(p, 15);
        if lit is not None {
            imag = rule_imaginary_number(p);
            if imag is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    real,
                    Sub(),
                    imag,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_compound_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    res: stmt | None = None;
    if (
        (
            peg_positive_lookahead_token(p, 574)
            or peg_positive_lookahead_token(p, 49)
            or peg_positive_lookahead_token(p, 699)
        )
    ) {
        function_def = rule_function_def(p);
        if function_def is not None {
            res = function_def;
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 623)) {
        if_stmt = rule_if_stmt(p);
        if if_stmt is not None {
            res = if_stmt;
            return res;
        }
    }
    peg_reset(p, mark);
    if (
        (peg_positive_lookahead_token(p, 570) or peg_positive_lookahead_token(p, 49))
    ) {
        class_def = rule_class_def(p);
        if class_def is not None {
            res = class_def;
            return res;
        }
    }
    peg_reset(p, mark);
    if (
        (peg_positive_lookahead_token(p, 700) or peg_positive_lookahead_token(p, 699))
    ) {
        with_stmt = rule_with_stmt(p);
        if with_stmt is not None {
            res = with_stmt;
            return res;
        }
    }
    peg_reset(p, mark);
    if (
        (peg_positive_lookahead_token(p, 621) or peg_positive_lookahead_token(p, 699))
    ) {
        for_stmt = rule_for_stmt(p);
        if for_stmt is not None {
            res = for_stmt;
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 692)) {
        try_stmt = rule_try_stmt(p);
        if try_stmt is not None {
            res = try_stmt;
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 693)) {
        while_stmt = rule_while_stmt(p);
        if while_stmt is not None {
            res = while_stmt;
            return res;
        }
    }
    peg_reset(p, mark);
    match_stmt = rule_match_stmt(p);
    if match_stmt is not None {
        res = match_stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_conjunction(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_conjunction);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_inversion(p);
    if a is not None {
        b = rule__loop1_14(p);
        if b is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_boolop(
                And(),
                pa_check(pa_seq_insert_front_expr(a, b)) as list[expr],
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            peg_insert_memo(p, mark, RULE_conjunction, res);
            return res;
        }
    }
    peg_reset(p, mark);
    inversion = rule_inversion(p);
    if inversion is not None {
        res = inversion;
        peg_insert_memo(p, mark, RULE_conjunction, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_conjunction, None);
    return None;
}

def rule_continue_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 687);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_continue(
            start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_decorators(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    a = rule__loop1_15(p);
    if a is not None {
        res = a as list[expr];
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_default(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 22);
    if lit is not None {
        a = rule_expression(p);
        if a is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    invalid_default = rule_invalid_default(p);
    if invalid_default is not None {
        res = invalid_default as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_del_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 680);
    if kw is not None {
        a = rule_del_targets(p);
        if a is not None {
            if (
                (
                    peg_positive_lookahead_token(p, 13)
                    or peg_positive_lookahead_token(p, NEWLINE)
                )
            ) {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_delete(
                    a, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_del_stmt = rule_invalid_del_stmt(p);
    if invalid_del_stmt is not None {
        res = invalid_del_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_del_t_atom(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        res = pa_set_context(a, Del());
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a_2 = rule_del_target(p);
        if a_2 is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = pa_set_context(a_2, Del());
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a_3 = rule_del_targets(p);
        lit_1 = peg_expect_token(p, 8);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                a_3, Del(), start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 9);
    if lit is not None {
        a_4 = rule_del_targets(p);
        lit_1 = peg_expect_token(p, 10);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = List(
                elts=(a_4 if a_4 is not None else []),
                ctx=Del(),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_del_target(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_del_target);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            b = pa_name_from_token(peg_expect_token(p, NAME));
            if b is not None {
                if ((rule_t_lookahead(p) is None)) {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = pa_ast_attribute(
                        a,
                        pa_name_id(b),
                        Del(),
                        start_lineno,
                        start_col_offset,
                        end_lineno,
                        end_col_offset
                    );
                    peg_insert_memo(p, mark, RULE_del_target, res);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 9);
        if lit is not None {
            b_2 = rule_slices(p);
            if b_2 is not None {
                lit_1 = peg_expect_token(p, 10);
                if lit_1 is not None {
                    if ((rule_t_lookahead(p) is None)) {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_ast_subscript(
                            a,
                            b_2,
                            Del(),
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        peg_insert_memo(p, mark, RULE_del_target, res);
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    del_t_atom = rule_del_t_atom(p);
    if del_t_atom is not None {
        res = del_t_atom;
        peg_insert_memo(p, mark, RULE_del_target, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_del_target, None);
    return None;
}

def rule_del_targets(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    a = rule__gather_17(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_dict(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_double_starred_kvpairs(p);
        lit_1 = peg_expect_token(p, 26);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Dict(
                keys=(
                    pa_check(pa_get_keys(a)) as list[expr]
                        if pa_check(pa_get_keys(a)) as list[expr] is not None
                        else []
                ),
                values=(
                    pa_check(pa_get_values(a)) as list[expr]
                        if pa_check(pa_get_values(a)) as list[expr] is not None
                        else []
                ),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        invalid_double_starred_kvpairs = rule_invalid_double_starred_kvpairs(p);
        if invalid_double_starred_kvpairs is not None {
            lit_1 = peg_expect_token(p, 26);
            if lit_1 is not None {
                res = invalid_double_starred_kvpairs as expr;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_dictcomp(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_kvpair(p);
        if a is not None {
            b = rule_for_if_clauses(p);
            if b is not None {
                lit_1 = peg_expect_token(p, 26);
                if lit_1 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = DictComp(
                        key=pa_comp_field(a, "key"),
                        value=pa_comp_field(a, "value"),
                        generators=(b if b is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_dict_comprehension = rule_invalid_dict_comprehension(p);
    if invalid_dict_comprehension is not None {
        res = invalid_dict_comprehension as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_disjunction(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_disjunction);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_conjunction(p);
    if a is not None {
        b = rule__loop1_18(p);
        if b is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_boolop(
                Or(),
                pa_check(pa_seq_insert_front_expr(a, b)) as list[expr],
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            peg_insert_memo(p, mark, RULE_disjunction, res);
            return res;
        }
    }
    peg_reset(p, mark);
    conjunction = rule_conjunction(p);
    if conjunction is not None {
        res = conjunction;
        peg_insert_memo(p, mark, RULE_disjunction, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_disjunction, None);
    return None;
}

def rule_dotted_as_name(p: peg_parser) -> alias | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: alias | None = None;
    invalid_dotted_as_name = rule_invalid_dotted_as_name(p);
    if invalid_dotted_as_name is not None {
        res = invalid_dotted_as_name as alias;
        return res;
    }
    peg_reset(p, mark);
    a = rule_dotted_name(p);
    if a is not None {
        b = rule__tmp_19(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = alias(
            name=pa_name_id(a),
            asname=(pa_name_id(b) if b is not None else None),
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_dotted_as_names(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    res: list[alias] | None = None;
    a = rule__gather_21(p);
    if a is not None {
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_dotted_name(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_dotted_name);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_dotted_name, res);
        peg_reset(p, mark);
        raw = rule_dotted_name_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_dotted_name_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    a = rule_dotted_name(p);
    if a is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            b = pa_name_from_token(peg_expect_token(p, NAME));
            if b is not None {
                res = pa_join_names_with_dot(a, b);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        res = name_var as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_double_star_pattern(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 35);
    if lit is not None {
        target = rule_pattern_capture_target(p);
        if target is not None {
            res = target;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_double_starred_kvpair(p: peg_parser) -> kv_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: kv_pair | None = None;
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_key_value_pair(
                None, a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    kvpair = rule_kvpair(p);
    if kvpair is not None {
        res = kvpair;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_double_starred_kvpairs(p: peg_parser) -> list[kv_pair] | None {
    mark = peg_mark(p);
    res: list[kv_pair] | None = None;
    a = rule__gather_23(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        res = a;
        return res as list[kv_pair];
    }
    peg_reset(p, mark);
    return None;
}

def rule_elif_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_elif_stmt = rule_invalid_elif_stmt(p);
    if invalid_elif_stmt is not None {
        res = invalid_elif_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 581);
    if kw is not None {
        a = rule_named_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    c = rule_elif_stmt(p);
                    if c is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = If(
                            `test=a,
                            body=(b if b is not None else []),
                            orelse=pa_stmt_list_or_empty(
                                pa_check(pa_singleton_seq_stmt(c)) as list[stmt]
                            ),
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 581);
    if kw is not None {
        a = rule_named_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    c_3 = rule_else_block(p);
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = If(
                        `test=a,
                        body=(b if b is not None else []),
                        orelse=pa_stmt_list_or_empty(c_3),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_else_block(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    invalid_else_stmt = rule_invalid_else_stmt(p);
    if invalid_else_stmt is not None {
        res = invalid_else_stmt as list[stmt];
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 606);
    if kw is not None {
        forced = peg_expect_forced_token(p, 11, ':');
        if forced is not None {
            b = rule_block(p);
            if b is not None {
                res = b;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_eq_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    lit = peg_expect_token(p, 27);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(Eq(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_eval(p: peg_parser) -> mod | None {
    mark = peg_mark(p);
    res: mod | None = None;
    a = rule_expressions(p);
    if a is not None {
        seq = rule__loop0_24(p);
        if seq is not None {
            _endmarker = peg_expect_token(p, ENDMARKER);
            if _endmarker is not None {
                res = pa_ast_expression(a);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_except_block(p: peg_parser) -> excepthandler | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: excepthandler | None = None;
    invalid_except_stmt_indent = rule_invalid_except_stmt_indent(p);
    if invalid_except_stmt_indent is not None {
        res = invalid_except_stmt_indent as excepthandler;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        e = rule_expression(p);
        if e is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = ExceptHandler(
                        `type=e,
                        name=None,
                        body=(b if b is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        e = rule_expression(p);
        if e is not None {
            kw_1 = peg_expect_token(p, 694);
            if kw_1 is not None {
                t = pa_name_from_token(peg_expect_token(p, NAME));
                if t is not None {
                    lit = peg_expect_token(p, 11);
                    if lit is not None {
                        b = rule_block(p);
                        if b is not None {
                            end_tok = peg_get_last_nonnwhitespace_token(p);
                            if end_tok is None {
                                p.error_indicator = True;
                                peg_reset(p, mark);
                                return None;
                            }
                            end_lineno = end_tok.end_lineno;
                            end_col_offset = end_tok.end_col_offset;
                            res = ExceptHandler(
                                `type=e,
                                name=pa_name_id(t),
                                body=(b if b is not None else []),
                                lineno=start_lineno,
                                col_offset=start_col_offset,
                                end_lineno=end_lineno,
                                end_col_offset=end_col_offset
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        e = rule_expressions(p);
        if e is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = ExceptHandler(
                        `type=e,
                        name=None,
                        body=(b if b is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    if p.feature_version < 14 {
                        raise syntax_failure(
                            "except expressions without parentheses are only supported in Python 3.14 and greater",
                            filename=p.filename,
                            lineno=start_lineno,
                            offset=start_col_offset + 1
                        );
                    }
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            b = rule_block(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = ExceptHandler(
                    `type=None,
                    name=None,
                    body=(b if b is not None else []),
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_except_stmt = rule_invalid_except_stmt(p);
    if invalid_except_stmt is not None {
        res = invalid_except_stmt as excepthandler;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_except_star_block(p: peg_parser) -> excepthandler | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: excepthandler | None = None;
    invalid_except_star_stmt_indent = rule_invalid_except_star_stmt_indent(p);
    if invalid_except_star_stmt_indent is not None {
        res = invalid_except_star_stmt_indent as excepthandler;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            e = rule_expression(p);
            if e is not None {
                lit_1 = peg_expect_token(p, 11);
                if lit_1 is not None {
                    b = rule_block(p);
                    if b is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = ExceptHandler(
                            `type=e,
                            name=None,
                            body=(b if b is not None else []),
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            e = rule_expression(p);
            if e is not None {
                kw_1 = peg_expect_token(p, 694);
                if kw_1 is not None {
                    t = pa_name_from_token(peg_expect_token(p, NAME));
                    if t is not None {
                        lit_1 = peg_expect_token(p, 11);
                        if lit_1 is not None {
                            b = rule_block(p);
                            if b is not None {
                                end_tok = peg_get_last_nonnwhitespace_token(p);
                                if end_tok is None {
                                    p.error_indicator = True;
                                    peg_reset(p, mark);
                                    return None;
                                }
                                end_lineno = end_tok.end_lineno;
                                end_col_offset = end_tok.end_col_offset;
                                res = ExceptHandler(
                                    `type=e,
                                    name=pa_name_id(t),
                                    body=(b if b is not None else []),
                                    lineno=start_lineno,
                                    col_offset=start_col_offset,
                                    end_lineno=end_lineno,
                                    end_col_offset=end_col_offset
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            e = rule_expressions(p);
            if e is not None {
                lit_1 = peg_expect_token(p, 11);
                if lit_1 is not None {
                    b = rule_block(p);
                    if b is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = ExceptHandler(
                            `type=e,
                            name=None,
                            body=(b if b is not None else []),
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        );
                        if p.feature_version < 14 {
                            raise syntax_failure(
                                "except expressions without parentheses are only supported in Python 3.14 and greater",
                                filename=p.filename,
                                lineno=start_lineno,
                                offset=start_col_offset + 1
                            );
                        }
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_except_star_stmt = rule_invalid_except_star_stmt(p);
    if invalid_except_star_stmt is not None {
        res = invalid_except_star_stmt as excepthandler;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_expression(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_expression);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    invalid_expression = rule_invalid_expression(p);
    if invalid_expression is not None {
        res = invalid_expression as expr;
        peg_insert_memo(p, mark, RULE_expression, res);
        return res;
    }
    peg_reset(p, mark);
    invalid_legacy_expression = rule_invalid_legacy_expression(p);
    if invalid_legacy_expression is not None {
        res = invalid_legacy_expression as expr;
        peg_insert_memo(p, mark, RULE_expression, res);
        return res;
    }
    peg_reset(p, mark);
    a = rule_disjunction(p);
    if a is not None {
        kw = peg_expect_token(p, 623);
        if kw is not None {
            b = rule_disjunction(p);
            if b is not None {
                kw_1 = peg_expect_token(p, 606);
                if kw_1 is not None {
                    c = rule_expression(p);
                    if c is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_ast_ifexp(
                            b,
                            a,
                            c,
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        peg_insert_memo(p, mark, RULE_expression, res);
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    disjunction = rule_disjunction(p);
    if disjunction is not None {
        res = disjunction;
        peg_insert_memo(p, mark, RULE_expression, res);
        return res;
    }
    peg_reset(p, mark);
    lambdef = rule_lambdef(p);
    if lambdef is not None {
        res = lambdef;
        peg_insert_memo(p, mark, RULE_expression, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_expression, None);
    return None;
}

def rule_expression_without_invalid(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_disjunction(p);
    if a is not None {
        kw = peg_expect_token(p, 623);
        if kw is not None {
            b = rule_disjunction(p);
            if b is not None {
                kw_1 = peg_expect_token(p, 606);
                if kw_1 is not None {
                    c = rule_expression(p);
                    if c is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_ast_ifexp(
                            b,
                            a,
                            c,
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    disjunction = rule_disjunction(p);
    if disjunction is not None {
        res = disjunction;
        return res;
    }
    peg_reset(p, mark);
    lambdef = rule_lambdef(p);
    if lambdef is not None {
        res = lambdef;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_expressions(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_expression(p);
    if a is not None {
        b = rule__loop1_25(p);
        if b is not None {
            lit = peg_expect_token(p, 12);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                pa_check(pa_seq_insert_front_expr(a, b)) as list[expr],
                Load(),
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_expression(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                pa_check(pa_singleton_seq_expr(a)) as list[expr],
                Load(),
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        res = expression;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_factor(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_factor);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 14);
    if lit is not None {
        a = rule_factor(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_unaryop(
                UAdd(), a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            peg_insert_memo(p, mark, RULE_factor, res);
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 15);
    if lit is not None {
        a = rule_factor(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_unaryop(
                USub(), a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            peg_insert_memo(p, mark, RULE_factor, res);
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 31);
    if lit is not None {
        a = rule_factor(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_unaryop(
                Invert(), a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            peg_insert_memo(p, mark, RULE_factor, res);
            return res;
        }
    }
    peg_reset(p, mark);
    power = rule_power(p);
    if power is not None {
        res = power;
        peg_insert_memo(p, mark, RULE_factor, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_factor, None);
    return None;
}

def rule_file(p: peg_parser) -> mod | None {
    mark = peg_mark(p);
    res: mod | None = None;
    a = rule_statements(p);
    _endmarker = peg_expect_token(p, ENDMARKER);
    if _endmarker is not None {
        res = pa_make_module(a);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_finally_block(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    invalid_finally_stmt = rule_invalid_finally_stmt(p);
    if invalid_finally_stmt is not None {
        res = invalid_finally_stmt as list[stmt];
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 637);
    if kw is not None {
        forced = peg_expect_forced_token(p, 11, ':');
        if forced is not None {
            a = rule_block(p);
            if a is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_for_if_clause(p: peg_parser) -> comprehension | None {
    mark = peg_mark(p);
    res: comprehension | None = None;
    kw = peg_expect_token(p, 699);
    if kw is not None {
        kw_1 = peg_expect_token(p, 621);
        if kw_1 is not None {
            a = rule_star_targets(p);
            if a is not None {
                kw_2 = peg_expect_token(p, 672);
                if kw_2 is not None {
                    cut = True;
                    b = rule_disjunction(p);
                    if b is not None {
                        c = rule__loop0_26(p);
                        if c is not None {
                            res = comprehension(
                                target=a,
                                iter=b,
                                ifs=(c if c is not None else []),
                                is_async=1
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 621);
    if kw is not None {
        a = rule_star_targets(p);
        if a is not None {
            kw_1 = peg_expect_token(p, 672);
            if kw_1 is not None {
                cut = True;
                b = rule_disjunction(p);
                if b is not None {
                    c = rule__loop0_26(p);
                    if c is not None {
                        res = comprehension(
                            target=a,
                            iter=b,
                            ifs=(c if c is not None else []),
                            is_async=0
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_for_if_clause = rule_invalid_for_if_clause(p);
    if invalid_for_if_clause is not None {
        res = invalid_for_if_clause as comprehension;
        return res;
    }
    peg_reset(p, mark);
    invalid_for_target = rule_invalid_for_target(p);
    if invalid_for_target is not None {
        res = invalid_for_target as comprehension;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_for_if_clauses(p: peg_parser) -> list[comprehension] | None {
    mark = peg_mark(p);
    res: list[comprehension] | None = None;
    a = rule__loop1_27(p);
    if a is not None {
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_for_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_for_stmt = rule_invalid_for_stmt(p);
    if invalid_for_stmt is not None {
        res = invalid_for_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 621);
    if kw is not None {
        t = rule_star_targets(p);
        if t is not None {
            kw_1 = peg_expect_token(p, 672);
            if kw_1 is not None {
                cut = True;
                ex = rule_star_expressions(p);
                if ex is not None {
                    lit = peg_expect_token(p, 11);
                    if lit is not None {
                        tc = peg_expect_token(p, TYPE_COMMENT);
                        b = rule_block(p);
                        if b is not None {
                            el = rule_else_block(p);
                            end_tok = peg_get_last_nonnwhitespace_token(p);
                            if end_tok is None {
                                p.error_indicator = True;
                                peg_reset(p, mark);
                                return None;
                            }
                            end_lineno = end_tok.end_lineno;
                            end_col_offset = end_tok.end_col_offset;
                            res = For(
                                target=t,
                                iter=ex,
                                body=(b if b is not None else []),
                                orelse=pa_stmt_list_or_empty(el),
                                type_comment=tc.spelling if tc is not None else None,
                                lineno=start_lineno,
                                col_offset=start_col_offset,
                                end_lineno=end_lineno,
                                end_col_offset=end_col_offset
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    if kw is not None {
        kw_1 = peg_expect_token(p, 621);
        if kw_1 is not None {
            t = rule_star_targets(p);
            if t is not None {
                kw_2 = peg_expect_token(p, 672);
                if kw_2 is not None {
                    cut = True;
                    ex = rule_star_expressions(p);
                    if ex is not None {
                        lit = peg_expect_token(p, 11);
                        if lit is not None {
                            tc = peg_expect_token(p, TYPE_COMMENT);
                            b = rule_block(p);
                            if b is not None {
                                el = rule_else_block(p);
                                end_tok = peg_get_last_nonnwhitespace_token(p);
                                if end_tok is None {
                                    p.error_indicator = True;
                                    peg_reset(p, mark);
                                    return None;
                                }
                                end_lineno = end_tok.end_lineno;
                                end_col_offset = end_tok.end_col_offset;
                                res = AsyncFor(
                                    target=t,
                                    iter=ex,
                                    body=(b if b is not None else []),
                                    orelse=pa_stmt_list_or_empty(el),
                                    type_comment=tc.spelling
                                        if tc is not None
                                        else None,
                                    lineno=start_lineno,
                                    col_offset=start_col_offset,
                                    end_lineno=end_lineno,
                                    end_col_offset=end_col_offset
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_for_target = rule_invalid_for_target(p);
    if invalid_for_target is not None {
        res = invalid_for_target as stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_fstring(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = peg_expect_token(p, FSTRING_START);
    if a is not None {
        b = rule__loop0_28(p);
        if b is not None {
            c = peg_expect_token(p, FSTRING_END);
            if c is not None {
                end_tok = c as peg_token;
                res = pa_joined_str(
                    a,
                    b,
                    c,
                    start_lineno,
                    start_col_offset,
                    end_tok.end_lineno,
                    end_tok.end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_fstring_conversion(p: peg_parser) -> Name | None {
    mark = peg_mark(p);
    res: Name | None = None;
    conv_token = peg_expect_token(p, 54);
    if conv_token is not None {
        conv = pa_name_from_token(peg_expect_token(p, NAME));
        if conv is not None {
            res = pa_check_fstring_conversion(p, conv_token, conv);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_fstring_format_spec(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    t = peg_expect_token(p, FSTRING_MIDDLE);
    if t is not None {
        res = pa_decoded_constant_from_token(t);
        return res;
    }
    peg_reset(p, mark);
    fstring_replacement_field = rule_fstring_replacement_field(p);
    if fstring_replacement_field is not None {
        res = fstring_replacement_field;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_fstring_full_format_spec(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    colon = peg_expect_token(p, 11);
    if colon is not None {
        spec = rule__loop0_29(p);
        if spec is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_setup_full_format_spec(
                colon, spec, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_fstring_middle(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    fstring_replacement_field = rule_fstring_replacement_field(p);
    if fstring_replacement_field is not None {
        res = fstring_replacement_field;
        return res;
    }
    peg_reset(p, mark);
    t = peg_expect_token(p, FSTRING_MIDDLE);
    if t is not None {
        res = pa_constant_from_fstring_middle(t, False);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_fstring_replacement_field(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_annotated_rhs(p);
        if a is not None {
            debug_expr = peg_expect_token(p, 22);
            conversion = rule_fstring_conversion(p);
            format = rule_fstring_full_format_spec(p);
            rbrace = peg_expect_token(p, 26);
            if rbrace is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_formatted_value(
                    p,
                    a,
                    debug_expr,
                    conversion,
                    format,
                    rbrace,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_fstring_replacement_field = rule_invalid_fstring_replacement_field(p);
    if invalid_fstring_replacement_field is not None {
        res = invalid_fstring_replacement_field as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_func_type(p: peg_parser) -> mod | None {
    mark = peg_mark(p);
    res: mod | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule_type_expressions(p);
        lit_1 = peg_expect_token(p, 8);
        if lit_1 is not None {
            lit_2 = peg_expect_token(p, 51);
            if lit_2 is not None {
                b = rule_expression(p);
                if b is not None {
                    seq = rule__loop0_24(p);
                    if seq is not None {
                        _endmarker = peg_expect_token(p, ENDMARKER);
                        if _endmarker is not None {
                            res = FunctionType(
                                argtypes=(a if a is not None else []), returns=b
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_func_type_comment(p: peg_parser) -> peg_token | None {
    mark = peg_mark(p);
    res: peg_token | None = None;
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        t = peg_expect_token(p, TYPE_COMMENT);
        if t is not None {
            lookahead = peg_mark(p);
            matched = rule__tmp_30(p) is not None;
            peg_reset(p, lookahead);
            if matched {
                res = t;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_double_type_comments = rule_invalid_double_type_comments(p);
    if invalid_double_type_comments is not None {
        res = invalid_double_type_comments as peg_token;
        return res;
    }
    peg_reset(p, mark);
    type_comment_tok = peg_expect_token(p, TYPE_COMMENT);
    if type_comment_tok is not None {
        res = type_comment_tok;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_function_def(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    res: stmt | None = None;
    d = rule_decorators(p);
    if d is not None {
        f = rule_function_def_raw(p);
        if f is not None {
            res = pa_function_def_decorators(d, f);
            return res;
        }
    }
    peg_reset(p, mark);
    function_def_raw = rule_function_def_raw(p);
    if function_def_raw is not None {
        res = function_def_raw;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_function_def_raw(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 574);
    if kw is not None {
        n = pa_name_from_token(peg_expect_token(p, NAME));
        if n is not None {
            t = rule_type_params(p);
            lit = peg_expect_token(p, 7);
            if lit is not None {
                params = rule_params(p);
                lit_1 = peg_expect_token(p, 8);
                if lit_1 is not None {
                    a = rule__tmp_31(p);
                    lit_2 = peg_expect_token(p, 11);
                    if lit_2 is not None {
                        tc = rule_func_type_comment(p);
                        b = rule_block(p);
                        if b is not None {
                            end_tok = peg_get_last_nonnwhitespace_token(p);
                            if end_tok is None {
                                p.error_indicator = True;
                                peg_reset(p, mark);
                                return None;
                            }
                            end_lineno = end_tok.end_lineno;
                            end_col_offset = end_tok.end_col_offset;
                            res = FunctionDef(
                                name=pa_name_id(n),
                                args=(
                                    params
                                        if params is not None
                                        else pa_check(pa_empty_arguments()) as arguments
                                ),
                                body=(b if b is not None else []),
                                decorator_list=[],
                                returns=a,
                                type_comment=tc.spelling if tc is not None else None,
                                type_params=(t if t is not None else []),
                                lineno=start_lineno,
                                col_offset=start_col_offset,
                                end_lineno=end_lineno,
                                end_col_offset=end_col_offset
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    if kw is not None {
        kw_1 = peg_expect_token(p, 574);
        if kw_1 is not None {
            n = pa_name_from_token(peg_expect_token(p, NAME));
            if n is not None {
                t = rule_type_params(p);
                lit = peg_expect_token(p, 7);
                if lit is not None {
                    params = rule_params(p);
                    lit_1 = peg_expect_token(p, 8);
                    if lit_1 is not None {
                        a = rule__tmp_31(p);
                        lit_2 = peg_expect_token(p, 11);
                        if lit_2 is not None {
                            tc = rule_func_type_comment(p);
                            b = rule_block(p);
                            if b is not None {
                                end_tok = peg_get_last_nonnwhitespace_token(p);
                                if end_tok is None {
                                    p.error_indicator = True;
                                    peg_reset(p, mark);
                                    return None;
                                }
                                end_lineno = end_tok.end_lineno;
                                end_col_offset = end_tok.end_col_offset;
                                res = AsyncFunctionDef(
                                    name=pa_name_id(n),
                                    args=(
                                        params
                                            if params is not None
                                            else pa_check(
                                                pa_empty_arguments()
                                            ) as arguments
                                    ),
                                    body=(b if b is not None else []),
                                    decorator_list=[],
                                    returns=a,
                                    type_comment=tc.spelling
                                        if tc is not None
                                        else None,
                                    type_params=(t if t is not None else []),
                                    lineno=start_lineno,
                                    col_offset=start_col_offset,
                                    end_lineno=end_lineno,
                                    end_col_offset=end_col_offset
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_def_raw = rule_invalid_def_raw(p);
    if invalid_def_raw is not None {
        res = invalid_def_raw as stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_genexp(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule__tmp_2(p);
        if a is not None {
            b = rule_for_if_clauses(p);
            if b is not None {
                lit_1 = peg_expect_token(p, 8);
                if lit_1 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = GeneratorExp(
                        elt=a,
                        generators=(b if b is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_comprehension = rule_invalid_comprehension(p);
    if invalid_comprehension is not None {
        res = invalid_comprehension as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_global_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 688);
    if kw is not None {
        a = rule__gather_33(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Global(
                names=(
                    pa_check(pa_map_names_to_ids(a)) as list[str]
                        if pa_check(pa_map_names_to_ids(a)) as list[str] is not None
                        else []
                ),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_group(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule__tmp_34(p);
        if a is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_group = rule_invalid_group(p);
    if invalid_group is not None {
        res = invalid_group as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_group_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    res: pattern | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        pattern = rule_pattern(p);
        if pattern is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = pattern;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_gt_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    lit = peg_expect_token(p, 21);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(Gt(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_gte_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    lit = peg_expect_token(p, 30);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(GtE(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_guard(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    kw = peg_expect_token(p, 623);
    if kw is not None {
        guard = rule_named_expression(p);
        if guard is not None {
            res = guard;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_if_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_if_stmt = rule_invalid_if_stmt(p);
    if invalid_if_stmt is not None {
        res = invalid_if_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 623);
    if kw is not None {
        a = rule_named_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    c = rule_elif_stmt(p);
                    if c is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = If(
                            `test=a,
                            body=(b if b is not None else []),
                            orelse=pa_stmt_list_or_empty(
                                pa_check(pa_singleton_seq_stmt(c)) as list[stmt]
                            ),
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 623);
    if kw is not None {
        a = rule_named_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    c_3 = rule_else_block(p);
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = If(
                        `test=a,
                        body=(b if b is not None else []),
                        orelse=pa_stmt_list_or_empty(c_3),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_imaginary_number(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    imag = pa_number_from_token(peg_expect_token(p, NUMBER));
    if imag is not None {
        res = pa_ensure_imaginary(p, imag);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_import_from(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 702);
    if kw is not None {
        a = rule__loop0_35(p);
        if a is not None {
            b = rule_dotted_name(p);
            if b is not None {
                kw_1 = peg_expect_token(p, 681);
                if kw_1 is not None {
                    c = rule_import_from_targets(p);
                    if c is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_checked_future_import(
                            p,
                            pa_name_id(b),
                            c,
                            pa_seq_count_dots(a),
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 702);
    if kw is not None {
        a = rule__loop1_36(p);
        if a is not None {
            kw_1 = peg_expect_token(p, 681);
            if kw_1 is not None {
                b_2 = rule_import_from_targets(p);
                if b_2 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = ImportFrom(
                        module=None,
                        names=(b_2 if b_2 is not None else []),
                        level=pa_seq_count_dots(a),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_import_from_as_name(p: peg_parser) -> alias | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: alias | None = None;
    invalid_import_from_as_name = rule_invalid_import_from_as_name(p);
    if invalid_import_from_as_name is not None {
        res = invalid_import_from_as_name as alias;
        return res;
    }
    peg_reset(p, mark);
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        b = rule__tmp_19(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = alias(
            name=pa_name_id(a),
            asname=(pa_name_id(b) if b is not None else None),
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_import_from_as_names(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    res: list[alias] | None = None;
    a = rule__gather_38(p);
    if a is not None {
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_import_from_targets(p: peg_parser) -> list[alias] | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: list[alias] | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule_import_from_as_names(p);
        if a is not None {
            lit_1 = peg_expect_token(p, 12);
            lit_2 = peg_expect_token(p, 8);
            if lit_2 is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    import_from_as_names = rule_import_from_as_names(p);
    if import_from_as_names is not None {
        if (peg_negative_lookahead_token(p, 12)) {
            res = import_from_as_names;
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_singleton_seq_alias(
            pa_check(
                pa_alias_for_star(
                    start_lineno, start_col_offset, end_lineno, end_col_offset
                )
            ) as alias
        );
        return res;
    }
    peg_reset(p, mark);
    invalid_import_from_targets = rule_invalid_import_from_targets(p);
    if invalid_import_from_targets is not None {
        res = invalid_import_from_targets as list[alias];
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_import_name(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 681);
    if kw is not None {
        a = rule_dotted_as_names(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Import(
                names=(a if a is not None else []),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_import_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    res: stmt | None = None;
    invalid_import = rule_invalid_import(p);
    if invalid_import is not None {
        res = invalid_import as stmt;
        return res;
    }
    peg_reset(p, mark);
    import_name = rule_import_name(p);
    if import_name is not None {
        res = import_name;
        return res;
    }
    peg_reset(p, mark);
    import_from = rule_import_from(p);
    if import_from is not None {
        res = import_from;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_in_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    kw = peg_expect_token(p, 672);
    if kw is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(In(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_interactive(p: peg_parser) -> mod | None {
    mark = peg_mark(p);
    res: mod | None = None;
    a = rule_statement_newline(p);
    if a is not None {
        res = Interactive(body=a);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_ann_assign_target(p: peg_parser) -> expr | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    list = rule_list(p);
    if list is not None {
        res = list;
        return res;
    }
    peg_reset(p, mark);
    tuple = rule_tuple(p);
    if tuple is not None {
        res = tuple;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule_invalid_ann_assign_target(p);
        if a is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_arguments(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    rhs = rule__tmp_39(p);
    if rhs is not None {
        a = peg_expect_token(p, 12);
        if a is not None {
            gather = rule__gather_42(p);
            if gather is not None {
                res = pa_raise_syntax_known_expr(
                    p,
                    False,
                    a,
                    "iterable argument unpacking follows keyword argument unpacking"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = rule_expression(p);
    if a_2 is not None {
        b = rule_for_if_clauses(p);
        if b is not None {
            lit = peg_expect_token(p, 12);
            if lit is not None {
                rhs = rule__tmp_43(p);
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    a_2,
                    pa_get_last_comprehension_item(pa_seq_last(b)),
                    "Generator expression must be parenthesized"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_3 = pa_name_from_token(peg_expect_token(p, NAME));
    if a_3 is not None {
        b_3 = peg_expect_token(p, 22);
        if b_3 is not None {
            expression = rule_expression(p);
            if expression is not None {
                for_if_clauses = rule_for_if_clauses(p);
                if for_if_clauses is not None {
                    res = pa_raise_syntax_known_range(
                        p,
                        False,
                        a_3,
                        b_3,
                        "invalid syntax. Maybe you meant '==' or ':=' instead of '='?"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    rhs_4 = rule__tmp_44(p);
    a_4 = pa_name_from_token(peg_expect_token(p, NAME));
    if a_4 is not None {
        b_4 = peg_expect_token(p, 22);
        if b_4 is not None {
            if (
                (
                    peg_positive_lookahead_token(p, 12)
                    or peg_positive_lookahead_token(p, 8)
                )
            ) {
                res = pa_raise_syntax_known_range(
                    p, False, a_4, b_4, "expected argument value expression"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_5 = rule_args(p);
    if a_5 is not None {
        b = rule_for_if_clauses(p);
        if b is not None {
            res = pa_nonparen_genexp_in_call(a_5, b);
            return res;
        }
    }
    peg_reset(p, mark);
    args = rule_args(p);
    if args is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            a_6 = rule_expression(p);
            if a_6 is not None {
                b = rule_for_if_clauses(p);
                if b is not None {
                    res = pa_raise_syntax_known_range(
                        p,
                        False,
                        a_6,
                        pa_get_last_comprehension_item(pa_seq_last(b)),
                        "Generator expression must be parenthesized"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a_7 = rule_args(p);
    if a_7 is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            args = rule_args(p);
            if args is not None {
                res = pa_arguments_parsing_error(a_7);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_arithmetic(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    sum = rule_sum(p);
    if sum is not None {
        rhs = rule__tmp_45(p);
        if rhs is not None {
            a = peg_expect_token(p, 671);
            if a is not None {
                b = rule_inversion(p);
                if b is not None {
                    res = pa_raise_syntax_known_range(
                        p, False, a, b, "'not' after an operator must be parenthesized"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_as_pattern(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    or_pattern = rule_or_pattern(p);
    if or_pattern is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            a = peg_expect_soft_keyword(p, "_");
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "cannot use '_' as a target"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    or_pattern = rule_or_pattern(p);
    if or_pattern is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            a_2 = rule_expression(p);
            if a_2 is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a_2, "cannot use %s as pattern target"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_assignment(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule_invalid_ann_assign_target(p);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            expression = rule_expression(p);
            if expression is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "only single target (not %s) can be annotated"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_star_named_expression(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            seq = rule__loop0_46(p);
            if seq is not None {
                lit_1 = peg_expect_token(p, 11);
                if lit_1 is not None {
                    expression = rule_expression(p);
                    if expression is not None {
                        res = pa_raise_syntax_known_expr(
                            p,
                            False,
                            a,
                            "only single target (not tuple) can be annotated"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_expression(p);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            expression = rule_expression(p);
            if expression is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "illegal target for annotation"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    seq = rule__loop0_47(p);
    if seq is not None {
        a = rule_star_expressions(p);
        if a is not None {
            lit = peg_expect_token(p, 22);
            if lit is not None {
                res = pa_raise_invalid_target(p, STAR_TARGETS, a);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    seq = rule__loop0_47(p);
    if seq is not None {
        a = rule_yield_expr(p);
        if a is not None {
            lit = peg_expect_token(p, 22);
            if lit is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "assignment to yield expression not possible"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_star_expressions(p);
    if a is not None {
        augassign = rule_augassign(p);
        if augassign is not None {
            annotated_rhs = rule_annotated_rhs(p);
            if annotated_rhs is not None {
                res = pa_raise_syntax_known_expr(
                    p,
                    False,
                    a,
                    "'%s' is an illegal expression for augmented assignment"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_block(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        if (peg_negative_lookahead_token(p, INDENT)) {
            res = pa_raise_syntax(p, False, "expected an indented block");
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_case_block(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    soft = peg_expect_soft_keyword(p, "case");
    if soft is not None {
        patterns = rule_patterns(p);
        if patterns is not None {
            guard = rule_guard(p);
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_soft_keyword(p, "case");
    if a is not None {
        patterns = rule_patterns(p);
        if patterns is not None {
            guard = rule_guard(p);
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'case' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_class_argument_pattern(p: peg_parser) -> list[pattern] | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    rhs = rule__tmp_48(p);
    keyword_patterns = pa_pattern_list(rule_keyword_patterns(p));
    if keyword_patterns is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            a = rule_positional_patterns(p);
            if a is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_class_def_raw(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 570);
    if kw is not None {
        name_var = pa_name_from_token(peg_expect_token(p, NAME));
        if name_var is not None {
            type_params = rule_type_params(p);
            rhs = rule__tmp_12(p);
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_token(p, 570);
    if a is not None {
        name_var = pa_name_from_token(peg_expect_token(p, NAME));
        if name_var is not None {
            type_params = rule_type_params(p);
            rhs = rule__tmp_12(p);
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after class definition on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_class_pattern(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    name_or_attr = rule_name_or_attr(p);
    if name_or_attr is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            a = rule_invalid_class_argument_pattern(p);
            if a is not None {
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    pa_seq_first(a),
                    pa_seq_last(a),
                    "positional patterns follow keyword patterns"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_comprehension(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    rhs = rule__tmp_49(p);
    if rhs is not None {
        a = rule_starred_expression(p);
        if a is not None {
            for_if_clauses = rule_for_if_clauses(p);
            if for_if_clauses is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "iterable unpacking cannot be used in comprehension"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    rhs = rule__tmp_50(p);
    if rhs is not None {
        a = rule_star_named_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 12);
            if lit is not None {
                b = rule_star_named_expressions(p);
                if b is not None {
                    for_if_clauses = rule_for_if_clauses(p);
                    if for_if_clauses is not None {
                        res = pa_raise_syntax_known_range(
                            p,
                            False,
                            a,
                            pa_seq_last(b),
                            "did you forget parentheses around the comprehension target?"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    rhs = rule__tmp_50(p);
    if rhs is not None {
        a = rule_star_named_expression(p);
        if a is not None {
            b_3 = peg_expect_token(p, 12);
            if b_3 is not None {
                for_if_clauses = rule_for_if_clauses(p);
                if for_if_clauses is not None {
                    res = pa_raise_syntax_known_range(
                        p,
                        False,
                        a,
                        b_3,
                        "did you forget parentheses around the comprehension target?"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_def_raw(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 699);
    a = peg_expect_token(p, 574);
    if a is not None {
        name_var = pa_name_from_token(peg_expect_token(p, NAME));
        if name_var is not None {
            type_params = rule_type_params(p);
            lit = peg_expect_token(p, 7);
            if lit is not None {
                params = rule_params(p);
                lit_1 = peg_expect_token(p, 8);
                if lit_1 is not None {
                    rhs = rule__tmp_31(p);
                    lit_2 = peg_expect_token(p, 11);
                    if lit_2 is not None {
                        _newline = peg_expect_token(p, NEWLINE);
                        if _newline is not None {
                            if (peg_negative_lookahead_token(p, INDENT)) {
                                res = pa_raise_syntax(
                                    p,
                                    False,
                                    "expected an indented block after function definition on line %d"
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    kw_1 = peg_expect_token(p, 574);
    if kw_1 is not None {
        name_var = pa_name_from_token(peg_expect_token(p, NAME));
        if name_var is not None {
            type_params = rule_type_params(p);
            forced = peg_expect_forced_token(p, 7, '(');
            if forced is not None {
                params = rule_params(p);
                lit = peg_expect_token(p, 8);
                if lit is not None {
                    rhs = rule__tmp_31(p);
                    forced_1 = peg_expect_forced_token(p, 11, ':');
                    if forced_1 is not None {
                        func_type_comment = rule_func_type_comment(p);
                        block = rule_block(p);
                        if block is not None {
                            # This error-pass alternative only probes forced
                            # delimiters. finish_parse discards its result;
                            # use a typed statement sentinel instead of the
                            # generator's heterogeneous dummy-value list.
                            return Pass(
                                lineno=kw_1.lineno,
                                col_offset=kw_1.col_offset,
                                end_lineno=kw_1.end_lineno,
                                end_col_offset=kw_1.end_col_offset
                            );
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_default(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 22);
    if a is not None {
        if (
            (peg_positive_lookahead_token(p, 8) or peg_positive_lookahead_token(p, 12))
        ) {
            res = pa_raise_syntax_known_expr(
                p, False, a, "expected default value expression"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_del_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 680);
    if kw is not None {
        a = rule_star_expressions(p);
        if a is not None {
            res = pa_raise_invalid_target(p, DEL_TARGETS, a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_dict_comprehension(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 35);
        if a is not None {
            bitwise_or = rule_bitwise_or(p);
            if bitwise_or is not None {
                for_if_clauses = rule_for_if_clauses(p);
                if for_if_clauses is not None {
                    lit_1 = peg_expect_token(p, 26);
                    if lit_1 is not None {
                        res = pa_raise_syntax_known_expr(
                            p,
                            False,
                            a,
                            "dict unpacking cannot be used in dict comprehension"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_dotted_as_name(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    dotted_name = rule_dotted_name(p);
    if dotted_name is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            if ((rule__tmp_51(p) is None)) {
                a = rule_expression(p);
                if a is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a, "cannot use %s as import target"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_double_starred_kvpairs(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    gather = rule__gather_23(p);
    if gather is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            invalid_kvpair = rule_invalid_kvpair(p);
            if invalid_kvpair is not None {
                res = [gather, lit, invalid_kvpair];
                return res;
            }
        }
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            a = peg_expect_token(p, 16);
            if a is not None {
                bitwise_or = rule_bitwise_or(p);
                if bitwise_or is not None {
                    res = pa_raise_syntax_known_expr(
                        p,
                        False,
                        a,
                        "cannot use a starred expression in a dictionary value"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        a = peg_expect_token(p, 11);
        if a is not None {
            if (
                (
                    peg_positive_lookahead_token(p, 26)
                    or peg_positive_lookahead_token(p, 12)
                )
            ) {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "expression expected after dictionary key and ':'"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_double_type_comments(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    type_comment_tok = peg_expect_token(p, TYPE_COMMENT);
    if type_comment_tok is not None {
        _newline = peg_expect_token(p, NEWLINE);
        if _newline is not None {
            type_comment_tok_1 = peg_expect_token(p, TYPE_COMMENT);
            if type_comment_tok_1 is not None {
                _newline_1 = peg_expect_token(p, NEWLINE);
                if _newline_1 is not None {
                    _indent = peg_expect_token(p, INDENT);
                    if _indent is not None {
                        res = pa_raise_syntax(
                            p, False, "Cannot have two type comments on def"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_elif_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 581);
    if kw is not None {
        named_expression = rule_named_expression(p);
        if named_expression is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_token(p, 581);
    if a is not None {
        named_expression = rule_named_expression(p);
        if named_expression is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'elif' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_else_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 606);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                if (peg_negative_lookahead_token(p, INDENT)) {
                    res = pa_raise_syntax(
                        p,
                        False,
                        "expected an indented block after 'else' statement on line %d"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 606);
    if kw is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            block = rule_block(p);
            if block is not None {
                kw_1 = peg_expect_token(p, 581);
                if kw_1 is not None {
                    res = pa_raise_syntax(
                        p, False, "'elif' block follows an 'else' block"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_except_star_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 642);
    if kw is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            a = rule_expression(p);
            if a is not None {
                lit_1 = peg_expect_token(p, 12);
                if lit_1 is not None {
                    expressions = rule_expressions(p);
                    if expressions is not None {
                        kw_1 = peg_expect_token(p, 694);
                        if kw_1 is not None {
                            name_var = pa_name_from_token(peg_expect_token(p, NAME));
                            if name_var is not None {
                                lit_2 = peg_expect_token(p, 11);
                                if lit_2 is not None {
                                    res = pa_raise_syntax_known_expr(
                                        p,
                                        False,
                                        a,
                                        "multiple exception types must be parenthesized when using 'as'"
                                    );
                                    return res;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a_2 = peg_expect_token(p, 642);
    if a_2 is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            expression = rule_expression(p);
            if expression is not None {
                rhs = rule__tmp_19(p);
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    res = pa_raise_syntax(p, False, "expected ':'");
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a_3 = peg_expect_token(p, 642);
    if a_3 is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            rhs_3 = rule__tmp_53(p);
            if rhs_3 is not None {
                res = pa_raise_syntax(p, False, "expected one or more exception types");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            expression = rule_expression(p);
            if expression is not None {
                kw_1 = peg_expect_token(p, 694);
                if kw_1 is not None {
                    a = rule_expression(p);
                    if a is not None {
                        lit_1 = peg_expect_token(p, 11);
                        if lit_1 is not None {
                            block = rule_block(p);
                            if block is not None {
                                res = pa_raise_syntax_known_expr(
                                    p, False, a, "cannot use except* statement with %s"
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_except_star_stmt_indent(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 642);
    if a is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            expression = rule_expression(p);
            if expression is not None {
                rhs = rule__tmp_19(p);
                lit_1 = peg_expect_token(p, 11);
                if lit_1 is not None {
                    _newline = peg_expect_token(p, NEWLINE);
                    if _newline is not None {
                        if (peg_negative_lookahead_token(p, INDENT)) {
                            res = pa_raise_syntax(
                                p,
                                False,
                                "expected an indented block after 'except*' statement on line %d"
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_except_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 642);
    if kw is not None {
        a = rule_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 12);
            if lit is not None {
                expressions = rule_expressions(p);
                if expressions is not None {
                    kw_1 = peg_expect_token(p, 694);
                    if kw_1 is not None {
                        name_var = pa_name_from_token(peg_expect_token(p, NAME));
                        if name_var is not None {
                            lit_1 = peg_expect_token(p, 11);
                            if lit_1 is not None {
                                res = pa_raise_syntax_known_expr(
                                    p,
                                    False,
                                    a,
                                    "multiple exception types must be parenthesized when using 'as'"
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a_2 = peg_expect_token(p, 642);
    if a_2 is not None {
        expression = rule_expression(p);
        if expression is not None {
            rhs = rule__tmp_19(p);
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_3 = peg_expect_token(p, 642);
    if a_3 is not None {
        _newline = peg_expect_token(p, NEWLINE);
        if _newline is not None {
            res = pa_raise_syntax(p, False, "expected ':'");
            return res;
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 642);
    if kw is not None {
        expression = rule_expression(p);
        if expression is not None {
            kw_1 = peg_expect_token(p, 694);
            if kw_1 is not None {
                a = rule_expression(p);
                if a is not None {
                    lit = peg_expect_token(p, 11);
                    if lit is not None {
                        block = rule_block(p);
                        if block is not None {
                            res = pa_raise_syntax_known_expr(
                                p, False, a, "cannot use except statement with %s"
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_except_stmt_indent(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 642);
    if a is not None {
        expression = rule_expression(p);
        if expression is not None {
            rhs = rule__tmp_19(p);
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'except' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_token(p, 642);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                if (peg_negative_lookahead_token(p, INDENT)) {
                    res = pa_raise_syntax(
                        p,
                        False,
                        "expected an indented block after 'except' statement on line %d"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_expression(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    string_var = peg_expect_token(p, STRING);
    if string_var is not None {
        a = rule__loop1_54(p);
        if a is not None {
            string_var_1 = peg_expect_token(p, STRING);
            if string_var_1 is not None {
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    pa_seq_first(a),
                    pa_seq_last(a),
                    "invalid syntax. Is this intended to be part of the string?"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    if ((rule__tmp_55(p) is None)) {
        a_2 = rule_disjunction(p);
        if a_2 is not None {
            b = rule_expression_without_invalid(p);
            if b is not None {
                res = pa_check_legacy_stmt_or_raise(p, a_2, b);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_3 = rule_disjunction(p);
    if a_3 is not None {
        kw = peg_expect_token(p, 623);
        if kw is not None {
            b = rule_disjunction(p);
            if b is not None {
                if (
                    (
                        peg_negative_lookahead_token(p, 606)
                        and peg_negative_lookahead_token(p, 11)
                    )
                ) {
                    res = pa_raise_syntax_known_range(
                        p, False, a_3, b, "expected 'else' after 'if' expression"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a_4 = rule_disjunction(p);
    if a_4 is not None {
        kw = peg_expect_token(p, 623);
        if kw is not None {
            b = rule_disjunction(p);
            if b is not None {
                kw_1 = peg_expect_token(p, 606);
                if kw_1 is not None {
                    if ((rule_expression(p) is None)) {
                        res = pa_raise_syntax(
                            p,
                            True,
                            "expected expression after 'else', but statement is given"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a_5 = rule__tmp_56(p);
    if a_5 is not None {
        kw = peg_expect_token(p, 623);
        if kw is not None {
            b = rule_disjunction(p);
            if b is not None {
                kw_1 = peg_expect_token(p, 606);
                if kw_1 is not None {
                    c = rule_simple_stmt(p);
                    if c is not None {
                        res = pa_raise_syntax_known_expr(
                            p,
                            False,
                            a_5,
                            "expected expression before 'if', but statement is given"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a_6 = peg_expect_token(p, 663);
    if a_6 is not None {
        lambda_params = rule_lambda_params(p);
        b_6 = peg_expect_token(p, 11);
        if b_6 is not None {
            if (peg_positive_lookahead_token(p, FSTRING_MIDDLE)) {
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    a_6,
                    b_6,
                    "f-string: lambda expressions are not allowed without parentheses"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_7 = peg_expect_token(p, 663);
    if a_7 is not None {
        lambda_params = rule_lambda_params(p);
        b_7 = peg_expect_token(p, 11);
        if b_7 is not None {
            if (peg_positive_lookahead_token(p, TSTRING_MIDDLE)) {
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    a_7,
                    b_7,
                    "t-string: lambda expressions are not allowed without parentheses"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_factor(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    rhs = rule__tmp_57(p);
    if rhs is not None {
        a = peg_expect_token(p, 671);
        if a is not None {
            b = rule_factor(p);
            if b is not None {
                res = pa_raise_syntax_known_range(
                    p, False, a, b, "'not' after an operator must be parenthesized"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_finally_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 637);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                if (peg_negative_lookahead_token(p, INDENT)) {
                    res = pa_raise_syntax(
                        p,
                        False,
                        "expected an indented block after 'finally' statement on line %d"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_for_if_clause(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 699);
    kw_1 = peg_expect_token(p, 621);
    if kw_1 is not None {
        rhs = rule__tmp_58(p);
        if rhs is not None {
            if (peg_negative_lookahead_token(p, 672)) {
                res = pa_raise_syntax(
                    p, False, "'in' expected after for-loop variables"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_for_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 699);
    kw_1 = peg_expect_token(p, 621);
    if kw_1 is not None {
        star_targets = rule_star_targets(p);
        if star_targets is not None {
            kw_2 = peg_expect_token(p, 672);
            if kw_2 is not None {
                star_expressions = rule_star_expressions(p);
                if star_expressions is not None {
                    _newline = peg_expect_token(p, NEWLINE);
                    if _newline is not None {
                        res = pa_raise_syntax(p, False, "expected ':'");
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    a = peg_expect_token(p, 621);
    if a is not None {
        star_targets = rule_star_targets(p);
        if star_targets is not None {
            kw_1 = peg_expect_token(p, 672);
            if kw_1 is not None {
                star_expressions = rule_star_expressions(p);
                if star_expressions is not None {
                    lit = peg_expect_token(p, 11);
                    if lit is not None {
                        _newline = peg_expect_token(p, NEWLINE);
                        if _newline is not None {
                            if (peg_negative_lookahead_token(p, INDENT)) {
                                res = pa_raise_syntax(
                                    p,
                                    False,
                                    "expected an indented block after 'for' statement on line %d"
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_for_target(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 699);
    kw_1 = peg_expect_token(p, 621);
    if kw_1 is not None {
        a = rule_star_expressions(p);
        if a is not None {
            res = pa_raise_invalid_target(p, FOR_TARGETS, a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_fstring_conversion_character(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 54);
    if lit is not None {
        if (
            (peg_positive_lookahead_token(p, 11) or peg_positive_lookahead_token(p, 26))
        ) {
            res = pa_raise_syntax(p, True, "f-string: missing conversion character");
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 54);
    if lit is not None {
        if (peg_negative_lookahead_token(p, NAME)) {
            res = pa_raise_syntax(p, True, "f-string: invalid conversion character");
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_fstring_replacement_field(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 22);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "f-string: valid expression required before '='"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 54);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "f-string: valid expression required before '!'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 11);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "f-string: valid expression required before ':'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 26);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "f-string: valid expression required before '}'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        if ((rule_annotated_rhs(p) is None)) {
            res = pa_raise_syntax(
                p, True, "f-string: expecting a valid expression after '{'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            if (
                (
                    peg_negative_lookahead_token(p, 22)
                    and peg_negative_lookahead_token(p, 54)
                    and peg_negative_lookahead_token(p, 11)
                    and peg_negative_lookahead_token(p, 26)
                )
            ) {
                res = (
                    None
                        if pa_err_occurred(p)
                        else pa_raise_syntax(
                            p, True, "f-string: expecting '=', or '!', or ':', or '}'"
                        )
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            if lit_1 is not None {
                if (
                    (
                        peg_negative_lookahead_token(p, 54)
                        and peg_negative_lookahead_token(p, 11)
                        and peg_negative_lookahead_token(p, 26)
                    )
                ) {
                    res = (
                        None
                            if pa_err_occurred(p)
                            else pa_raise_syntax(
                                p, True, "f-string: expecting '!', or ':', or '}'"
                            )
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            invalid_fstring_conversion_character = rule_invalid_fstring_conversion_character(
                p
            );
            if invalid_fstring_conversion_character is not None {
                res = [lit, annotated_rhs, lit_1, invalid_fstring_conversion_character];
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            rhs = rule__tmp_60(p);
            if (
                (
                    peg_negative_lookahead_token(p, 11)
                    and peg_negative_lookahead_token(p, 26)
                )
            ) {
                res = (
                    None
                        if pa_err_occurred(p)
                        else pa_raise_syntax(p, True, "f-string: expecting ':' or '}'")
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            rhs = rule__tmp_60(p);
            lit_2 = peg_expect_token(p, 11);
            if lit_2 is not None {
                seq = rule__loop0_29(p);
                if seq is not None {
                    if (peg_negative_lookahead_token(p, 26)) {
                        res = (
                            None
                                if pa_err_occurred(p)
                                else pa_raise_syntax(
                                    p, True, "f-string: expecting '}', or format specs"
                                )
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            rhs = rule__tmp_60(p);
            if (peg_negative_lookahead_token(p, 26)) {
                res = (
                    None
                        if pa_err_occurred(p)
                        else pa_raise_syntax(p, True, "f-string: expecting '}'")
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_group(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule_starred_expression(p);
        if a is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "cannot use starred expression here"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a_2 = peg_expect_token(p, 35);
        if a_2 is not None {
            expression = rule_expression(p);
            if expression is not None {
                lit_1 = peg_expect_token(p, 8);
                if lit_1 is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a_2, "cannot use double starred expression here"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_if_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 623);
    if kw is not None {
        named_expression = rule_named_expression(p);
        if named_expression is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = peg_expect_token(p, 623);
    if a_2 is not None {
        a_2_1 = rule_named_expression(p);
        if a_2_1 is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'if' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_import(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 681);
    if a is not None {
        gather = rule__gather_62(p);
        if gather is not None {
            kw = peg_expect_token(p, 702);
            if kw is not None {
                dotted_name = rule_dotted_name(p);
                if dotted_name is not None {
                    res = pa_raise_syntax_known_expr(
                        p,
                        False,
                        a,
                        "Did you mean to use 'from ... import ...' instead?"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 681);
    if kw is not None {
        token = peg_expect_token(p, NEWLINE);
        if token is not None {
            res = pa_raise_syntax_known_expr(
                p, False, token, "Expected one or more names after 'import'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_import_from_as_name(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            if ((rule__tmp_51(p) is None)) {
                a = rule_expression(p);
                if a is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a, "cannot use %s as import target"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_import_from_targets(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    import_from_as_names = rule_import_from_as_names(p);
    if import_from_as_names is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(
                    p,
                    False,
                    "trailing comma not allowed without surrounding parentheses"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    token = peg_expect_token(p, NEWLINE);
    if token is not None {
        res = pa_raise_syntax_known_expr(
            p, False, token, "Expected one or more names after 'import'"
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_kvpair(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule_expression(p);
    if a is not None {
        if (peg_negative_lookahead_token(p, 11)) {
            res = pa_raise_kvpair_error(p, a);
            return res;
        }
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            a_2 = peg_expect_token(p, 16);
            if a_2 is not None {
                bitwise_or = rule_bitwise_or(p);
                if bitwise_or is not None {
                    res = pa_raise_syntax_known_expr(
                        p,
                        False,
                        a_2,
                        "cannot use a_2 starred expression in a_2 dictionary value"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        a_3 = peg_expect_token(p, 11);
        if a_3 is not None {
            if (
                (
                    peg_positive_lookahead_token(p, 26)
                    or peg_positive_lookahead_token(p, 12)
                )
            ) {
                res = pa_raise_syntax_known_expr(
                    p, False, a_3, "expression expected after dictionary key and ':'"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_kwarg(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule__tmp_63(p);
    if a is not None {
        b = peg_expect_token(p, 22);
        if b is not None {
            res = pa_raise_syntax_known_range(p, False, a, b, "cannot assign to %s");
            return res;
        }
    }
    peg_reset(p, mark);
    a_2 = pa_name_from_token(peg_expect_token(p, NAME));
    if a_2 is not None {
        b = peg_expect_token(p, 22);
        if b is not None {
            expression = rule_expression(p);
            if expression is not None {
                for_if_clauses = rule_for_if_clauses(p);
                if for_if_clauses is not None {
                    res = pa_raise_syntax_known_range(
                        p,
                        False,
                        a_2,
                        b,
                        "invalid syntax. Maybe you meant '==' or ':=' instead of '='?"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    if ((rule__tmp_64(p) is None)) {
        a_3 = rule_expression(p);
        if a_3 is not None {
            b = peg_expect_token(p, 22);
            if b is not None {
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    a_3,
                    b,
                    "expression cannot contain assignment, perhaps you meant \\\"==\\\"?"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_token(p, 35);
    if a is not None {
        expression = rule_expression(p);
        if expression is not None {
            lit = peg_expect_token(p, 22);
            if lit is not None {
                b_4 = rule_expression(p);
                if b_4 is not None {
                    res = pa_raise_syntax_known_range(
                        p, False, a, b_4, "cannot assign to keyword argument unpacking"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_kwds(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 35);
    if lit is not None {
        param = rule_param(p);
        if param is not None {
            a = peg_expect_token(p, 22);
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "var-keyword argument cannot have default value"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        param = rule_param(p);
        if param is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                a_2 = rule_param(p);
                if a_2 is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a_2, "arguments cannot follow var-keyword argument"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        param = rule_param(p);
        if param is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                a = rule__tmp_65(p);
                if a is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a, "arguments cannot follow var-keyword argument"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_lambda_kwds(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 35);
    if lit is not None {
        lambda_param = rule_lambda_param(p);
        if lambda_param is not None {
            a = peg_expect_token(p, 22);
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "var-keyword argument cannot have default value"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        lambda_param = rule_lambda_param(p);
        if lambda_param is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                a_2 = rule_lambda_param(p);
                if a_2 is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a_2, "arguments cannot follow var-keyword argument"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        lambda_param = rule_lambda_param(p);
        if lambda_param is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                a = rule__tmp_65(p);
                if a is not None {
                    res = pa_raise_syntax_known_expr(
                        p, False, a, "arguments cannot follow var-keyword argument"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_lambda_parameters(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 17);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "at least one argument must precede /"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    rhs = rule__tmp_66(p);
    if rhs is not None {
        seq = rule__loop0_67(p);
        if seq is not None {
            a = peg_expect_token(p, 17);
            if a is not None {
                res = pa_raise_syntax_known_expr(p, False, a, "/ may appear only once");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lambda_slash_no_default = rule_lambda_slash_no_default(p);
    seq_3 = rule__loop0_68(p);
    if seq_3 is not None {
        invalid_lambda_parameters_helper = rule_invalid_lambda_parameters_helper(p);
        if invalid_lambda_parameters_helper is not None {
            a_3 = rule_lambda_param_no_default(p);
            if a_3 is not None {
                res = pa_raise_syntax_known_expr(
                    p,
                    False,
                    a_3,
                    "parameter without a_3 default follows parameter with a_3 default"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    seq_4 = rule__loop0_68(p);
    if seq_4 is not None {
        a = peg_expect_token(p, 7);
        if a is not None {
            gather = rule__gather_70(p);
            if gather is not None {
                lit = peg_expect_token(p, 12);
                b = peg_expect_token(p, 8);
                if b is not None {
                    res = pa_raise_syntax_known_range(
                        p,
                        False,
                        a,
                        b,
                        "Lambda expression parameters cannot be parenthesized"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    rhs = rule__tmp_66(p);
    seq = rule__loop0_67(p);
    if seq is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            rhs_1 = rule__tmp_71(p);
            if rhs_1 is not None {
                seq_1 = rule__loop0_67(p);
                if seq_1 is not None {
                    a = peg_expect_token(p, 17);
                    if a is not None {
                        res = pa_raise_syntax_known_expr(
                            p, False, a, "/ must be ahead of *"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    seq = rule__loop1_72(p);
    if seq is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            a = peg_expect_token(p, 16);
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "expected comma between / and *"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_lambda_parameters_helper(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule_lambda_slash_with_default(p);
    if a is not None {
        res = pa_singleton_seq(a);
        return res;
    }
    peg_reset(p, mark);
    seq = rule__loop1_73(p);
    if seq is not None {
        res = seq as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_lambda_star_etc(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        rhs = rule__tmp_74(p);
        if rhs is not None {
            res = pa_raise_syntax(p, False, "named arguments must follow bare *");
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        lambda_param = rule_lambda_param(p);
        if lambda_param is not None {
            a = peg_expect_token(p, 22);
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "var-positional argument cannot have default value"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        rhs_3 = rule__tmp_76(p);
        if rhs_3 is not None {
            seq = rule__loop0_67(p);
            if seq is not None {
                a = peg_expect_token(p, 16);
                if a is not None {
                    rhs_3_1 = rule__tmp_76(p);
                    if rhs_3_1 is not None {
                        res = pa_raise_syntax_known_expr(
                            p, False, a, "* argument may appear only once"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_legacy_expression(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        if (peg_negative_lookahead_token(p, 7)) {
            b = rule_star_expressions(p);
            if b is not None {
                res = pa_raise_syntax_known_range(
                    p,
                    False,
                    a,
                    b,
                    "Missing parentheses in call to '%U'. Did you mean %U(...)?"
                )
                    if pa_check_legacy_stmt(p, a)
                    else None;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_match_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    soft = peg_expect_soft_keyword(p, "match");
    if soft is not None {
        subject_expr = rule_subject_expr(p);
        if subject_expr is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_soft_keyword(p, "match");
    if a is not None {
        subject = rule_subject_expr(p);
        if subject is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'match' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_named_expression(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    hit = peg_check_memo(p, RULE_invalid_named_expression);
    if hit.hit {
        return hit.result as object | None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule_expression(p);
    if a is not None {
        lit = peg_expect_token(p, 53);
        if lit is not None {
            expression = rule_expression(p);
            if expression is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "cannot use assignment expressions with %s"
                );
                peg_insert_memo(p, mark, RULE_invalid_named_expression, res);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = pa_name_from_token(peg_expect_token(p, NAME));
    if a_2 is not None {
        lit = peg_expect_token(p, 22);
        if lit is not None {
            b = rule_bitwise_or(p);
            if b is not None {
                if (
                    (
                        peg_negative_lookahead_token(p, 22)
                        and peg_negative_lookahead_token(p, 53)
                    )
                ) {
                    res = pa_raise_syntax_known_range(
                        p,
                        False,
                        a_2,
                        b,
                        "invalid syntax. Maybe you meant '==' or ':=' instead of '='?"
                    );
                    peg_insert_memo(p, mark, RULE_invalid_named_expression, res);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    if ((rule__tmp_77(p) is None)) {
        a = rule_bitwise_or(p);
        if a is not None {
            b_3 = peg_expect_token(p, 22);
            if b_3 is not None {
                bitwise_or = rule_bitwise_or(p);
                if bitwise_or is not None {
                    if (
                        (
                            peg_negative_lookahead_token(p, 22)
                            and peg_negative_lookahead_token(p, 53)
                        )
                    ) {
                        res = pa_raise_syntax_known_expr(
                            p,
                            False,
                            a,
                            "cannot assign to %s here. Maybe you meant '==' instead of '='?"
                        );
                        peg_insert_memo(p, mark, RULE_invalid_named_expression, res);
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_invalid_named_expression, None);
    return None;
}

def rule_invalid_parameters(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 17);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "at least one argument must precede /"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    rhs = rule__tmp_78(p);
    if rhs is not None {
        seq = rule__loop0_79(p);
        if seq is not None {
            a = peg_expect_token(p, 17);
            if a is not None {
                res = pa_raise_syntax_known_expr(p, False, a, "/ may appear only once");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    slash_no_default = rule_slash_no_default(p);
    seq_3 = rule__loop0_80(p);
    if seq_3 is not None {
        invalid_parameters_helper = rule_invalid_parameters_helper(p);
        if invalid_parameters_helper is not None {
            a_3 = rule_param_no_default(p);
            if a_3 is not None {
                res = pa_raise_syntax_known_expr(
                    p,
                    False,
                    a_3,
                    "parameter without a_3 default follows parameter with a_3 default"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    seq_4 = rule__loop0_80(p);
    if seq_4 is not None {
        a = peg_expect_token(p, 7);
        if a is not None {
            seq_4_1 = rule__loop1_81(p);
            if seq_4_1 is not None {
                lit = peg_expect_token(p, 12);
                b = peg_expect_token(p, 8);
                if b is not None {
                    res = pa_raise_syntax_known_range(
                        p, False, a, b, "Function parameters cannot be parenthesized"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    rhs = rule__tmp_78(p);
    seq = rule__loop0_79(p);
    if seq is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            rhs_1 = rule__tmp_82(p);
            if rhs_1 is not None {
                seq_1 = rule__loop0_79(p);
                if seq_1 is not None {
                    a = peg_expect_token(p, 17);
                    if a is not None {
                        res = pa_raise_syntax_known_expr(
                            p, False, a, "/ must be ahead of *"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    seq = rule__loop1_83(p);
    if seq is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            a = peg_expect_token(p, 16);
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "expected comma between / and *"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_parameters_helper(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule_slash_with_default(p);
    if a is not None {
        res = pa_singleton_seq(a);
        return res;
    }
    peg_reset(p, mark);
    seq = rule__loop1_84(p);
    if seq is not None {
        res = seq as object;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_star_etc(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 16);
    if a is not None {
        rhs = rule__tmp_85(p);
        if rhs is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "named arguments must follow bare *"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        lit_1 = peg_expect_token(p, 12);
        if lit_1 is not None {
            type_comment_tok = peg_expect_token(p, TYPE_COMMENT);
            if type_comment_tok is not None {
                res = pa_raise_syntax(p, False, "bare * has associated type comment");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        param = rule_param(p);
        if param is not None {
            a = peg_expect_token(p, 22);
            if a is not None {
                res = pa_raise_syntax_known_expr(
                    p, False, a, "var-positional argument cannot have default value"
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        rhs_4 = rule__tmp_87(p);
        if rhs_4 is not None {
            seq = rule__loop0_79(p);
            if seq is not None {
                a = peg_expect_token(p, 16);
                if a is not None {
                    rhs_4_1 = rule__tmp_87(p);
                    if rhs_4_1 is not None {
                        res = pa_raise_syntax_known_expr(
                            p, False, a, "* argument may appear only once"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_starred_expression(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        res = pa_raise_syntax(p, False, "Invalid star expression");
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_starred_expression_unpacking(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 16);
    if a is not None {
        expression = rule_expression(p);
        if expression is not None {
            lit = peg_expect_token(p, 22);
            if lit is not None {
                b = rule_expression(p);
                if b is not None {
                    res = pa_raise_syntax_known_range(
                        p, False, a, b, "cannot assign to iterable argument unpacking"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_string_tstring_concat(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = rule__loop1_88(p);
    if a is not None {
        b = rule_tstring(p);
        if b is not None {
            res = pa_raise_syntax_known_range(
                p,
                False,
                pa_seq_last(a),
                b,
                "cannot mix t-string literals with string or bytes literals"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule__loop1_89(p);
    if a is not None {
        b = rule__tmp_90(p);
        if b is not None {
            res = pa_raise_syntax_known_range(
                p,
                False,
                pa_seq_last(a),
                b,
                "cannot mix t-string literals with string or bytes literals"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_try_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    a = peg_expect_token(p, 692);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                if (peg_negative_lookahead_token(p, INDENT)) {
                    res = pa_raise_syntax(
                        p,
                        False,
                        "expected an indented block after 'try' statement on line %d"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 692);
    if kw is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            block = rule_block(p);
            if block is not None {
                if (
                    (
                        peg_negative_lookahead_token(p, 642)
                        and peg_negative_lookahead_token(p, 637)
                    )
                ) {
                    res = pa_raise_syntax(
                        p, False, "expected 'except' or 'finally' block"
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 692);
    if kw is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            seq_3 = rule__loop0_91(p);
            if seq_3 is not None {
                seq_3_1 = rule__loop1_92(p);
                if seq_3_1 is not None {
                    a = peg_expect_token(p, 642);
                    if a is not None {
                        b = peg_expect_token(p, 16);
                        if b is not None {
                            expression = rule_expression(p);
                            if expression is not None {
                                rhs = rule__tmp_19(p);
                                lit_1 = peg_expect_token(p, 11);
                                if lit_1 is not None {
                                    res = pa_raise_syntax_known_range(
                                        p,
                                        False,
                                        a,
                                        b,
                                        "cannot have both 'except' and 'except*' on the same 'try'"
                                    );
                                    return res;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 692);
    if kw is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            seq_4 = rule__loop0_91(p);
            if seq_4 is not None {
                seq_4_1 = rule__loop1_93(p);
                if seq_4_1 is not None {
                    a = peg_expect_token(p, 642);
                    if a is not None {
                        rhs_4 = rule__tmp_94(p);
                        lit_1 = peg_expect_token(p, 11);
                        if lit_1 is not None {
                            res = pa_raise_syntax_known_expr(
                                p,
                                False,
                                a,
                                "cannot have both 'except' and 'except*' on the same 'try'"
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_tstring_conversion_character(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 54);
    if lit is not None {
        if (
            (peg_positive_lookahead_token(p, 11) or peg_positive_lookahead_token(p, 26))
        ) {
            res = pa_raise_syntax(p, True, "t-string: missing conversion character");
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 54);
    if lit is not None {
        if (peg_negative_lookahead_token(p, NAME)) {
            res = pa_raise_syntax(p, True, "t-string: invalid conversion character");
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_tstring_replacement_field(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 22);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "t-string: valid expression required before '='"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 54);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "t-string: valid expression required before '!'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 11);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "t-string: valid expression required before ':'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = peg_expect_token(p, 26);
        if a is not None {
            res = pa_raise_syntax_known_expr(
                p, False, a, "t-string: valid expression required before '}'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        if ((rule_annotated_rhs(p) is None)) {
            res = pa_raise_syntax(
                p, True, "t-string: expecting a valid expression after '{'"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            if (
                (
                    peg_negative_lookahead_token(p, 22)
                    and peg_negative_lookahead_token(p, 54)
                    and peg_negative_lookahead_token(p, 11)
                    and peg_negative_lookahead_token(p, 26)
                )
            ) {
                res = (
                    None
                        if pa_err_occurred(p)
                        else pa_raise_syntax(
                            p, True, "t-string: expecting '=', or '!', or ':', or '}'"
                        )
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            if lit_1 is not None {
                if (
                    (
                        peg_negative_lookahead_token(p, 54)
                        and peg_negative_lookahead_token(p, 11)
                        and peg_negative_lookahead_token(p, 26)
                    )
                ) {
                    res = (
                        None
                            if pa_err_occurred(p)
                            else pa_raise_syntax(
                                p, True, "t-string: expecting '!', or ':', or '}'"
                            )
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            invalid_tstring_conversion_character = rule_invalid_tstring_conversion_character(
                p
            );
            if invalid_tstring_conversion_character is not None {
                res = [lit, annotated_rhs, lit_1, invalid_tstring_conversion_character];
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            rhs = rule__tmp_60(p);
            if (
                (
                    peg_negative_lookahead_token(p, 11)
                    and peg_negative_lookahead_token(p, 26)
                )
            ) {
                res = (
                    None
                        if pa_err_occurred(p)
                        else pa_raise_syntax(p, True, "t-string: expecting ':' or '}'")
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            rhs = rule__tmp_60(p);
            lit_2 = peg_expect_token(p, 11);
            if lit_2 is not None {
                seq = rule__loop0_29(p);
                if seq is not None {
                    if (peg_negative_lookahead_token(p, 26)) {
                        res = (
                            None
                                if pa_err_occurred(p)
                                else pa_raise_syntax(
                                    p, True, "t-string: expecting '}', or format specs"
                                )
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        annotated_rhs = rule_annotated_rhs(p);
        if annotated_rhs is not None {
            lit_1 = peg_expect_token(p, 22);
            rhs = rule__tmp_60(p);
            if (peg_negative_lookahead_token(p, 26)) {
                res = (
                    None
                        if pa_err_occurred(p)
                        else pa_raise_syntax(p, True, "t-string: expecting '}'")
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_type_param(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = pa_name_from_token(peg_expect_token(p, NAME));
        if a is not None {
            colon = peg_expect_token(p, 11);
            if colon is not None {
                e = rule_expression(p);
                if e is not None {
                    res = pa_raise_type_param_error(p, colon, e, True);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a = pa_name_from_token(peg_expect_token(p, NAME));
        if a is not None {
            colon = peg_expect_token(p, 11);
            if colon is not None {
                e = rule_expression(p);
                if e is not None {
                    res = pa_raise_type_param_error(p, colon, e, False);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_type_params(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 9);
    if lit is not None {
        token = peg_expect_token(p, 10);
        if token is not None {
            res = pa_raise_syntax_known_expr(
                p, False, token, "Type parameter list cannot be empty"
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_while_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 693);
    if kw is not None {
        named_expression = rule_named_expression(p);
        if named_expression is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = peg_expect_token(p, 693);
    if a is not None {
        named_expression = rule_named_expression(p);
        if named_expression is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'while' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_with_item(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    expression = rule_expression(p);
    if expression is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            a = rule_expression(p);
            if a is not None {
                if (
                    (
                        peg_positive_lookahead_token(p, 12)
                        or peg_positive_lookahead_token(p, 8)
                        or peg_positive_lookahead_token(p, 11)
                    )
                ) {
                    res = pa_raise_invalid_target(p, STAR_TARGETS, a);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_with_stmt(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 699);
    kw_1 = peg_expect_token(p, 700);
    if kw_1 is not None {
        gather = rule__gather_97(p);
        if gather is not None {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_raise_syntax(p, False, "expected ':'");
                return res;
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    kw_1 = peg_expect_token(p, 700);
    if kw_1 is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            gather = rule__gather_99(p);
            if gather is not None {
                lit_1 = peg_expect_token(p, 12);
                lit_2 = peg_expect_token(p, 8);
                if lit_2 is not None {
                    _newline = peg_expect_token(p, NEWLINE);
                    if _newline is not None {
                        res = pa_raise_syntax(p, False, "expected ':'");
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_invalid_with_stmt_indent(p: peg_parser) -> object | None {
    if not p.call_invalid_rules {
        return None;
    }
    mark = peg_mark(p);
    res: object | None = None;
    kw = peg_expect_token(p, 699);
    a = peg_expect_token(p, 700);
    if a is not None {
        gather = rule__gather_97(p);
        if gather is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    if (peg_negative_lookahead_token(p, INDENT)) {
                        res = pa_raise_syntax(
                            p,
                            False,
                            "expected an indented block after 'with' statement on line %d"
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    a = peg_expect_token(p, 700);
    if a is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            gather = rule__gather_99(p);
            if gather is not None {
                lit_1 = peg_expect_token(p, 12);
                lit_2 = peg_expect_token(p, 8);
                if lit_2 is not None {
                    lit_3 = peg_expect_token(p, 11);
                    if lit_3 is not None {
                        _newline = peg_expect_token(p, NEWLINE);
                        if _newline is not None {
                            if (peg_negative_lookahead_token(p, INDENT)) {
                                res = pa_raise_syntax(
                                    p,
                                    False,
                                    "expected an indented block after 'with' statement on line %d"
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_inversion(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_inversion);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    kw = peg_expect_token(p, 671);
    if kw is not None {
        a = rule_inversion(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_unaryop(
                Not(), a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            peg_insert_memo(p, mark, RULE_inversion, res);
            return res;
        }
    }
    peg_reset(p, mark);
    comparison = rule_comparison(p);
    if comparison is not None {
        res = comparison;
        peg_insert_memo(p, mark, RULE_inversion, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_inversion, None);
    return None;
}

def rule_is_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    kw = peg_expect_token(p, 661);
    if kw is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(Is(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_isnot_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    kw = peg_expect_token(p, 661);
    if kw is not None {
        kw_1 = peg_expect_token(p, 671);
        if kw_1 is not None {
            a = rule_bitwise_or(p);
            if a is not None {
                res = pa_make_cmpop_pair(IsNot(), a);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_items_pattern(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    gather = rule__gather_101(p);
    if gather is not None {
        res = gather as list[pattern];
        return res as list[pattern];
    }
    peg_reset(p, mark);
    return None;
}

def rule_key_value_pattern(p: peg_parser) -> kp_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: kp_pair | None = None;
    key = rule__tmp_102(p);
    if key is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            pattern = rule_pattern(p);
            if pattern is not None {
                res = pa_key_pattern_pair(
                    key,
                    pattern,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_keyword_pattern(p: peg_parser) -> kp_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: kp_pair | None = None;
    arg = pa_name_from_token(peg_expect_token(p, NAME));
    if arg is not None {
        lit = peg_expect_token(p, 22);
        if lit is not None {
            value = rule_pattern(p);
            if value is not None {
                res = pa_key_pattern_pair(
                    arg,
                    value,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_keyword_patterns(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    gather = rule__gather_104(p);
    if gather is not None {
        res = gather as list[pattern];
        return res as list[pattern];
    }
    peg_reset(p, mark);
    return None;
}

def rule_kvpair(p: peg_parser) -> kv_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: kv_pair | None = None;
    a = rule_expression(p);
    if a is not None {
        lit = peg_expect_token(p, 11);
        if lit is not None {
            b = rule_expression(p);
            if b is not None {
                res = pa_key_value_pair(
                    a, b, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_kwarg_or_double_starred(p: peg_parser) -> keyword_or_starred_node | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: keyword_or_starred_node | None = None;
    invalid_kwarg = rule_invalid_kwarg(p);
    if invalid_kwarg is not None {
        res = invalid_kwarg as keyword_or_starred_node;
        return res;
    }
    peg_reset(p, mark);
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        lit = peg_expect_token(p, 22);
        if lit is not None {
            b = rule_expression(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_keyword_or_starred(
                    pa_check(
                        keyword(
                            arg=pa_name_id(a),
                            value=b,
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        )
                    ) as keyword,
                    1,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a_3 = rule_expression(p);
        if a_3 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_keyword_or_starred(
                pa_check(
                    keyword(
                        arg=None,
                        value=a_3,
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    )
                ) as keyword,
                1,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_kwarg_or_starred(p: peg_parser) -> keyword_or_starred_node | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: keyword_or_starred_node | None = None;
    invalid_kwarg = rule_invalid_kwarg(p);
    if invalid_kwarg is not None {
        res = invalid_kwarg as keyword_or_starred_node;
        return res;
    }
    peg_reset(p, mark);
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        lit = peg_expect_token(p, 22);
        if lit is not None {
            b = rule_expression(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_keyword_or_starred(
                    pa_check(
                        keyword(
                            arg=pa_name_id(a),
                            value=b,
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        )
                    ) as keyword,
                    1,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_3 = rule_starred_expression(p);
    if a_3 is not None {
        res = pa_keyword_or_starred(
            a_3, 0, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_kwargs(p: peg_parser) -> list[keyword_or_starred_node] | None {
    mark = peg_mark(p);
    res: list[keyword_or_starred_node] | None = None;
    a = rule__gather_106(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            b = rule__gather_108(p);
            if b is not None {
                res = pa_join_sequences_kw_or_starred(a, b);
                return res as list[keyword_or_starred_node];
            }
        }
    }
    peg_reset(p, mark);
    gather = rule__gather_106(p);
    if gather is not None {
        res = gather;
        return res as list[keyword_or_starred_node];
    }
    peg_reset(p, mark);
    gather = rule__gather_108(p);
    if gather is not None {
        res = gather;
        return res as list[keyword_or_starred_node];
    }
    peg_reset(p, mark);
    return None;
}

def rule_kwds(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    res: arg | None = None;
    invalid_kwds = rule_invalid_kwds(p);
    if invalid_kwds is not None {
        res = invalid_kwds as arg;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a = rule_param_no_default(p);
        if a is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_kwds(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    res: arg | None = None;
    invalid_lambda_kwds = rule_invalid_lambda_kwds(p);
    if invalid_lambda_kwds is not None {
        res = invalid_lambda_kwds as arg;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a = rule_lambda_param_no_default(p);
        if a is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_param(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: arg | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = arg(
            arg=pa_name_id(a),
            annotation=None,
            type_comment=None,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_param_maybe_default(p: peg_parser) -> name_default_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: name_default_pair | None = None;
    a = rule_lambda_param(p);
    if a is not None {
        c = rule_default(p);
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = pa_name_default_pair(
                a, c, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_lambda_param(p);
    if a is not None {
        c = rule_default(p);
        if (peg_positive_lookahead_token(p, 11)) {
            res = pa_name_default_pair(
                a, c, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_param_no_default(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    res: arg | None = None;
    a = rule_lambda_param(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_lambda_param(p);
    if a is not None {
        if (peg_positive_lookahead_token(p, 11)) {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_param_with_default(p: peg_parser) -> name_default_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: name_default_pair | None = None;
    a = rule_lambda_param(p);
    if a is not None {
        c = rule_default(p);
        if c is not None {
            lit = peg_expect_token(p, 12);
            if lit is not None {
                res = pa_name_default_pair(
                    a, c, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_lambda_param(p);
    if a is not None {
        c = rule_default(p);
        if c is not None {
            if (peg_positive_lookahead_token(p, 11)) {
                res = pa_name_default_pair(
                    a, c, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_parameters(p: peg_parser) -> arguments | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: arguments | None = None;
    a = rule_lambda_slash_no_default(p);
    if a is not None {
        b = rule__loop0_68(p);
        if b is not None {
            c = rule__loop0_109(p);
            if c is not None {
                d = rule_lambda_star_etc(p);
                res = pa_make_arguments(
                    a,
                    None,
                    b,
                    c,
                    d,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = rule_lambda_slash_with_default(p);
    if a_2 is not None {
        b_2 = rule__loop0_109(p);
        if b_2 is not None {
            c_2 = rule_lambda_star_etc(p);
            res = pa_make_arguments(
                None,
                a_2,
                None,
                b_2,
                c_2,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule__loop1_110(p);
    if a is not None {
        b_3 = rule__loop0_109(p);
        if b_3 is not None {
            c_3 = rule_lambda_star_etc(p);
            res = pa_make_arguments(
                None,
                None,
                a,
                b_3,
                c_3,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a_4 = rule__loop1_73(p);
    if a_4 is not None {
        b_4 = rule_lambda_star_etc(p);
        res = pa_make_arguments(
            None,
            None,
            None,
            a_4,
            b_4,
            start_lineno,
            start_col_offset,
            end_lineno,
            end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    a_5 = rule_lambda_star_etc(p);
    if a_5 is not None {
        res = pa_make_arguments(
            None,
            None,
            None,
            None,
            a_5,
            start_lineno,
            start_col_offset,
            end_lineno,
            end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_params(p: peg_parser) -> arguments | None {
    mark = peg_mark(p);
    res: arguments | None = None;
    invalid_lambda_parameters = rule_invalid_lambda_parameters(p);
    if invalid_lambda_parameters is not None {
        res = invalid_lambda_parameters as arguments;
        return res;
    }
    peg_reset(p, mark);
    lambda_parameters = rule_lambda_parameters(p);
    if lambda_parameters is not None {
        res = lambda_parameters;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_slash_no_default(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    res: list[arg] | None = None;
    a = rule__loop1_110(p);
    if a is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule__loop1_110(p);
    if a is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            if (peg_positive_lookahead_token(p, 11)) {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_slash_with_default(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: object | None = None;
    a = rule__loop0_68(p);
    if a is not None {
        b = rule__loop1_73(p);
        if b is not None {
            lit = peg_expect_token(p, 17);
            if lit is not None {
                lit_1 = peg_expect_token(p, 12);
                if lit_1 is not None {
                    res = pa_slash_with_default(
                        a, b, start_lineno, start_col_offset, end_lineno, end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule__loop0_68(p);
    if a is not None {
        b = rule__loop1_73(p);
        if b is not None {
            lit = peg_expect_token(p, 17);
            if lit is not None {
                if (peg_positive_lookahead_token(p, 11)) {
                    res = pa_slash_with_default(
                        a, b, start_lineno, start_col_offset, end_lineno, end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambda_star_etc(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: object | None = None;
    invalid_lambda_star_etc = rule_invalid_lambda_star_etc(p);
    if invalid_lambda_star_etc is not None {
        res = invalid_lambda_star_etc;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule_lambda_param_no_default(p);
        if a is not None {
            b = rule__loop0_67(p);
            if b is not None {
                c = rule_lambda_kwds(p);
                res = pa_star_etc(
                    a, b, c, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        lit_1 = peg_expect_token(p, 12);
        if lit_1 is not None {
            b = rule__loop1_72(p);
            if b is not None {
                c = rule_lambda_kwds(p);
                res = pa_star_etc(
                    None,
                    b,
                    c,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_lambda_kwds(p);
    if a is not None {
        res = pa_star_etc(
            None, None, a, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_lambdef(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    kw = peg_expect_token(p, 663);
    if kw is not None {
        a = rule_lambda_params(p);
        lit = peg_expect_token(p, 11);
        if lit is not None {
            b = rule_expression(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = Lambda(
                    args=(
                        a
                            if a is not None
                            else pa_check(pa_empty_arguments()) as arguments
                    ),
                    body=b,
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_list(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 9);
    if lit is not None {
        a = rule_star_named_expressions(p);
        lit_1 = peg_expect_token(p, 10);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = List(
                elts=(a if a is not None else []),
                ctx=Load(),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_listcomp(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 9);
    if lit is not None {
        a = rule_named_expression(p);
        if a is not None {
            b = rule_for_if_clauses(p);
            if b is not None {
                lit_1 = peg_expect_token(p, 10);
                if lit_1 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = ListComp(
                        elt=a,
                        generators=(b if b is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_comprehension = rule_invalid_comprehension(p);
    if invalid_comprehension is not None {
        res = invalid_comprehension as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_literal_expr(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    signed_number = rule_signed_number(p);
    if signed_number is not None {
        if (
            (
                peg_negative_lookahead_token(p, 14)
                and peg_negative_lookahead_token(p, 15)
            )
        ) {
            res = signed_number;
            return res;
        }
    }
    peg_reset(p, mark);
    complex_number = rule_complex_number(p);
    if complex_number is not None {
        res = complex_number;
        return res;
    }
    peg_reset(p, mark);
    if (
        (
            peg_positive_lookahead_token(p, STRING)
            or peg_positive_lookahead_token(p, FSTRING_START)
            or peg_positive_lookahead_token(p, TSTRING_START)
        )
    ) {
        strings = rule_strings(p);
        if strings is not None {
            res = strings;
            return res;
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 667);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_none(
            start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 668);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_bool(
            True, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 669);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_constant_bool(
            False, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_literal_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    value = rule_signed_number(p);
    if value is not None {
        if (
            (
                peg_negative_lookahead_token(p, 14)
                and peg_negative_lookahead_token(p, 15)
            )
        ) {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchValue(
                value=value,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    value = rule_complex_number(p);
    if value is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = MatchValue(
            value=value,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    value = rule_strings(p);
    if value is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = MatchValue(
            value=value,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 667);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_match_singleton(
            None, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 668);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_match_singleton(
            True, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 669);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_match_singleton(
            False, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_lt_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    lit = peg_expect_token(p, 20);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(Lt(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_lte_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    lit = peg_expect_token(p, 29);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(LtE(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_mapping_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        lit_1 = peg_expect_token(p, 26);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchMapping(
                keys=[],
                patterns=[],
                rest=None,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        rest = rule_double_star_pattern(p);
        if rest is not None {
            lit_1 = peg_expect_token(p, 12);
            lit_2 = peg_expect_token(p, 26);
            if lit_2 is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = MatchMapping(
                    keys=[],
                    patterns=[],
                    rest=pa_name_id(rest),
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        items = pa_pattern_list(rule_items_pattern(p));
        if items is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                rest = rule_double_star_pattern(p);
                if rest is not None {
                    lit_2 = peg_expect_token(p, 12);
                    lit_3 = peg_expect_token(p, 26);
                    if lit_3 is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = MatchMapping(
                            keys=(
                                pa_check(pa_get_pattern_keys(items)) as list[expr]
                                    if pa_check(pa_get_pattern_keys(items)) as list[
                                        expr
                                    ]
                                        is not None
                                    else []
                            ),
                            patterns=(
                                pa_check(pa_get_patterns(items)) as list[pattern]
                                    if pa_check(pa_get_patterns(items)) as list[pattern]
                                        is not None
                                    else []
                            ),
                            rest=pa_name_id(rest),
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 25);
    if lit is not None {
        items = pa_pattern_list(rule_items_pattern(p));
        if items is not None {
            lit_1 = peg_expect_token(p, 12);
            lit_2 = peg_expect_token(p, 26);
            if lit_2 is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = MatchMapping(
                    keys=(
                        pa_check(pa_get_pattern_keys(items)) as list[expr]
                            if pa_check(pa_get_pattern_keys(items)) as list[expr]
                                is not None
                            else []
                    ),
                    patterns=(
                        pa_check(pa_get_patterns(items)) as list[pattern]
                            if pa_check(pa_get_patterns(items)) as list[pattern]
                                is not None
                            else []
                    ),
                    rest=None,
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_match_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    soft = peg_expect_soft_keyword(p, "match");
    if soft is not None {
        subject = rule_subject_expr(p);
        if subject is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                _newline = peg_expect_token(p, NEWLINE);
                if _newline is not None {
                    _indent = peg_expect_token(p, INDENT);
                    if _indent is not None {
                        cases = rule__loop1_111(p);
                        if cases is not None {
                            _dedent = peg_expect_token(p, DEDENT);
                            if _dedent is not None {
                                end_tok = peg_get_last_nonnwhitespace_token(p);
                                if end_tok is None {
                                    p.error_indicator = True;
                                    peg_reset(p, mark);
                                    return None;
                                }
                                end_lineno = end_tok.end_lineno;
                                end_col_offset = end_tok.end_col_offset;
                                res = Match(
                                    subject=subject,
                                    cases=(cases if cases is not None else []),
                                    lineno=start_lineno,
                                    col_offset=start_col_offset,
                                    end_lineno=end_lineno,
                                    end_col_offset=end_col_offset
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_match_stmt = rule_invalid_match_stmt(p);
    if invalid_match_stmt is not None {
        res = invalid_match_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_maybe_sequence_pattern(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    patterns = rule__gather_113(p);
    if patterns is not None {
        lit = peg_expect_token(p, 12);
        res = patterns;
        return res as list[pattern];
    }
    peg_reset(p, mark);
    return None;
}

def rule_maybe_star_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    res: pattern | None = None;
    star_pattern = rule_star_pattern(p);
    if star_pattern is not None {
        res = star_pattern;
        return res;
    }
    peg_reset(p, mark);
    pattern = rule_pattern(p);
    if pattern is not None {
        res = pattern;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_name_or_attr(p: peg_parser) -> expr | None {
    return rule_name_or_attr_raw(p);
}

def rule_name_or_attr_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    attr = rule_attr(p);
    if attr is not None {
        res = attr;
        return res;
    }
    peg_reset(p, mark);
    name_var = pa_name_from_token(peg_expect_token(p, NAME));
    if name_var is not None {
        res = name_var as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_named_expression(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    assignment_expression = rule_assignment_expression(p);
    if assignment_expression is not None {
        res = assignment_expression;
        return res;
    }
    peg_reset(p, mark);
    invalid_named_expression = rule_invalid_named_expression(p);
    if invalid_named_expression is not None {
        res = invalid_named_expression as expr;
        return res;
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        if (peg_negative_lookahead_token(p, 53)) {
            res = expression;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_nonlocal_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 689);
    if kw is not None {
        a = rule__gather_33(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Nonlocal(
                names=(
                    pa_check(pa_map_names_to_ids(a)) as list[str]
                        if pa_check(pa_map_names_to_ids(a)) as list[str] is not None
                        else []
                ),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_noteq_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    tok = peg_expect_token(p, 28);
    if tok is not None {
        barry = bool(p.flags & 0x400000);
        if (barry and tok.spelling == "!=") or (not barry and tok.spelling == "<>") {
            pa_raise_syntax_known_expr(
                p,
                False,
                tok,
                "with Barry as BDFL, use '<>' instead of '!='"
                    if barry
                    else "invalid syntax"
            );
            return None;
        }
        a = rule_bitwise_or(p);
        if a is not None {
            res = pa_make_cmpop_pair(NotEq(), a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_notin_bitwise_or(p: peg_parser) -> pa_cmpop_expr_pair | None {
    mark = peg_mark(p);
    res: pa_cmpop_expr_pair | None = None;
    kw = peg_expect_token(p, 671);
    if kw is not None {
        kw_1 = peg_expect_token(p, 672);
        if kw_1 is not None {
            a = rule_bitwise_or(p);
            if a is not None {
                res = pa_make_cmpop_pair(NotIn(), a);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_open_sequence_pattern(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    pattern = rule_maybe_star_pattern(p);
    if pattern is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            patterns = pa_pattern_list(rule_maybe_sequence_pattern(p));
            res = pa_pattern_list(pa_seq_insert_front_pattern(pattern, patterns));
            return res as list[pattern];
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_or_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    patterns = rule__gather_115(p);
    if patterns is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_or_pattern_singleton(
            patterns, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_param(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: arg | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        b = rule_annotation(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = arg(
            arg=pa_name_id(a),
            annotation=b,
            type_comment=None,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_param_maybe_default(p: peg_parser) -> name_default_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: name_default_pair | None = None;
    a = rule_param(p);
    if a is not None {
        c = rule_default(p);
        lit = peg_expect_token(p, 12);
        if lit is not None {
            tc = peg_expect_token(p, TYPE_COMMENT);
            res = pa_name_default_pair(
                pa_add_type_comment_to_arg(a, tc),
                c,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_param(p);
    if a is not None {
        c = rule_default(p);
        tc = peg_expect_token(p, TYPE_COMMENT);
        if (peg_positive_lookahead_token(p, 8)) {
            res = pa_name_default_pair(
                pa_add_type_comment_to_arg(a, tc),
                c,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_param_no_default(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    res: arg | None = None;
    a = rule_param(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            tc = peg_expect_token(p, TYPE_COMMENT);
            res = pa_add_type_comment_to_arg(a, tc);
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_param(p);
    if a is not None {
        tc = peg_expect_token(p, TYPE_COMMENT);
        if (peg_positive_lookahead_token(p, 8)) {
            res = pa_add_type_comment_to_arg(a, tc);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_param_no_default_star_annotation(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    res: arg | None = None;
    a = rule_param_star_annotation(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            tc = peg_expect_token(p, TYPE_COMMENT);
            res = pa_add_type_comment_to_arg(a, tc);
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_param_star_annotation(p);
    if a is not None {
        tc = peg_expect_token(p, TYPE_COMMENT);
        if (peg_positive_lookahead_token(p, 8)) {
            res = pa_add_type_comment_to_arg(a, tc);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_param_star_annotation(p: peg_parser) -> arg | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: arg | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        b = rule_star_annotation(p);
        if b is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = arg(
                arg=pa_name_id(a),
                annotation=b,
                type_comment=None,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_param_with_default(p: peg_parser) -> name_default_pair | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: name_default_pair | None = None;
    a = rule_param(p);
    if a is not None {
        c = rule_default(p);
        if c is not None {
            lit = peg_expect_token(p, 12);
            if lit is not None {
                tc = peg_expect_token(p, TYPE_COMMENT);
                res = pa_name_default_pair(
                    pa_add_type_comment_to_arg(a, tc),
                    c,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_param(p);
    if a is not None {
        c = rule_default(p);
        if c is not None {
            tc = peg_expect_token(p, TYPE_COMMENT);
            if (peg_positive_lookahead_token(p, 8)) {
                res = pa_name_default_pair(
                    pa_add_type_comment_to_arg(a, tc),
                    c,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_parameters(p: peg_parser) -> arguments | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: arguments | None = None;
    a = rule_slash_no_default(p);
    if a is not None {
        b = rule__loop0_80(p);
        if b is not None {
            c = rule__loop0_116(p);
            if c is not None {
                d = rule_star_etc(p);
                res = pa_make_arguments(
                    a,
                    None,
                    b,
                    c,
                    d,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = rule_slash_with_default(p);
    if a_2 is not None {
        b_2 = rule__loop0_116(p);
        if b_2 is not None {
            c_2 = rule_star_etc(p);
            res = pa_make_arguments(
                None,
                a_2,
                None,
                b_2,
                c_2,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule__loop1_81(p);
    if a is not None {
        b_3 = rule__loop0_116(p);
        if b_3 is not None {
            c_3 = rule_star_etc(p);
            res = pa_make_arguments(
                None,
                None,
                a,
                b_3,
                c_3,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a_4 = rule__loop1_84(p);
    if a_4 is not None {
        b_4 = rule_star_etc(p);
        res = pa_make_arguments(
            None,
            None,
            None,
            a_4,
            b_4,
            start_lineno,
            start_col_offset,
            end_lineno,
            end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    a_5 = rule_star_etc(p);
    if a_5 is not None {
        res = pa_make_arguments(
            None,
            None,
            None,
            None,
            a_5,
            start_lineno,
            start_col_offset,
            end_lineno,
            end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_params(p: peg_parser) -> arguments | None {
    mark = peg_mark(p);
    res: arguments | None = None;
    invalid_parameters = rule_invalid_parameters(p);
    if invalid_parameters is not None {
        res = invalid_parameters as arguments;
        return res;
    }
    peg_reset(p, mark);
    parameters = rule_parameters(p);
    if parameters is not None {
        res = parameters;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_pass_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 679);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_pass(start_lineno, start_col_offset, end_lineno, end_col_offset);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    res: pattern | None = None;
    as_pattern = rule_as_pattern(p);
    if as_pattern is not None {
        res = as_pattern;
        return res;
    }
    peg_reset(p, mark);
    or_pattern = rule_or_pattern(p);
    if or_pattern is not None {
        res = or_pattern;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_pattern_capture_target(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    if ((peg_expect_soft_keyword(p, "_") is None)) {
        name = pa_name_from_token(peg_expect_token(p, NAME));
        if name is not None {
            if (
                (
                    peg_negative_lookahead_token(p, 23)
                    and peg_negative_lookahead_token(p, 7)
                    and peg_negative_lookahead_token(p, 22)
                )
            ) {
                res = pa_set_context(name, Store());
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_patterns(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    open_patterns = rule_open_sequence_pattern(p);
    if open_patterns is not None {
        patterns = pa_pattern_list(open_patterns);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = MatchSequence(
            patterns=pa_pattern_list(patterns),
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    pattern = rule_pattern(p);
    if pattern is not None {
        res = pattern;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_positional_patterns(p: peg_parser) -> list[pattern] | None {
    mark = peg_mark(p);
    res: list[pattern] | None = None;
    args = rule__gather_118(p);
    if args is not None {
        res = args;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_power(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_await_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 35);
        if lit is not None {
            b = rule_factor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    Pow(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    await_primary = rule_await_primary(p);
    if await_primary is not None {
        res = await_primary;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_primary(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_primary);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_primary, res);
        peg_reset(p, mark);
        raw = rule_primary_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_primary_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            b = pa_name_from_token(peg_expect_token(p, NAME));
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_attribute(
                    a,
                    pa_name_id(b),
                    Load(),
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_primary(p);
    if a is not None {
        b_2 = rule_genexp(p);
        if b_2 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_call(
                a,
                pa_check(pa_singleton_seq_expr(b_2)) as list[expr],
                None,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            b_3 = rule_arguments(p);
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_call(
                    a,
                    (pa_call_args(b_3) if b_3 is not None else []),
                    (pa_call_keywords(b_3) if b_3 is not None else []),
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 9);
        if lit is not None {
            b_4 = rule_slices(p);
            if b_4 is not None {
                lit_1 = peg_expect_token(p, 10);
                if lit_1 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = pa_ast_subscript(
                        a,
                        b_4,
                        Load(),
                        start_lineno,
                        start_col_offset,
                        end_lineno,
                        end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    atom = rule_atom(p);
    if atom is not None {
        res = atom;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_raise_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 683);
    if kw is not None {
        a = rule_expression(p);
        if a is not None {
            b = rule__tmp_119(p);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = Raise(
                exc=a,
                cause=b,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 683);
    if kw is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = Raise(
            exc=None,
            cause=None,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_real_number(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    real = pa_number_from_token(peg_expect_token(p, NUMBER));
    if real is not None {
        res = pa_ensure_real(p, real);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_return_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    kw = peg_expect_token(p, 678);
    if kw is not None {
        a = rule_star_expressions(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_return(
            a, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_sequence_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    lit = peg_expect_token(p, 9);
    if lit is not None {
        patterns = pa_pattern_list(rule_maybe_sequence_pattern(p));
        lit_1 = peg_expect_token(p, 10);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchSequence(
                patterns=pa_pattern_list(patterns),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        patterns = pa_pattern_list(rule_open_sequence_pattern(p));
        lit_1 = peg_expect_token(p, 8);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchSequence(
                patterns=pa_pattern_list(patterns),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_set(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_star_named_expressions(p);
        if a is not None {
            lit_1 = peg_expect_token(p, 26);
            if lit_1 is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = Set(
                    elts=(a if a is not None else []),
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_setcomp(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_named_expression(p);
        if a is not None {
            b = rule_for_if_clauses(p);
            if b is not None {
                lit_1 = peg_expect_token(p, 26);
                if lit_1 is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = SetComp(
                        elt=a,
                        generators=(b if b is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_comprehension = rule_invalid_comprehension(p);
    if invalid_comprehension is not None {
        res = invalid_comprehension as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_shift_expr(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_shift_expr);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_shift_expr, res);
        peg_reset(p, mark);
        raw = rule_shift_expr_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_shift_expr_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_shift_expr(p);
    if a is not None {
        lit = peg_expect_token(p, 33);
        if lit is not None {
            b = rule_sum(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    LShift(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_shift_expr(p);
    if a is not None {
        lit = peg_expect_token(p, 34);
        if lit is not None {
            b = rule_sum(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    RShift(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_arithmetic = rule_invalid_arithmetic(p);
    if invalid_arithmetic is not None {
        res = invalid_arithmetic as expr;
        return res;
    }
    peg_reset(p, mark);
    sum = rule_sum(p);
    if sum is not None {
        res = sum;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_signed_number(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    number_var = pa_number_from_token(peg_expect_token(p, NUMBER));
    if number_var is not None {
        res = number_var;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 15);
    if lit is not None {
        number = pa_number_from_token(peg_expect_token(p, NUMBER));
        if number is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_unaryop(
                USub(),
                number,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_signed_real_number(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    real_number = rule_real_number(p);
    if real_number is not None {
        res = real_number;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 15);
    if lit is not None {
        real = rule_real_number(p);
        if real is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_unaryop(
                USub(), real, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_simple_stmt(p: peg_parser) -> stmt | None {
    hit = peg_check_memo(p, RULE_simple_stmt);
    if hit.hit {
        return hit.result as stmt | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    assignment = rule_assignment(p);
    if assignment is not None {
        res = assignment;
        peg_insert_memo(p, mark, RULE_simple_stmt, res);
        return res;
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 678)) {
        return_stmt = rule_return_stmt(p);
        if return_stmt is not None {
            res = return_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 679)) {
        pass_stmt = rule_pass_stmt(p);
        if pass_stmt is not None {
            res = pass_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    type_alias = rule_type_alias(p);
    if type_alias is not None {
        res = type_alias;
        peg_insert_memo(p, mark, RULE_simple_stmt, res);
        return res;
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 680)) {
        del_stmt = rule_del_stmt(p);
        if del_stmt is not None {
            res = del_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    e = rule_star_expressions(p);
    if e is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_expr_stmt(
            e, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        peg_insert_memo(p, mark, RULE_simple_stmt, res);
        return res;
    }
    peg_reset(p, mark);
    if (
        (peg_positive_lookahead_token(p, 681) or peg_positive_lookahead_token(p, 702))
    ) {
        import_stmt = rule_import_stmt(p);
        if import_stmt is not None {
            res = import_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 683)) {
        raise_stmt = rule_raise_stmt(p);
        if raise_stmt is not None {
            res = raise_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 703)) {
        yield_stmt = rule_yield_stmt(p);
        if yield_stmt is not None {
            res = yield_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 685)) {
        assert_stmt = rule_assert_stmt(p);
        if assert_stmt is not None {
            res = assert_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 686)) {
        break_stmt = rule_break_stmt(p);
        if break_stmt is not None {
            res = break_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 687)) {
        continue_stmt = rule_continue_stmt(p);
        if continue_stmt is not None {
            res = continue_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 688)) {
        global_stmt = rule_global_stmt(p);
        if global_stmt is not None {
            res = global_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    if (peg_positive_lookahead_token(p, 689)) {
        nonlocal_stmt = rule_nonlocal_stmt(p);
        if nonlocal_stmt is not None {
            res = nonlocal_stmt;
            peg_insert_memo(p, mark, RULE_simple_stmt, res);
            return res;
        }
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_simple_stmt, None);
    return None;
}

def rule_simple_stmts(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    a = rule_simple_stmt(p);
    if a is not None {
        if (peg_negative_lookahead_token(p, 13)) {
            _newline = peg_expect_token(p, NEWLINE);
            if _newline is not None {
                res = pa_singleton_seq_stmt(a);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a_2 = rule__gather_121(p);
    if a_2 is not None {
        lit = peg_expect_token(p, 13);
        _newline = peg_expect_token(p, NEWLINE);
        if _newline is not None {
            res = a_2;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_single_compound_stmt(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    a = rule_compound_stmt(p);
    if a is not None {
        res = pa_singleton_seq_stmt(a);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_single_subscript_attribute_target(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            b = pa_name_from_token(peg_expect_token(p, NAME));
            if b is not None {
                if ((rule_t_lookahead(p) is None)) {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = pa_ast_attribute(
                        a,
                        pa_name_id(b),
                        Store(),
                        start_lineno,
                        start_col_offset,
                        end_lineno,
                        end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 9);
        if lit is not None {
            b_2 = rule_slices(p);
            if b_2 is not None {
                lit_1 = peg_expect_token(p, 10);
                if lit_1 is not None {
                    if ((rule_t_lookahead(p) is None)) {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_ast_subscript(
                            a,
                            b_2,
                            Store(),
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_single_target(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    single_subscript_attribute_target = rule_single_subscript_attribute_target(p);
    if single_subscript_attribute_target is not None {
        res = single_subscript_attribute_target;
        return res;
    }
    peg_reset(p, mark);
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        res = pa_set_context(a, Store());
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a_3 = rule_single_target(p);
        if a_3 is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = a_3;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_slash_no_default(p: peg_parser) -> list[arg] | None {
    mark = peg_mark(p);
    res: list[arg] | None = None;
    a = rule__loop1_81(p);
    if a is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule__loop1_81(p);
    if a is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            if (peg_positive_lookahead_token(p, 8)) {
                res = a;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_slash_with_default(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: object | None = None;
    a = rule__loop0_80(p);
    if a is not None {
        b = rule__loop1_84(p);
        if b is not None {
            lit = peg_expect_token(p, 17);
            if lit is not None {
                lit_1 = peg_expect_token(p, 12);
                if lit_1 is not None {
                    res = pa_slash_with_default(
                        a, b, start_lineno, start_col_offset, end_lineno, end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule__loop0_80(p);
    if a is not None {
        b = rule__loop1_84(p);
        if b is not None {
            lit = peg_expect_token(p, 17);
            if lit is not None {
                if (peg_positive_lookahead_token(p, 8)) {
                    res = pa_slash_with_default(
                        a, b, start_lineno, start_col_offset, end_lineno, end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_slice(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_expression(p);
    lit = peg_expect_token(p, 11);
    if lit is not None {
        b = rule_expression(p);
        c = rule__tmp_122(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_slice(
            a, b, c, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    a = rule_named_expression(p);
    if a is not None {
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_slices(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_slice(p);
    if a is not None {
        if (peg_negative_lookahead_token(p, 12)) {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    a_2 = rule__gather_124(p);
    if a_2 is not None {
        lit = peg_expect_token(p, 12);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_tuple(
            a_2, Load(), start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_annotation(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 11);
    if lit is not None {
        a = rule_star_expression(p);
        if a is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_atom(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        res = pa_set_context(a, Store());
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a_2 = rule_target_with_star_atom(p);
        if a_2 is not None {
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                res = pa_set_context(a_2, Store());
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a_3 = rule_star_targets_tuple_seq(p);
        lit_1 = peg_expect_token(p, 8);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                a_3, Store(), start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 9);
    if lit is not None {
        a_4 = rule_star_targets_list_seq(p);
        lit_1 = peg_expect_token(p, 10);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = List(
                elts=(a_4 if a_4 is not None else []),
                ctx=Store(),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_etc(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: object | None = None;
    invalid_star_etc = rule_invalid_star_etc(p);
    if invalid_star_etc is not None {
        res = invalid_star_etc;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule_param_no_default(p);
        if a is not None {
            b = rule__loop0_79(p);
            if b is not None {
                c = rule_kwds(p);
                res = pa_star_etc(
                    a, b, c, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule_param_no_default_star_annotation(p);
        if a is not None {
            b = rule__loop0_79(p);
            if b is not None {
                c = rule_kwds(p);
                res = pa_star_etc(
                    a, b, c, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        lit_1 = peg_expect_token(p, 12);
        if lit_1 is not None {
            b = rule__loop1_83(p);
            if b is not None {
                c = rule_kwds(p);
                res = pa_star_etc(
                    None,
                    b,
                    c,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_kwds(p);
    if a is not None {
        res = pa_star_etc(
            None, None, a, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_expression(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_star_expression);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_starred(
                a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            peg_insert_memo(p, mark, RULE_star_expression, res);
            return res;
        }
    }
    peg_reset(p, mark);
    expression = rule_expression(p);
    if expression is not None {
        res = expression;
        peg_insert_memo(p, mark, RULE_star_expression, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_star_expression, None);
    return None;
}

def rule_star_expressions(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_star_expression(p);
    if a is not None {
        b = rule__loop1_125(p);
        if b is not None {
            lit = peg_expect_token(p, 12);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                pa_check(pa_seq_insert_front_expr(a, b)) as list[expr],
                Load(),
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_star_expression(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                pa_check(pa_singleton_seq_expr(a)) as list[expr],
                Load(),
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    star_expression = rule_star_expression(p);
    if star_expression is not None {
        res = star_expression;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_named_expression(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule_bitwise_or(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_starred(
                a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    named_expression = rule_named_expression(p);
    if named_expression is not None {
        res = named_expression;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_named_expressions(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    a = rule__gather_127(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_pattern(p: peg_parser) -> pattern | None {
    hit = peg_check_memo(p, RULE_star_pattern);
    if hit.hit {
        return hit.result as pattern | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        target = rule_pattern_capture_target(p);
        if target is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchStar(
                name=pa_name_id(target),
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            peg_insert_memo(p, mark, RULE_star_pattern, res);
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        wildcard_pattern = rule_wildcard_pattern(p);
        if wildcard_pattern is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchStar(
                name=None,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            peg_insert_memo(p, mark, RULE_star_pattern, res);
            return res;
        }
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_star_pattern, None);
    return None;
}

def rule_star_target(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_star_target);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule__tmp_128(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_starred(
                pa_check(pa_set_context(a, Store())) as expr,
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset,
                ctx=Store()
            );
            peg_insert_memo(p, mark, RULE_star_target, res);
            return res;
        }
    }
    peg_reset(p, mark);
    target_with_star_atom = rule_target_with_star_atom(p);
    if target_with_star_atom is not None {
        res = target_with_star_atom;
        peg_insert_memo(p, mark, RULE_star_target, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_star_target, None);
    return None;
}

def rule_star_targets(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_star_target(p);
    if a is not None {
        if (peg_negative_lookahead_token(p, 12)) {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_star_target(p);
    if a is not None {
        b = rule__loop0_129(p);
        if b is not None {
            lit = peg_expect_token(p, 12);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                pa_check(pa_seq_insert_front_expr(a, b)) as list[expr],
                Store(),
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_targets_list_seq(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    a = rule__gather_131(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_star_targets_tuple_seq(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    a = rule_star_target(p);
    if a is not None {
        b = rule__loop1_132(p);
        if b is not None {
            lit = peg_expect_token(p, 12);
            res = pa_seq_insert_front_expr(a, b);
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule_star_target(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            res = pa_singleton_seq_expr(a);
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_starred_expression(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    invalid_starred_expression_unpacking = rule_invalid_starred_expression_unpacking(p);
    if invalid_starred_expression_unpacking is not None {
        res = invalid_starred_expression_unpacking as expr;
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = rule_expression(p);
        if a is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_starred(
                a, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    invalid_starred_expression = rule_invalid_starred_expression(p);
    if invalid_starred_expression is not None {
        res = invalid_starred_expression as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_statement(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    a = rule_compound_stmt(p);
    if a is not None {
        res = pa_singleton_seq_stmt(a);
        return res;
    }
    peg_reset(p, mark);
    a_2 = rule_simple_stmts(p);
    if a_2 is not None {
        res = a_2;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_statement_newline(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: list[stmt] | None = None;
    a = rule_single_compound_stmt(p);
    if a is not None {
        _newline = peg_expect_token(p, NEWLINE);
        if _newline is not None {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    simple_stmts = rule_simple_stmts(p);
    if simple_stmts is not None {
        res = simple_stmts;
        return res;
    }
    peg_reset(p, mark);
    _newline = peg_expect_token(p, NEWLINE);
    if _newline is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_singleton_seq_stmt(
            pa_check(
                pa_ast_pass(start_lineno, start_col_offset, end_lineno, end_col_offset)
            ) as stmt
        );
        return res;
    }
    peg_reset(p, mark);
    _endmarker = peg_expect_token(p, ENDMARKER);
    if _endmarker is not None {
        res = pa_interactive_exit(p);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_statements(p: peg_parser) -> list[stmt] | None {
    mark = peg_mark(p);
    res: list[stmt] | None = None;
    a = rule__loop1_133(p);
    if a is not None {
        res = pa_seq_flatten(a);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_string(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    s = peg_expect_token(p, STRING);
    if s is not None {
        res = pa_constant_from_string(s);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_strings(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_strings);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    invalid_string_tstring_concat = rule_invalid_string_tstring_concat(p);
    if invalid_string_tstring_concat is not None {
        res = invalid_string_tstring_concat as expr;
        peg_insert_memo(p, mark, RULE_strings, res);
        return res;
    }
    peg_reset(p, mark);
    a = rule__loop1_88(p);
    if a is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_concatenate_strings(a);
        peg_insert_memo(p, mark, RULE_strings, res);
        return res;
    }
    peg_reset(p, mark);
    a = rule__loop1_89(p);
    if a is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_concatenate_tstrings(a);
        peg_insert_memo(p, mark, RULE_strings, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_strings, None);
    return None;
}

def rule_subject_expr(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    value = rule_star_named_expression(p);
    if value is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            values = rule_star_named_expressions(p);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                pa_check(pa_seq_insert_front_expr(value, values)) as list[expr],
                Load(),
                start_lineno,
                start_col_offset,
                end_lineno,
                end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    named_expression = rule_named_expression(p);
    if named_expression is not None {
        res = named_expression;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_sum(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_sum);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_sum, res);
        peg_reset(p, mark);
        raw = rule_sum_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_sum_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_sum(p);
    if a is not None {
        lit = peg_expect_token(p, 14);
        if lit is not None {
            b = rule_term(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    Add(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_sum(p);
    if a is not None {
        lit = peg_expect_token(p, 15);
        if lit is not None {
            b = rule_term(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    Sub(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    term = rule_term(p);
    if term is not None {
        res = term;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_t_lookahead(p: peg_parser) -> object | None {
    mark = peg_mark(p);
    res: object | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        res = lit as object;
        peg_reset(p, mark);
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 9);
    if lit is not None {
        res = lit as object;
        peg_reset(p, mark);
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 23);
    if lit is not None {
        res = lit as object;
        peg_reset(p, mark);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_t_primary(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_t_primary);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_t_primary, res);
        peg_reset(p, mark);
        raw = rule_t_primary_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_t_primary_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            b = pa_name_from_token(peg_expect_token(p, NAME));
            if b is not None {
                if ((rule_t_lookahead(p) is not None)) {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = pa_ast_attribute(
                        a,
                        pa_name_id(b),
                        Load(),
                        start_lineno,
                        start_col_offset,
                        end_lineno,
                        end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 9);
        if lit is not None {
            b_2 = rule_slices(p);
            if b_2 is not None {
                lit_1 = peg_expect_token(p, 10);
                if lit_1 is not None {
                    if ((rule_t_lookahead(p) is not None)) {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_ast_subscript(
                            a,
                            b_2,
                            Load(),
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_t_primary(p);
    if a is not None {
        b_3 = rule_genexp(p);
        if b_3 is not None {
            if ((rule_t_lookahead(p) is not None)) {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_call(
                    a,
                    pa_check(pa_singleton_seq_expr(b_3)) as list[expr],
                    None,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            b_4 = rule_arguments(p);
            lit_1 = peg_expect_token(p, 8);
            if lit_1 is not None {
                if ((rule_t_lookahead(p) is not None)) {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = pa_ast_call(
                        a,
                        (pa_call_args(b_4) if b_4 is not None else []),
                        (pa_call_keywords(b_4) if b_4 is not None else []),
                        start_lineno,
                        start_col_offset,
                        end_lineno,
                        end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_atom(p);
    if a is not None {
        if ((rule_t_lookahead(p) is not None)) {
            res = a;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_target_with_star_atom(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_target_with_star_atom);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 23);
        if lit is not None {
            b = pa_name_from_token(peg_expect_token(p, NAME));
            if b is not None {
                if ((rule_t_lookahead(p) is None)) {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = pa_ast_attribute(
                        a,
                        pa_name_id(b),
                        Store(),
                        start_lineno,
                        start_col_offset,
                        end_lineno,
                        end_col_offset
                    );
                    peg_insert_memo(p, mark, RULE_target_with_star_atom, res);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule_t_primary(p);
    if a is not None {
        lit = peg_expect_token(p, 9);
        if lit is not None {
            b_2 = rule_slices(p);
            if b_2 is not None {
                lit_1 = peg_expect_token(p, 10);
                if lit_1 is not None {
                    if ((rule_t_lookahead(p) is None)) {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = pa_ast_subscript(
                            a,
                            b_2,
                            Store(),
                            start_lineno,
                            start_col_offset,
                            end_lineno,
                            end_col_offset
                        );
                        peg_insert_memo(p, mark, RULE_target_with_star_atom, res);
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    star_atom = rule_star_atom(p);
    if star_atom is not None {
        res = star_atom;
        peg_insert_memo(p, mark, RULE_target_with_star_atom, res);
        return res;
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_target_with_star_atom, None);
    return None;
}

def rule_term(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_term);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    res: expr | None = None;
    resmark = mark;
    while True {
        peg_update_memo(p, mark, RULE_term, res);
        peg_reset(p, mark);
        raw = rule_term_raw(p);
        if peg_has_error(p) {
            return None;
        }
        if raw is None or peg_mark(p) <= resmark {
            break;
        }
        resmark = peg_mark(p);
        res = raw;
    }
    return peg_left_rec_finish(p, resmark, res) as expr | None;
}

def rule_term_raw(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = rule_term(p);
    if a is not None {
        lit = peg_expect_token(p, 16);
        if lit is not None {
            b = rule_factor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    Mult(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_term(p);
    if a is not None {
        lit = peg_expect_token(p, 17);
        if lit is not None {
            b = rule_factor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    Div(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_term(p);
    if a is not None {
        lit = peg_expect_token(p, 47);
        if lit is not None {
            b = rule_factor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    FloorDiv(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_term(p);
    if a is not None {
        lit = peg_expect_token(p, 24);
        if lit is not None {
            b = rule_factor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    Mod(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    a = rule_term(p);
    if a is not None {
        lit = peg_expect_token(p, 49);
        if lit is not None {
            b = rule_factor(p);
            if b is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_ast_binop(
                    a,
                    MatMult(),
                    b,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_factor = rule_invalid_factor(p);
    if invalid_factor is not None {
        res = invalid_factor as expr;
        return res;
    }
    peg_reset(p, mark);
    factor = rule_factor(p);
    if factor is not None {
        res = factor;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_try_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_try_stmt = rule_invalid_try_stmt(p);
    if invalid_try_stmt is not None {
        res = invalid_try_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 692);
    if kw is not None {
        forced = peg_expect_forced_token(p, 11, ':');
        if forced is not None {
            b = rule_block(p);
            if b is not None {
                f = rule_finally_block(p);
                if f is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = Try(
                        body=(b if b is not None else []),
                        handlers=[],
                        orelse=pa_stmt_list_or_empty(None),
                        finalbody=(f if f is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 692);
    if kw is not None {
        forced = peg_expect_forced_token(p, 11, ':');
        if forced is not None {
            b = rule_block(p);
            if b is not None {
                ex = rule__loop1_92(p);
                if ex is not None {
                    el = rule_else_block(p);
                    f = rule_finally_block(p);
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = Try(
                        body=(b if b is not None else []),
                        handlers=(ex if ex is not None else []),
                        orelse=pa_stmt_list_or_empty(el),
                        finalbody=(f if f is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 692);
    if kw is not None {
        forced = peg_expect_forced_token(p, 11, ':');
        if forced is not None {
            b = rule_block(p);
            if b is not None {
                ex = rule__loop1_93(p);
                if ex is not None {
                    el = rule_else_block(p);
                    f = rule_finally_block(p);
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = TryStar(
                        body=(b if b is not None else []),
                        handlers=(ex if ex is not None else []),
                        orelse=pa_stmt_list_or_empty(el),
                        finalbody=(f if f is not None else []),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_tstring(p: peg_parser) -> expr | None {
    hit = peg_check_memo(p, RULE_tstring);
    if hit.hit {
        return hit.result as expr | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    a = peg_expect_token(p, TSTRING_START);
    if a is not None {
        b = rule__loop0_134(p);
        if b is not None {
            c = peg_expect_token(p, TSTRING_END);
            if c is not None {
                res = pa_template_str(
                    a, b, c, start_lineno, start_col_offset, end_lineno, end_col_offset
                );
                peg_insert_memo(p, mark, RULE_tstring, res);
                return res;
            }
        }
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_tstring, None);
    return None;
}

def rule_tstring_format_spec(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    t = peg_expect_token(p, TSTRING_MIDDLE);
    if t is not None {
        res = pa_decoded_constant_from_token(t);
        return res;
    }
    peg_reset(p, mark);
    tstring_format_spec_replacement_field = rule_tstring_format_spec_replacement_field(
        p
    );
    if tstring_format_spec_replacement_field is not None {
        res = tstring_format_spec_replacement_field;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_tstring_format_spec_replacement_field(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_annotated_rhs(p);
        if a is not None {
            debug_expr = peg_expect_token(p, 22);
            conversion = rule_fstring_conversion(p);
            format = rule_tstring_full_format_spec(p);
            rbrace = peg_expect_token(p, 26);
            if rbrace is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_formatted_value(
                    p,
                    a,
                    debug_expr,
                    conversion,
                    format,
                    rbrace,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_tstring_replacement_field = rule_invalid_tstring_replacement_field(p);
    if invalid_tstring_replacement_field is not None {
        res = invalid_tstring_replacement_field as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_tstring_full_format_spec(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    colon = peg_expect_token(p, 11);
    if colon is not None {
        spec = rule__loop0_135(p);
        if spec is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_setup_full_format_spec(
                colon, spec, start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_tstring_middle(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    tstring_replacement_field = rule_tstring_replacement_field(p);
    if tstring_replacement_field is not None {
        res = tstring_replacement_field;
        return res;
    }
    peg_reset(p, mark);
    t = peg_expect_token(p, TSTRING_MIDDLE);
    if t is not None {
        res = pa_constant_from_token(t);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_tstring_replacement_field(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 25);
    if lit is not None {
        a = rule_annotated_rhs(p);
        if a is not None {
            debug_expr = peg_expect_token(p, 22);
            conversion = rule_fstring_conversion(p);
            format = rule_tstring_full_format_spec(p);
            rbrace = peg_expect_token(p, 26);
            if rbrace is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = pa_interpolation(
                    p,
                    a,
                    debug_expr,
                    conversion,
                    format,
                    rbrace,
                    start_lineno,
                    start_col_offset,
                    end_lineno,
                    end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    invalid_tstring_replacement_field = rule_invalid_tstring_replacement_field(p);
    if invalid_tstring_replacement_field is not None {
        res = invalid_tstring_replacement_field as expr;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_tuple(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    lit = peg_expect_token(p, 7);
    if lit is not None {
        a = rule__tmp_136(p);
        lit_1 = peg_expect_token(p, 8);
        if lit_1 is not None {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = pa_ast_tuple(
                a, Load(), start_lineno, start_col_offset, end_lineno, end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_alias(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    soft = peg_expect_soft_keyword(p, "type");
    if soft is not None {
        n = pa_name_from_token(peg_expect_token(p, NAME));
        if n is not None {
            t = rule_type_params(p);
            lit = peg_expect_token(p, 22);
            if lit is not None {
                b = rule_expression(p);
                if b is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = TypeAlias(
                        name=pa_check(pa_set_context(n, Store())) as expr,
                        type_params=(t if t is not None else []),
                        value=b,
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_expressions(p: peg_parser) -> list[expr] | None {
    mark = peg_mark(p);
    res: list[expr] | None = None;
    a = rule__gather_138(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            lit_1 = peg_expect_token(p, 16);
            if lit_1 is not None {
                b = rule_expression(p);
                if b is not None {
                    lit_2 = peg_expect_token(p, 12);
                    if lit_2 is not None {
                        lit_3 = peg_expect_token(p, 35);
                        if lit_3 is not None {
                            c = rule_expression(p);
                            if c is not None {
                                res = pa_seq_append_to_end_expr(
                                    pa_check(pa_seq_append_to_end_expr(a, b)) as list[
                                        expr
                                    ],
                                    c
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule__gather_138(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            lit_1 = peg_expect_token(p, 16);
            if lit_1 is not None {
                b = rule_expression(p);
                if b is not None {
                    res = pa_seq_append_to_end_expr(a, b);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    a = rule__gather_138(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        if lit is not None {
            lit_1 = peg_expect_token(p, 35);
            if lit_1 is not None {
                b = rule_expression(p);
                if b is not None {
                    res = pa_seq_append_to_end_expr(a, b);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a_4 = rule_expression(p);
        if a_4 is not None {
            lit_1 = peg_expect_token(p, 12);
            if lit_1 is not None {
                lit_2 = peg_expect_token(p, 35);
                if lit_2 is not None {
                    b = rule_expression(p);
                    if b is not None {
                        res = pa_seq_append_to_end_expr(
                            pa_check(pa_singleton_seq_expr(a_4)) as list[expr], b
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a_5 = rule_expression(p);
        if a_5 is not None {
            res = pa_singleton_seq_expr(a_5);
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a_6 = rule_expression(p);
        if a_6 is not None {
            res = pa_singleton_seq_expr(a_6);
            return res;
        }
    }
    peg_reset(p, mark);
    a = rule__gather_138(p);
    if a is not None {
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_param(p: peg_parser) -> type_param | None {
    hit = peg_check_memo(p, RULE_type_param);
    if hit.hit {
        return hit.result as type_param | None;
    }
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: type_param | None = None;
    a = pa_name_from_token(peg_expect_token(p, NAME));
    if a is not None {
        b = rule_type_param_bound(p);
        c = rule_type_param_default(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = TypeVar(
            name=pa_name_id(a),
            bound=b,
            default_value=c,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        peg_insert_memo(p, mark, RULE_type_param, res);
        return res;
    }
    peg_reset(p, mark);
    invalid_type_param = rule_invalid_type_param(p);
    if invalid_type_param is not None {
        res = invalid_type_param as type_param;
        peg_insert_memo(p, mark, RULE_type_param, res);
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 16);
    if lit is not None {
        a = pa_name_from_token(peg_expect_token(p, NAME));
        if a is not None {
            b = rule_type_param_starred_default(p);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = TypeVarTuple(
                name=pa_name_id(a),
                default_value=b,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            peg_insert_memo(p, mark, RULE_type_param, res);
            return res;
        }
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 35);
    if lit is not None {
        a = pa_name_from_token(peg_expect_token(p, NAME));
        if a is not None {
            b = rule_type_param_default(p);
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = ParamSpec(
                name=pa_name_id(a),
                default_value=b,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            peg_insert_memo(p, mark, RULE_type_param, res);
            return res;
        }
    }
    peg_reset(p, mark);
    peg_insert_memo(p, mark, RULE_type_param, None);
    return None;
}

def rule_type_param_bound(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 11);
    if lit is not None {
        e = rule_expression(p);
        if e is not None {
            res = e;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_param_default(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 22);
    if lit is not None {
        e = rule_expression(p);
        if e is not None {
            res = e;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_param_seq(p: peg_parser) -> list[type_param] | None {
    mark = peg_mark(p);
    res: list[type_param] | None = None;
    a = rule__gather_140(p);
    if a is not None {
        lit = peg_expect_token(p, 12);
        res = a;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_param_starred_default(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    res: expr | None = None;
    lit = peg_expect_token(p, 22);
    if lit is not None {
        e = rule_star_expression(p);
        if e is not None {
            res = e;
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_type_params(p: peg_parser) -> list[type_param] | None {
    mark = peg_mark(p);
    res: list[type_param] | None = None;
    invalid_type_params = rule_invalid_type_params(p);
    if invalid_type_params is not None {
        res = invalid_type_params as list[type_param];
        return res;
    }
    peg_reset(p, mark);
    lit = peg_expect_token(p, 9);
    if lit is not None {
        t = rule_type_param_seq(p);
        if t is not None {
            lit_1 = peg_expect_token(p, 10);
            if lit_1 is not None {
                res = t;
                return res;
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_value_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    attr = rule_attr(p);
    if attr is not None {
        if (
            (
                peg_negative_lookahead_token(p, 23)
                and peg_negative_lookahead_token(p, 7)
                and peg_negative_lookahead_token(p, 22)
            )
        ) {
            end_tok = peg_get_last_nonnwhitespace_token(p);
            if end_tok is None {
                p.error_indicator = True;
                peg_reset(p, mark);
                return None;
            }
            end_lineno = end_tok.end_lineno;
            end_col_offset = end_tok.end_col_offset;
            res = MatchValue(
                value=attr,
                lineno=start_lineno,
                col_offset=start_col_offset,
                end_lineno=end_lineno,
                end_col_offset=end_col_offset
            );
            return res;
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_while_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_while_stmt = rule_invalid_while_stmt(p);
    if invalid_while_stmt is not None {
        res = invalid_while_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 693);
    if kw is not None {
        a = rule_named_expression(p);
        if a is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                b = rule_block(p);
                if b is not None {
                    c = rule_else_block(p);
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = While(
                        `test=a,
                        body=(b if b is not None else []),
                        orelse=pa_stmt_list_or_empty(c),
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    return None;
}

def rule_wildcard_pattern(p: peg_parser) -> pattern | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: pattern | None = None;
    soft = peg_expect_soft_keyword(p, "_");
    if soft is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = MatchAs(
            pattern=None,
            name=None,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_with_item(p: peg_parser) -> withitem | None {
    mark = peg_mark(p);
    res: withitem | None = None;
    e = rule_expression(p);
    if e is not None {
        kw = peg_expect_token(p, 694);
        if kw is not None {
            t = rule_star_target(p);
            if t is not None {
                if (
                    (
                        peg_positive_lookahead_token(p, 12)
                        or peg_positive_lookahead_token(p, 8)
                        or peg_positive_lookahead_token(p, 11)
                    )
                ) {
                    res = withitem(context_expr=e, optional_vars=t);
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_with_item = rule_invalid_with_item(p);
    if invalid_with_item is not None {
        res = invalid_with_item as withitem;
        return res;
    }
    peg_reset(p, mark);
    e = rule_expression(p);
    if e is not None {
        res = withitem(context_expr=e, optional_vars=None);
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_with_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    invalid_with_stmt_indent = rule_invalid_with_stmt_indent(p);
    if invalid_with_stmt_indent is not None {
        res = invalid_with_stmt_indent as stmt;
        return res;
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 700);
    if kw is not None {
        lit = peg_expect_token(p, 7);
        if lit is not None {
            a = rule__gather_142(p);
            if a is not None {
                lit_1 = peg_expect_token(p, 12);
                lit_2 = peg_expect_token(p, 8);
                if lit_2 is not None {
                    lit_3 = peg_expect_token(p, 11);
                    if lit_3 is not None {
                        tc = peg_expect_token(p, TYPE_COMMENT);
                        b = rule_block(p);
                        if b is not None {
                            end_tok = peg_get_last_nonnwhitespace_token(p);
                            if end_tok is None {
                                p.error_indicator = True;
                                peg_reset(p, mark);
                                return None;
                            }
                            end_lineno = end_tok.end_lineno;
                            end_col_offset = end_tok.end_col_offset;
                            res = With(
                                items=(a if a is not None else []),
                                body=(b if b is not None else []),
                                type_comment=tc.spelling if tc is not None else None,
                                lineno=start_lineno,
                                col_offset=start_col_offset,
                                end_lineno=end_lineno,
                                end_col_offset=end_col_offset
                            );
                            return res;
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 700);
    if kw is not None {
        a = rule__gather_142(p);
        if a is not None {
            lit = peg_expect_token(p, 11);
            if lit is not None {
                tc = peg_expect_token(p, TYPE_COMMENT);
                b = rule_block(p);
                if b is not None {
                    end_tok = peg_get_last_nonnwhitespace_token(p);
                    if end_tok is None {
                        p.error_indicator = True;
                        peg_reset(p, mark);
                        return None;
                    }
                    end_lineno = end_tok.end_lineno;
                    end_col_offset = end_tok.end_col_offset;
                    res = With(
                        items=(a if a is not None else []),
                        body=(b if b is not None else []),
                        type_comment=tc.spelling if tc is not None else None,
                        lineno=start_lineno,
                        col_offset=start_col_offset,
                        end_lineno=end_lineno,
                        end_col_offset=end_col_offset
                    );
                    return res;
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    if kw is not None {
        kw_1 = peg_expect_token(p, 700);
        if kw_1 is not None {
            lit = peg_expect_token(p, 7);
            if lit is not None {
                a = rule__gather_142(p);
                if a is not None {
                    lit_1 = peg_expect_token(p, 12);
                    lit_2 = peg_expect_token(p, 8);
                    if lit_2 is not None {
                        lit_3 = peg_expect_token(p, 11);
                        if lit_3 is not None {
                            b = rule_block(p);
                            if b is not None {
                                end_tok = peg_get_last_nonnwhitespace_token(p);
                                if end_tok is None {
                                    p.error_indicator = True;
                                    peg_reset(p, mark);
                                    return None;
                                }
                                end_lineno = end_tok.end_lineno;
                                end_col_offset = end_tok.end_col_offset;
                                res = AsyncWith(
                                    items=(a if a is not None else []),
                                    body=(b if b is not None else []),
                                    type_comment=None,
                                    lineno=start_lineno,
                                    col_offset=start_col_offset,
                                    end_lineno=end_lineno,
                                    end_col_offset=end_col_offset
                                );
                                return res;
                            }
                        }
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 699);
    if kw is not None {
        kw_1 = peg_expect_token(p, 700);
        if kw_1 is not None {
            a = rule__gather_142(p);
            if a is not None {
                lit = peg_expect_token(p, 11);
                if lit is not None {
                    tc = peg_expect_token(p, TYPE_COMMENT);
                    b = rule_block(p);
                    if b is not None {
                        end_tok = peg_get_last_nonnwhitespace_token(p);
                        if end_tok is None {
                            p.error_indicator = True;
                            peg_reset(p, mark);
                            return None;
                        }
                        end_lineno = end_tok.end_lineno;
                        end_col_offset = end_tok.end_col_offset;
                        res = AsyncWith(
                            items=(a if a is not None else []),
                            body=(b if b is not None else []),
                            type_comment=tc.spelling if tc is not None else None,
                            lineno=start_lineno,
                            col_offset=start_col_offset,
                            end_lineno=end_lineno,
                            end_col_offset=end_col_offset
                        );
                        return res;
                    }
                }
            }
        }
    }
    peg_reset(p, mark);
    invalid_with_stmt = rule_invalid_with_stmt(p);
    if invalid_with_stmt is not None {
        res = invalid_with_stmt as stmt;
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_yield_expr(p: peg_parser) -> expr | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: expr | None = None;
    kw = peg_expect_token(p, 703);
    if kw is not None {
        kw_1 = peg_expect_token(p, 702);
        if kw_1 is not None {
            a = rule_expression(p);
            if a is not None {
                end_tok = peg_get_last_nonnwhitespace_token(p);
                if end_tok is None {
                    p.error_indicator = True;
                    peg_reset(p, mark);
                    return None;
                }
                end_lineno = end_tok.end_lineno;
                end_col_offset = end_tok.end_col_offset;
                res = YieldFrom(
                    value=a,
                    lineno=start_lineno,
                    col_offset=start_col_offset,
                    end_lineno=end_lineno,
                    end_col_offset=end_col_offset
                );
                return res;
            }
        }
    }
    peg_reset(p, mark);
    kw = peg_expect_token(p, 703);
    if kw is not None {
        a = rule_star_expressions(p);
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = Yield(
            value=a,
            lineno=start_lineno,
            col_offset=start_col_offset,
            end_lineno=end_lineno,
            end_col_offset=end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def rule_yield_stmt(p: peg_parser) -> stmt | None {
    mark = peg_mark(p);
    start_lineno = 1;
    start_col_offset = 0;
    end_lineno = 1;
    end_col_offset = 0;
    if peg_fill_token(p) {
        t0 = p.tokens[p.mark];
        start_lineno = t0.lineno;
        start_col_offset = t0.col_offset;
    }
    res: stmt | None = None;
    y = rule_yield_expr(p);
    if y is not None {
        end_tok = peg_get_last_nonnwhitespace_token(p);
        if end_tok is None {
            p.error_indicator = True;
            peg_reset(p, mark);
            return None;
        }
        end_lineno = end_tok.end_lineno;
        end_col_offset = end_tok.end_col_offset;
        res = pa_ast_expr_stmt(
            y, start_lineno, start_col_offset, end_lineno, end_col_offset
        );
        return res;
    }
    peg_reset(p, mark);
    return None;
}

def _build_keyword_lists -> list[list[peg_keyword_entry]] {
    lists: list[list[peg_keyword_entry]] = [];
    i = 0;
    while i <= 8 {
        lists.append([]);
        i = i + 1;
    }
    lists[2].append(peg_keyword_entry(spelling="as", tok_type=694));
    lists[2].append(peg_keyword_entry(spelling="if", tok_type=623));
    lists[2].append(peg_keyword_entry(spelling="or", tok_type=521));
    lists[2].append(peg_keyword_entry(spelling="in", tok_type=672));
    lists[2].append(peg_keyword_entry(spelling="is", tok_type=661));
    lists[3].append(peg_keyword_entry(spelling="def", tok_type=574));
    lists[3].append(peg_keyword_entry(spelling="for", tok_type=621));
    lists[3].append(peg_keyword_entry(spelling="try", tok_type=692));
    lists[3].append(peg_keyword_entry(spelling="and", tok_type=518));
    lists[3].append(peg_keyword_entry(spelling="del", tok_type=680));
    lists[3].append(peg_keyword_entry(spelling="not", tok_type=671));
    lists[4].append(peg_keyword_entry(spelling="True", tok_type=668));
    lists[4].append(peg_keyword_entry(spelling="None", tok_type=667));
    lists[4].append(peg_keyword_entry(spelling="with", tok_type=700));
    lists[4].append(peg_keyword_entry(spelling="elif", tok_type=581));
    lists[4].append(peg_keyword_entry(spelling="else", tok_type=606));
    lists[4].append(peg_keyword_entry(spelling="from", tok_type=702));
    lists[4].append(peg_keyword_entry(spelling="pass", tok_type=679));
    lists[5].append(peg_keyword_entry(spelling="False", tok_type=669));
    lists[5].append(peg_keyword_entry(spelling="await", tok_type=505));
    lists[5].append(peg_keyword_entry(spelling="break", tok_type=686));
    lists[5].append(peg_keyword_entry(spelling="class", tok_type=570));
    lists[5].append(peg_keyword_entry(spelling="async", tok_type=699));
    lists[5].append(peg_keyword_entry(spelling="while", tok_type=693));
    lists[5].append(peg_keyword_entry(spelling="raise", tok_type=683));
    lists[5].append(peg_keyword_entry(spelling="yield", tok_type=703));
    lists[6].append(peg_keyword_entry(spelling="assert", tok_type=685));
    lists[6].append(peg_keyword_entry(spelling="except", tok_type=642));
    lists[6].append(peg_keyword_entry(spelling="global", tok_type=688));
    lists[6].append(peg_keyword_entry(spelling="import", tok_type=681));
    lists[6].append(peg_keyword_entry(spelling="lambda", tok_type=663));
    lists[6].append(peg_keyword_entry(spelling="return", tok_type=678));
    lists[7].append(peg_keyword_entry(spelling="finally", tok_type=637));
    lists[8].append(peg_keyword_entry(spelling="continue", tok_type=687));
    lists[8].append(peg_keyword_entry(spelling="nonlocal", tok_type=689));
    return lists;
}

glob SOFT_KEYWORDS: list[str] = ["_", "case", "match", "type"];

def parse_eval_module(p: peg_parser) -> mod | None {
    res = rule_eval(p);
    if res is None {
        return None;
    }
    if peg_expect_token(p, ENDMARKER) is None {
        return None;
    }
    return res;
}

def parse_eval_source(
    source: str, filename: str, flags: int = 0, feature_version: int = 14
) -> mod | None {
    p = peg_parser_from_source(source, filename, False, flags, feature_version);
    peg_set_keywords(p, _build_keyword_lists());
    peg_set_soft_keywords(p, SOFT_KEYWORDS);
    try {
        result = parse_eval_module(p);
    } except syntax_failure as error {
        raise_source_error(p, error);
    }
    return finish_parse(p, result, "eval");
}

def parse_eval_expr(
    source: str, filename: str, flags: int = 0, feature_version: int = 14
) -> expr | None {
    mod = parse_eval_source(source, filename, flags, feature_version);
    if mod is None {
        return None;
    }
    if isinstance(mod, Expression) {
        return (mod as Expression).body;
    }
    return None;
}

def parse_file_module(p: peg_parser) -> mod | None {
    res = rule_file(p);
    if res is None {
        return None;
    }
    if peg_expect_token(p, ENDMARKER) is None {
        return None;
    }
    return res;
}

def parse_file_source(
    source: str, filename: str, flags: int = 0, feature_version: int = 14
) -> mod | None {
    p = peg_parser_from_source(source, filename, True, flags, feature_version);
    peg_set_keywords(p, _build_keyword_lists());
    peg_set_soft_keywords(p, SOFT_KEYWORDS);
    try {
        result = parse_file_module(p);
    } except syntax_failure as error {
        raise_source_error(p, error);
    }
    return finish_parse(p, result, "exec");
}

def parse_file(
    source: str, filename: str, flags: int = 0, feature_version: int = 14
) -> Module | None {
    mod = parse_file_source(source, filename, flags, feature_version);
    if mod is None {
        return None;
    }
    if isinstance(mod, Module) {
        return mod as Module;
    }
    return None;
}

def parse_interactive(
    source: str, filename: str, flags: int = 0, feature_version: int = 14
) -> Interactive | None {
    p = peg_parser_from_source(source, filename, False, flags, feature_version);
    p.tok.single_input = True;
    peg_set_keywords(p, _build_keyword_lists());
    peg_set_soft_keywords(p, SOFT_KEYWORDS);
    try {
        body = rule_statement_newline(p);
    } except syntax_failure as error {
        raise_source_error(p, error);
    }
    if not body {
        finish_parse(p, None, "single");
        return None;
    }
    # A complete interactive statement may be followed only by whitespace or
    # comments. This check must not turn a second statement into incomplete input.
    extra = any(t.tok_type not in (NEWLINE, ENDMARKER) for t in p.tokens[p.mark:]);
    for line in split_source_lines(p.tok.source[p.tok.pos:]) {
        if line.strip() and not line.lstrip().startswith("#") {
            extra = True;
        }
    }
    if extra {
        p.tok.eof_reached = False;
        raise_source_error(
            p,
            syntax_failure(
                "multiple statements found while compiling a single statement"
            )
        );
    }
    return finish_parse(p, Interactive(body=body), "single") as Interactive;
}

def source_column(lines: list[str], line: int, column: int) -> int {
    if line > 0 and line <= len(lines) {
        return source_character_column(lines[line - 1], column);
    }
    return column;
}

def raise_source_error(p: peg_parser, error: syntax_failure) {
    token = p.tokens[-1] if p.tokens else None;
    if p.flags & 0x4000
        and p.tok.eof_reached
        and (token is None or token.tok_type != NUMBER) {
        raise_incomplete_input(p);
    }
    line = error.lineno or (token.lineno if token is not None else 1);
    column = error.offset - 1
        if error.offset >= 0
        else (token.col_offset if token is not None else 0);
    end_line = error.end_lineno or line;
    end_column = error.end_offset - 1 if error.end_offset >= 0 else column + 1;
    lines = split_source_lines(p.tok.source);
    text = lines[line - 1] if line <= len(lines) else None;
    raise syntax_failure(
        error.msg,
        filename=p.filename,
        lineno=line,
        offset=source_column(lines, line, column) + 1,
        text=text,
        end_lineno=end_line,
        end_offset=source_column(lines, end_line, end_column) + 1,
        kind=error.kind
    );
}

def raise_incomplete_input(p: peg_parser) {
    lines = split_source_lines(p.tok.source);
    line = max(1, p.tok.lineno);
    text = lines[line - 1] if line <= len(lines) else "";
    column = source_column(lines, line, p.tok.col_offset) + 1;
    raise syntax_failure(
        "incomplete input",
        filename=p.filename,
        lineno=line,
        offset=column,
        text=text,
        end_lineno=line,
        end_offset=column,
        kind="_IncompleteInputError"
    );
}

def finish_parse(p: peg_parser, result: mod | None, mode: str) -> mod | None {
    if result is not None and not p.error_indicator {
        import from jaclang.compiler.frontend.python.ast_nodes { TypeIgnore }
        import from jaclang.compiler.frontend.python.compiler_validate {
            validate_feature_version
        }
        if isinstance(result, Module) {
            result.type_ignores = [
                TypeIgnore(lineno=line, tag=tag) for (line, tag) in p.type_ignores
            ];
        }
        try {
            validate_feature_version(result, p.feature_version);
        } except syntax_failure as error {
            raise_source_error(p, error);
        }
        return result;
    }
    if p.flags & 0x4000 and p.tok.eof_reached {
        raise_incomplete_input(p);
    }
    # The first pass only recognizes valid syntax. Diagnostic productions run
    # once on failure, with memoized first-pass failures cleared.
    first_bad = p.tokens[-1] if p.tokens else None;
    if p.error is None {
        peg_reset_for_error_pass(p);
        if mode == "eval" {
            parse_eval_module(p);
        } elif mode == "exec" {
            parse_file_module(p);
        } elif mode == "func_type" {
            rule_func_type(p);
        } else {
            rule_statement_newline(p);
        }
    }
    kind = "SyntaxError";
    message = "invalid syntax";
    if first_bad is not None and first_bad.tok_type in (INDENT, DEDENT) {
        kind = "IndentationError";
        message = "unexpected indent"
            if first_bad.tok_type == INDENT
            else "unexpected unindent";
    }
    line = first_bad.lineno if first_bad is not None else 1;
    column = first_bad.col_offset if first_bad is not None else 0;
    end_line = line;
    end_column = column + 1;
    if p.error is not None {
        kind = p.error.kind;
        message = p.error.message;
        line = max(1, p.error.loc.lineno);
        column = max(0, p.error.loc.col_offset);
        end_line = max(line, p.error.loc.end_lineno);
        end_column = max(column + 1, p.error.loc.end_col_offset);
    }
    lines = split_source_lines(p.tok.source);
    text = lines[line - 1] if line > 0 and line <= len(lines) else "";
    if message.startswith("unterminated") {
        text = text.rstrip("\r\n");
    }
    column = source_column(lines, line, column);
    end_column = source_column(lines, end_line, end_column);
    raise syntax_failure(
        message,
        filename=p.filename,
        lineno=line,
        offset=column + 1,
        text=text,
        end_lineno=end_line,
        end_offset=end_column + 1,
        kind=kind
    );
}


def parse_func_type(
    source: str, filename: str, flags: int = 0, feature_version: int = 14
) -> mod | None {
    p = peg_parser_from_source(source, filename, False, flags, feature_version);
    peg_set_keywords(p, _build_keyword_lists());
    peg_set_soft_keywords(p, SOFT_KEYWORDS);
    try {
        result = rule_func_type(p);
    } except syntax_failure as error {
        raise_source_error(p, error);
    }
    return finish_parse(p, result, "func_type");
}
